<!-- seat: sonnet · model: claude-sonnet-5 · round: 5 · head: 20c6610 · pack: 63563afc2e88 · attempt: 1 · recorded: 2026-09-15T10:31:35Z -->
COUNCIL: anomaly-telemetry · round 5 · seat sonnet
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-5
VERDICT: GREEN
ASSUMED CONFIG: shell+Python+markdown toolkit, no compiler/build; single-node CLI hive orchestration; Telemetry Profile row present, default off
RAN: git ls-files '*.sh' | xargs -n1 bash -n; python -m py_compile .claude/hooks/*.py; git ls-files '*.json' | xargs -n1 jq -e .; bash .claude/hooks/handoff.sh status; bash tests/cycle.test.sh; bash tests/council.test.sh; bash tests/driver.test.sh; bash tests/telemetry.test.sh; scripts/telemetry.sh enum/agents/consent/scan/bundle/check/publish (in-COURT and in a /tmp copy); install.sh with --telemetry on/off and --upgrade
PATH: CLI-only (no browser client): scripts/telemetry.sh scan writes memory/stats/anomalies.jsonl -> bundle/check -> publish prints local-commit or gh-PR recipe; install.sh prompts for consent
ASK 1: GREEN - all terminals monitor anomalies and keep a log file - run: VULYK_HIVE=$(pwd) bash scripts/telemetry.sh scan --transcript <fixture ctx.jsonl> saw: new context_high row appended to memory/stats/anomalies.jsonl (value 155000/threshold 140000, model opus); pre-existing log already carries real council_rounds_high/scope_breach rows from past sessions
ASK 2: GREEN - weekly send: local commit into the repo, or PR from other machines - run: bash scripts/telemetry.sh publish --dry-run --week 2026-W38 (in a /tmp copy, consent forced on) saw: with a local VULYK checkout present, prints `cd ... && git add ... && git commit -m 'telemetry: 2026-W38 bundle' && git push`; with no local checkout, prints a full `gh repo fork`/branch/`gh pr create` recipe; nothing was actually pushed or PR'd (grep in tests/telemetry.test.sh's own probes also shows no git push/commit/gh call in the script, matched by my own run)
ASK 3: GREEN - logs maximally anonymized, no personal files - run: bash scripts/telemetry.sh bundle --week 2026-W38 --out /tmp/bundle38.jsonl; bash scripts/telemetry.sh check /tmp/bad.jsonl (a row with a path in `hive`) saw: bundle rows are 10 keys of codes/numbers/week/hashed hive only (no ts/spec/story/ref); check rejected the tainted row with "a string value carries a path, an address or whitespace", exit 1
ASK 4: GREEN - properly wrapped into the VULYK framework - run: bash tests/telemetry.test.sh; bash tests/cycle.test.sh (story 09 cases: anomalies.jsonl rides close-story --commit, ship-check stage 03, scope-check exemption) saw: telemetry.test.sh 213/213 checks passed; cycle.test.sh's anomalies.jsonl-specific cases (close-story --commit, ship-check stage 03, scope-check) all ok
ASK 5: GREEN - public GitHub repo documents the telemetry/PR/cleanup story - run: grep -n "Anomaly telemetry" README.md; cat docs/telemetry.md saw: README.md ### "Anomaly telemetry — opt-in, anonymized, never automatic" section describing the installer prompt, PR path, weekly /vulyk-evolve cleanup, and points to docs/telemetry.md, which spells out the full local-row/bundle-row/check/publish contract
ASK 6: GREEN - install/update asks the user once about telemetry, explains it, defaults off - run: bash install.sh /tmp/target (no tty, no flag); bash install.sh /tmp/target --telemetry on; bash install.sh /tmp/target --upgrade (no flag, already answered) saw: no-tty run writes `Telemetry | off` silently (A10-5); --telemetry on writes `Telemetry | on`; a second run with an existing row and no --telemetry flag leaves it unchanged (A10-4, no re-ask); code path (install.sh print_telemetry_explanation + y/N read) confirms explanation text and default-off on empty answer - the actual interactive keystroke could not be exercised since this Windows Git Bash sandbox has no /dev/tty/pty
UNASKED: none
BREACH: none
