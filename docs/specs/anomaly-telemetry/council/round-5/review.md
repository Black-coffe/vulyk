<!-- seat: review · model: unknown · round: 5 · head: 20c6610 · pack: 63563afc2e88 · attempt: 1 · recorded: 2026-09-15T10:31:46Z · verdict: PASS -->
VERDICT: PASS

Reviewed: docs/specs/anomaly-telemetry (plan incl. A19, Contracts "Bundle row" / `bundle` verb, Tradeoffs "Chosen for round 5", `## Descoped`, `## Plan deltas`; brief; story 13; journal; round-4 review and opus seat), diff b15b4ba..20c6610 with round 5's own commits 888bf44 (judge), 9a01714 (story 13), a1d7668 (plan delta), 20c6610 (open-round), ADR-001. Scope of 9a01714: `scripts/telemetry.sh`, `tests/telemetry.test.sh`, its story file and `memory/stats/scope.jsonl` (close-story's write) - no Law 3 violation. `docs/wiki/` does not exist. Did not re-run the suite; ran only what the diff made suspicious: `jq -n '... | strftime("%G-W%V")'` on this machine's jq 1.8.2 (prints `2026-W38` - the story's portability claim does not hold here); `record` in a scratch hive under a Windows-only PATH (script does not start: `dirname`/`grep` missing, so a fully stripped PATH is not the `"ts":""` producer); `bundle --week 2026-W38` over the real 156-row log (154 rows, 15.5 s wall). The fix itself is sound: US (0x1f) is not IFS whitespace, `read -r` keeps an empty leading or trailing field, jq strips US/quote/backslash/line breaks from every value before joining, and case 18 asserts the exact `model`/`agent` pair per row by position plus `check` exit 0 plus a byte-compare of `publish --dry-run`'s bundle against `bundle --out` - on the pre-fix emitter row 1 reads `worker-code/`, so the case fails when the feature is broken. Round-4 ask 3's condition is met.

## Critical

None.

## Major

1. `docs/specs/anomaly-telemetry/plan.md:96` (Contracts, `bundle` verb: "one `jq` pass per log file maps each local object of the week to its bundle object"), `:62` (Wave 9 bullet: "in one `jq` pass instead of a tab-`IFS` `read`"), `:133` (Tradeoffs: "`bundle` maps the local JSON object to the bundle object in `jq`") vs `scripts/telemetry.sh:546-580` - **plan** - the code does not do what the record says: the jq pass emits US-separated strings and the shell `while IFS=$(printf '\037') read -r ...` loop with its set checks, `week_of` and `printf` still builds the bundle row. The story's acceptance criterion allowed the separator variant and the worker's `## Implementation notes` say so, but the plan delta a1d7668 was committed 18 s after 9a01714 stating the jq-map unconditionally in the contract that seats and future workers read. Condition: the plan's Contracts and Tradeoffs describe the emitter as built (one jq pass to US-separated fields consumed by a non-collapsible-IFS shell loop) or the code is brought to the contract; the two must not disagree.

2. `memory/stats/anomalies.jsonl:154-155` (now committed by 888bf44, the round-4 `judge --commit`) and `scripts/telemetry.sh:189` / `scripts/lib.sh:74` (`now_ts` output substituted unchecked) - **plan** - round-4 review Major 1's condition was "the producer of the two `"ts":""` rows is identified before the log is committed by the next `cycle.sh --commit` verb"; the very next verb committed them with the producer still unknown, and `record` still writes an empty `ts` (a row with no week never bundles). What this round established: `.vulyk/telemetry/seen/` was created at 10:14:31Z by this session's own scan, so the 09:45 scan that wrote the two rows ran with no `--transcript` (or wrote no seen-list) in this hive; a fully Windows-only PATH cannot be the producer (the script dies at `dirname`); the producer is an environment with coreutils present but `date -u` absent or failing, still unnamed. Severity assumes the maintainer's configuration (Windows hive, hooks fired from a driver-spawned or seat session). Condition unchanged from round 4: `record` never writes a row whose `ts` is empty (fallback stamp, or refuse silently), and one recorded `anomaly-scan.sh` run from a real hook payload in this hive shows its rows with `ts` before ask 1 is claimed delivered.

## Minor

3. `docs/specs/anomaly-telemetry/anomaly-telemetry-13-bundle-agent-token.md:47` ("`strftime("%G-W%V")` ... is not portable across jq builds on Windows") - **worker** - the sentence is the stated reason for rejecting the plan's preferred design and it was not checked: this hive's jq 1.8.2 (WinGet build) prints `2026-W38` for it. Condition: the tradeoff names a jq build on which `%G-W%V` fails, or says the choice rests on caution rather than an observed failure.

4. `scripts/telemetry.sh:568` (`week_of` per local row, two `date` spawns each on the GNU-then-BSD fallback path) - **plan** - keeping the shell loop keeps one `date` spawn per local row per week: `bundle --week 2026-W38` over the real 156-row log took 15.5 s here, so a default `publish` (two weeks) is ~30 s and grows linearly with the log. The story did not ask for this; the jq-map design in A19 would have removed it. Condition: the plan records the accepted `bundle`/`publish` cost or a story computes the week inside the one jq pass (falling back to `date` only where jq's `strftime` is unavailable).

5. `memory/stats/scope.jsonl:110` (story 13: declared 2, changed 3, out_of_scope 1) and `memory/stats/anomalies.jsonl:156` (uncommitted, `scope_breach` value 1 for story 13, 10:14:32Z) - **plan** - 9a01714 touched exactly its two files; the third changed path at close-story (10:10:26Z) can only be the Queen's uncommitted `plan.md` delta (committed 3 min later as a1d7668): `scope-check.sh:60-78` excludes the story file and `anomalies.jsonl` but no other `is_paperwork_path` file, so the cycle's own paperwork counts against the worker and the hook turns it into a permanent row that will ride the next `--commit` verb. Same shape as round-4 Minor 8 with a different intruder. Condition: `scope-check` drops every `is_paperwork_path` path from the measurement (or `## Descoped` extends the artefact note to Queen-written paperwork and evolve discounts it).

6. `docs/specs/anomaly-telemetry/anomaly-telemetry-13-bundle-agent-token.md:39-42` - **worker** - all four acceptance boxes are unchecked under `status: done` / `returned: DONE` (the round-4 Minor 5 pattern repeated on the next story). Condition: the boxes reflect what the suite proves.

7. `docs/specs/anomaly-telemetry/plan.md:166` (Plan deltas: round-4 Major 2 and Minors 3, 4, 6, 7, 9, 10 "for the Queen") - **plan** - these are carried into round 5 with neither a story nor a `## Descoped` line; the plan's own rule since round 2 is that nothing stays out without a line there. Condition: each carried finding has a story, a `## Descoped` line with its reason, or an explicit next-spec pointer on the record before ship.

8. `docs/specs/anomaly-telemetry/anomaly-telemetry-13-bundle-agent-token.md:44` ("jq strips US from every value first ... so a hand-edited local row cannot inject a separator") - **worker** - holds for US, quote, backslash and line breaks; it does not hold for a hand-edited numeric field, but `is_number` on `value`/`threshold` and the set checks on the other fields close that, so the claim is complete only together with the loop's checks. Condition: none beyond the note saying so; recorded so nobody removes the loop's checks believing jq alone guards the row.
