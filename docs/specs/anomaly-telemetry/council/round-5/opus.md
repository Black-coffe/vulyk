<!-- seat: opus · model: claude-opus-5[1m] · round: 5 · head: 20c6610 · pack: 63563afc2e88 · attempt: 1 · recorded: 2026-09-15T10:31:41Z -->
COUNCIL: anomaly-telemetry · round 5 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-5
VERDICT: RED
ASSUMED CONFIG: Profile rows are all `<fill in>` placeholders except `| Telemetry | off - ... |`; VULYK's own repo, shell+python+markdown, no runner. So: single local hive, git, jq+python present, no deploy target.
RAN: telemetry.sh enum|agents|consent|record|bundle|check|publish|inbox|scan against three synthetic hives under VULYK_HIVE in the scratchpad; handoff.sh measure on synthetic transcripts; install.sh into a fresh project (--telemetry off, then --upgrade, --upgrade --check); a sourced harness over install.sh:315-355 for the consent prompt; a wrapper counting measure() invocations across two scans of a 60-subagent session
PATH: none named - the Profile's Client path row is still `<fill in>`; walked the CLI surface instead (scripts/telemetry.sh verbs, install.sh, the hook)
ASK 1: GREEN - every VULYK terminal monitors anomalies and keeps a log - run: `telemetry.sh scan --transcript <synthetic> --final` on a hive with council.jsonl/scope.jsonl/journal.md/subagent transcripts saw: all five detectors fired into memory/stats/anomalies.jsonl (context_high 190005/140000, agent_prefix_high 60000/50000, agent_empty, council_rounds_high 4/3, stage_long 76/24, scope_breach 2/0); a second scan added 0 rows. run: `install.sh <fresh> --telemetry off` then strip and `--upgrade` saw: `wire .claude/settings.json -> Stop: anomaly-scan.sh` + SessionEnd, on fresh install and re-wired on upgrade. run: measure-call counter over 60 subagents saw: scan 1 = 61 python starts / 15.8 s, scan 2 = 1 start / 4.4 s - the round-5 bounded-scan repair holds for under-threshold subagents, which are cached by byte size in .vulyk/telemetry/seen/<sid>.
ASK 2: GREEN - weekly send, local into the project or a PR from another machine - run: `telemetry.sh publish` with VULYK_LOCAL at a fake VULYK checkout saw: `wrote .../telemetry/inbox/2026-W38/<hive>.jsonl` plus a printed `cd && git add && git commit && git push` block. run: same with no local checkout saw: the 11-line `gh repo fork ... / git switch -c telemetry/<week>-<hive> / gh pr create --repo Black-coffe/vulyk` recipe, one clone re-based per week, every path single-quoted. Nothing is executed: no git/gh call exists in the script, and `publish` with consent off printed `telemetry: off (Profile row Telemetry) - nothing to send` and created no .vulyk/telemetry at all. Repo side: `telemetry.sh inbox` distilled `<week> <code> <rows> <hives>` and `--clear` staged the week's `git rm`. Weekly cadence is /vulyk-evolve, which CLAUDE.md already says to run weekly.
ASK 3: RED - logs maximally anonymized, no personal files - run: added `.claude/agents/worker-frontend.md` to a hive, `telemetry.sh record agent_prefix_high 60000 50000 --agent worker-frontend`, `bundle --out`, then `check` from both sides saw: the bundle row carries `"agent":"worker-frontend"` verbatim, hive-side `check` exits 0, VULYK-repo-side `check` exits 1 with `agent is not in the agent token set`. The agent token is resolved at bundle time from the hive's own `.claude/agents/*.md` basenames, and ADR-005:57-58 states `.claude/agents` is "exactly where owners add their own agents" - so an owner-chosen filename (`worker-acme-billing`) is free text that leaves the machine. That contradicts the confirmed answer ("только коды и числа ... Свободного текста в отправляемом файле нет вовсе", whose enumerated fields are code, number, threshold, version, tier, model alias, week, hive hash - `agent` is not among them) and docs/telemetry.md:79 / README.md:252 which both promise "Never included: ... free text". Same defect breaks the receiving end: that PR fails the `telemetry-inbox` CI job, and `telemetry.sh inbox` aborts the whole distil (rc=1, no table, nothing cleared) on one such bundle. Everything else held: run: `record` with paths, an email and a Windows path in --spec/--story/--ref then `bundle` saw: ts/spec/story/ref absent from every bundle row, `C:\Users\Andrei` and `andrei@example.com` gone; run: `check` on a poisoned file saw: all five lines rejected (extra key, path in a string, code off the enum, tier 9, not JSON); hive hash reproduced as sha256(toplevel)|cut -c1-12 = 72081a891cf9.
ASK 4: GREEN - properly wrapped into the framework so it does not get lost - run: greps and installs saw: one script owns the path (scripts/telemetry.sh, 873 lines, 9 verbs); a CLAUDE.md Commands row (`bash tests/telemetry.test.sh`); a Profile consent row inside the VULYK:PROFILE markers; docs/telemetry.md as the full contract; CHANGELOG entries; a `telemetry-inbox` CI job that checks `telemetry/inbox/**/*.jsonl`; `.vulyk/` gitignored while `memory/stats/anomalies.jsonl` is committed paperwork beside council.jsonl; driver_refused/driver_relaunched recorded from vulyk-build.md and vulyk-resume.md. Round-5 repair items 2 and 3 verified: docs/telemetry.md:117-130 prints the same 11-line recipe `publish` actually emitted, and scope-check.sh:74 says skills.json is NOT excluded (only anomalies.jsonl is the HOOKFILE).
ASK 5: GREEN - the public GitHub repo says logs are collected, can be pushed as a PR, are cleaned weekly and drive an update - url: COURT/README.md:243-261 saw: "### Anomaly telemetry — opt-in, anonymized, never automatic", the 10 keys and the 8 codes listed inline, "Off by default", "you push the PR into `telemetry/inbox/` yourself", "Weekly, `/vulyk-evolve` in the VULYK repo runs `scripts/telemetry.sh inbox` ... the emptied week directories are staged for deletion in the same changeset ... so the improvements ship with the next release". telemetry/inbox/README.md exists as the landing scaffold.
ASK 6: GREEN - install/update asks in the terminal, explains, says off by default - run: sourced harness over install.sh:315-355 saw: the explanation block ("Telemetry (optional, off by default) ... Never paths, slugs, story names, prompts, emails or any free text. Nothing is ever sent: /vulyk-evolve prints the command and you run it ... Default: off.") followed by `Enable telemetry? [y/N]`; `y|Y|yes|YES|Yes` -> on, anything else -> off; no tty -> no prompt and the row is written `off`; an upgrade of a hive that already answered is not re-asked unless `--telemetry ask`, which I read as the intended "ask once, never nag" rather than a miss. run: `install.sh <fresh> --telemetry off` saw: `| Telemetry | off - ... |` in the target CLAUDE.md and `telemetry.sh consent` -> off; `--upgrade` preserved it.
UNASKED: `docs/telemetry.md` is never installed into a hive (install.sh's copy_tree skips docs/; the fresh install produced only docs/adr, docs/specs, docs/wiki) - yet four in-hive surfaces point at it: the Profile Telemetry row in the installed CLAUDE.md:133, .claude/commands/vulyk-evolve.md:43, bootstrap/interview.md:20, and the installer's own consent explanation ("Details: docs/telemetry.md"). The one document a person needs in order to decide consent is a dead path exactly where the decision is taken; the owner asked for "объяснить, что это", and the explanation's own footnote goes nowhere. Also minor: `publish` keeps no ledger of what was sent, so a second run in the same week reprints a recipe whose `git switch -c` and `gh repo fork --clone` both fail on an existing branch/directory.
BREACH: none
