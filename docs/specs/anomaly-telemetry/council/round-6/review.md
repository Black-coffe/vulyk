<!-- seat: review · model: unknown · round: 6 · head: 42d225d · pack: c3cdb6873008 · attempt: 1 · recorded: 2026-09-15T11:05:10Z · verdict: PASS -->
VERDICT: PASS

Reviewed: docs/specs/anomaly-telemetry (plan incl. A20, Contracts "Agent token set" / Bundle row / `record`/`bundle`/`check` verbs, Tradeoffs "Chosen for round 6", `## Descoped`, `## Plan deltas`; brief; stories 13 and 14; journal; round-5 review and opus seat), diff b15b4ba..42d225d with round 6's own commits c2c717e (judge), 83437ce (story 14), 42d225d (open-round), ADR-001, ADR-005:57-58. Scope of 83437ce: `scripts/telemetry.sh`, `tests/telemetry.test.sh`, `docs/telemetry.md`, `README.md` (all four named in `## Files`), the story file and `memory/stats/scope.jsonl` (close-story's write) - no Law 3 violation; the plan-delta edit the worker saw in its tree was the Queen's and reached git in 42d225d. `docs/wiki/` does not exist. Did not re-run the suite; ran only what the diff made suspicious: `telemetry.sh agents` / `record --agent worker-frontend|worker-code|other|""` / `check` on an out-of-set and an empty `agent` in a scratch hive carrying `.claude/agents/worker-frontend.md` (`other`, `worker-code`, `other`, `""`; `check` exit 1 with `agent is not in the agent token set` on the first line only); `bundle` over a log holding one `"ts":""` row (emits nothing - `week_of` guards the empty `ts` before GNU `date -d ""` could stamp it into the current week); `grep -P '\r'` and `git ls-files --eol` on the test file; `tr -d ''` with an empty set (passes CR through). The fix is sound and complete for its ask: `AGENTS` is a literal in the script, `agent_set` no longer touches the filesystem, `record`:188, `bundle_emit`:583 and `check`:636/653 all go through it, the list equals this repo's 13 `.claude/agents/*.md` basenames, and case 19 fails on the pre-fix resolver (the fixture hive carries `worker-frontend.md`, so the old `agents` would print it and `expect_absent` goes red; `--agent worker-frontend` would record verbatim). Case 20 is a real drift guard, not a smoke check. The ADR-005 sentence the code comment and story rest on ("exactly where owners add their own agents") is at `docs/adr/005-*.md:57-58` as quoted. Round-5 ask 3's condition is met.

## Critical

None.

## Major

1. `scripts/telemetry.sh:189-190` (`"$(now_ts)"` substituted unchecked into the row), `memory/stats/anomalies.jsonl:154-155` (two committed `"ts":""` rows), `docs/specs/anomaly-telemetry/plan.md:233` (Plan deltas: "still not cut, Queen decides") - **plan** - round-4 review Major 1 and round-5 review Major 2 carried into round 6 with neither a story, a fold into 14, nor a `## Descoped` line: the plan offered the Queen "a one-bullet fold into 14 with the owner's nod or a wave 11" and neither happened, so at the ceiling round the log still accepts a row with no timestamp from an unnamed hook environment. Severity assumes the maintainer's configuration (Windows hive, hook fired from a driver- or seat-spawned session); a `ts`-less row is dropped by `bundle` (verified), so nothing leaks - it is ask 1's "ведут файл логирования" that is degraded. Condition unchanged from rounds 4 and 5: `record` never writes a row whose `ts` is empty (fallback stamp or silent refuse), and either one recorded `anomaly-scan.sh` run from a real hook payload in this hive shows its rows with `ts`, or `## Descoped` states that the producer stays unidentified at ship and why.

## Minor

2. `tests/telemetry.test.sh:982, 1006, 1007, 1012, 1014, 1022` (raw 0x0D byte inside `tr -d '<CR>'` where every pre-existing case writes `tr -d '\r'`, e.g. `:113`, `:282`) and `scripts/telemetry.sh:73-74` (`printf '%s<LF>'` with a literal newline in the format) - **worker** - the story's own map slice warns of "the heredoc-backslash trap in this suite"; the worker fell into it, and `git ls-files --eol` now classifies the test file as `i/-text` (lone CRs). Works today, but any CR normalisation (editor, `dos2unix`, a future `.gitattributes` pass) turns the six deletes into `tr -d ''`, which passes CR through, and on Windows - where jq emits CRLF - the six `expect_eq`s go red with visually identical strings. Condition: the new cases and `agent_set` use the escaped `'\r'` / `'\n'` forms the rest of the file uses, and the file carries no lone CR byte.

3. `docs/telemetry.md:33-36` ("Any other agent ... is reported as `other`, and so is the free-form dispatch `name`") - **worker** - the dispatch `name` is never read by `measure` or `record` (only `agentType` is), so "reported as `other`" implies a mapping that does not exist; the pre-fix sentence ("never the free-form dispatch `name`") was the accurate one. Condition: the sentence says the dispatch `name` is never read, not that it maps to `other`.

4. `docs/specs/anomaly-telemetry/anomaly-telemetry-14-agent-token-fixed-set.md:41-44` - **worker** - all four acceptance boxes are unchecked under `status: done` / `returned: DONE` (round-4 Minor 5 and round-5 Minor 6 repeated on the third story in a row). Condition: the boxes reflect what the suite proves.

5. `memory/stats/scope.jsonl:111-112` (two close-story rows for story 14, 22 s apart: `changed 5 / out_of_scope 1`, then `changed 6 / out_of_scope 2`) and `scripts/scope-check.sh:70-78` - **plan** - `close-story` ran twice with nothing in `journal.md` saying why, and the second run counted the first run's own `scope.jsonl` write as the worker's out-of-scope path beside the Queen's `plan.md` delta; `scope-check` excludes the story file and `anomalies.jsonl` but not its own ledger. Same artefact family as round-4 Minor 8 / round-5 Minor 5 (already in `## Descoped`) with a third intruder. Condition: a re-run of `close-story` does not count `memory/stats/scope.jsonl` against the story, or the `## Descoped` artefact line names that shape too so evolve discounts it.

6. `docs/specs/anomaly-telemetry/plan.md:222` (`## Descoped` carry-list with the `telemetry-hardening` pointer) vs the round-5 opus UNASKED - **plan** - `docs/telemetry.md` is not installed into hives while the installed Profile row, the installer's consent explanation ("Details: docs/telemetry.md"), `vulyk-evolve.md:43` and `bootstrap/interview.md:20` point at it; the plan mentions it only inside the round-5 Plan-deltas paragraph, not as a `## Descoped` line, so a reader of the descoped list alone will not find it. Does not narrow a confirmed ask (the explanation the owner asked for is printed in the terminal itself). Condition: the dead in-hive pointer is a `## Descoped` line with the next-spec pointer, or the installer's explanation cites the public URL instead of a path that does not exist in the hive.
