<!-- seat: opus · model: claude-opus-5[1m] · round: 6 · head: 42d225d · pack: c3cdb6873008 · attempt: 1 · recorded: 2026-09-15T11:04:46Z -->
COUNCIL: anomaly-telemetry · round 6 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-6
VERDICT: GREEN
ASSUMED CONFIG: Profile rows are unfilled placeholders except `Telemetry | off ...`; judged as the VULYK repo itself - shell+python+markdown toolkit, no runtime service, no Browser MCP.
RAN: install.sh (fresh + --upgrade + --telemetry on/ask) into scratch hives; telemetry.sh enum/agents/consent/record/scan/bundle/check/publish/inbox against a scratch hive and a scratch VULYK repo; anomaly-scan.sh with real/garbage/no payload, no jq, missing script; handoff.sh measure via synthetic transcripts (42 subagents); gh pr create --help; gh auth status
PATH: none named (Profile Client path `<fill in>`); walked the CLI entry points instead - `bash install.sh <target> [--upgrade]` and `bash scripts/telemetry.sh <verb>` end-to-end, hive -> bundle -> inbox -> staged deletion
ASK 1: GREEN - every VULYK terminal monitors anomalies and keeps a log - run: fresh `install.sh` then `telemetry.sh scan --transcript <synthetic> --final` saw: anomaly-scan.sh wired twice in the hive's settings.json (Stop + SessionEnd) and all five detectors fired into memory/stats/anomalies.jsonl - context_high 150010/140000, agent_prefix_high 65000/50000 (agent cycle-clerk), agent_empty only under --final, council_rounds_high 4/3, stage_long 72/24, scope_breach 2/0; rescan appended nothing (idempotent on (code,ref)); hook exit 0 and silent with garbage payload, no stdin, PATH without jq, and telemetry.sh deleted; 24 parallel `record` calls produced 24 valid JSON lines, 24 distinct refs
ASK 2: GREEN - weekly send, local into the main project, else a pull request - run: `telemetry.sh publish` with consent off, then on, with and without VULYK_LOCAL saw: off -> `telemetry: off (Profile row Telemetry) - nothing to send`, nothing written; on + VULYK_LOCAL -> wrote `<repo>/telemetry/inbox/2026-W38/72081a891cf9.jsonl` and printed the add/commit/push recipe (path held a space and a `#`, both survived single-quoting); on without a local checkout -> the 11-line fork/branch/copy/commit/push/`gh pr create` recipe, one branch per week off a captured `$base`; no git/gh call is made by the script. Repo side: `telemetry.sh inbox` distilled two merged bundles to `<week> <code> <rows> <hives>` and `--clear` staged the two deletions (`git status` = 2 `D`), README.md untouched
ASK 3: GREEN - maximally anonymized, no personal files - run: `record` with an owner-added agent, a spec slug, a story path and a junk model, then `bundle` + `check` saw: bundle rows carry exactly the 10 keys - no ts/spec/story/ref; `my-secret-project-agent` (a real file under .claude/agents/) became `other`, `gpt-fake` became ""; hive = 12 hex of the toplevel path; a hand-poisoned bundle was rejected line by line - `key set is not the 10 bundle keys`, `a string value carries a path, an address or whitespace` for both `C:/Users/Andrei` and `andrei@mail.com`; `check` also passes a CRLF bundle and `publish` refuses to copy anything a failing `check` produced; `inbox --clear` deleted nothing when one bundle failed (rc=1, all three files still on disk)
ASK 4: GREEN - properly wrapped into the framework so it is not lost - run: greps + the upgrade run saw: constitution Profile row + `| Anomaly telemetry contract tests | bash tests/telemetry.test.sh |` Commands row, docs/telemetry.md as the contract, CHANGELOG entries, CI job `telemetry-inbox`, `/vulyk-evolve` steps for both the hive and the repo side, driver codes recorded from vulyk-build.md and vulyk-resume.md; `--upgrade` of a hive with the hook unwired, the scripts deleted and the row removed re-copied anomaly-scan.sh + telemetry.sh, re-wired Stop and SessionEnd, appended `Telemetry = off` and left the filled `| Stack |` row intact; `.vulyk/` is added to the hive's .gitignore by the installer (`git check-ignore` confirms), so the seen-cache and bundles never enter a hive's history
ASK 5: GREEN - the public repo states what is collected, PR, weekly clean + update - run: `sed -n '243,262p' README.md` saw: section "Anomaly telemetry — opt-in, anonymized, never automatic" - logged locally to improve VULYK, the 10-key row and the 8 codes spelled out, "Never included: file paths, story/spec slugs, dispatch names, emails, free text", off by default, `/vulyk-evolve` prints the command you run to push the PR into telemetry/inbox/, weekly distil + staged deletion + "ship with the next release", linking docs/telemetry.md
ASK 6: GREEN - install/update asks in the terminal, explains, default off - run: fresh install and `--upgrade` without a tty; the `telemetry_decide` path driven directly with a stubbed tty saw: no tty -> no question at all and the row written `off` (fresh hive `telemetry.sh consent` = off); with a terminal the six-line explanation prints (what is logged, never paths/slugs/prompts/emails, "Nothing is ever sent", "Default: off") followed by `Enable telemetry? [y/N]`; empty or unrecognised answers resolve to off, an unreadable tty also resolves to off; `--telemetry on` flipped the row and `consent` returned on; `--telemetry ask` on an already-answered hive left the row untouched - asked once, not on every update
UNASKED: (1) `record` sanitizes only quotes, backslashes, CR/LF/TAB - any other C0 byte (e.g. 0x1F) in `--spec/--story/--ref` is written raw, so the local line is not valid JSON; with such a line first in memory/stats/anomalies.jsonl, `bundle` emitted 0 rows for a 7-row week and still exited 0 - a whole week of telemetry lost silently rather than reported. No shipped detector produces that input and nothing leaks (bundle drops those fields, `check` is fail-closed), but `sanitize()` should strip the full control range and/or `bundle` should count unparseable lines aloud. (2) the cross-machine recipe ends in `gh pr create --repo <upstream> --head <branch>` with a bare branch; `gh pr create --help` (gh 2.91.0) documents `--head <user>:<branch>` for selecting a head repo owned by another user, and docs/telemetry.md admits the recipe was never run against a real fork - if gh does not infer the fork from the clone's remotes, the last line fails after the push has already happened. (3) the Stop hook costs ~13.5 s cold and ~3.6 s warm on a synthetic 42-subagent session here (measured with a wrapper counting `handoff.sh measure` calls: 41 cold, 1 warm) - the round-5 caching works, but every turn end still pays a python start plus three jq passes.
BREACH: none
