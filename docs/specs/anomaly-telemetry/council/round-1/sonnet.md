<!-- seat: sonnet · model: claude-sonnet-5 · round: 1 · head: 4a6afd0 · pack: 9b30072f04a4 · attempt: 1 · recorded: 2026-09-15T06:37:49Z -->
COUNCIL: anomaly-telemetry · round 1 · seat sonnet
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-1
VERDICT: GREEN
ASSUMED CONFIG: shell + Python + markdown toolkit, no compiler/build; hooks under .claude/hooks, scripts under scripts/, VULYK repo itself is the "local checkout"
RAN: git ls-files '*.sh' | xargs -n1 bash -n; python -m py_compile .claude/hooks/*.py; git ls-files '*.json' | xargs jq -e .; bash .claude/hooks/handoff.sh status; bash tests/telemetry.test.sh (x4); bash scripts/telemetry.sh enum/agents/consent/record/bundle/check/publish (repo + scratch hive); bash install.sh (fresh, --telemetry ask piped, --telemetry on)
PATH: library/CLI only, no browser client - walked scripts/telemetry.sh CLI end to end (record -> bundle -> check -> publish) and install.sh's consent flow
ASK 1: GREEN - all terminals monitor anomalies and log - run: bash scripts/telemetry.sh enum; bash scripts/telemetry.sh record context_high 150000 110000 --tier 3 --model sonnet --ref sess:localtest saw: 8-code enum (context_high, agent_prefix_high, agent_empty, council_rounds_high, stage_long, driver_refused, driver_relaunched, scope_breach); local row appended to memory/stats/anomalies.jsonl with 12 keys; hook wiring confirmed via `anomaly-scan.sh is wired on Stop/SessionEnd` in tests/telemetry.test.sh
ASK 2: GREEN - weekly send: local repo commit cmd, remote as PR - run: bash scripts/telemetry.sh publish --dry-run (with Telemetry row set to on, restored after) saw: "would write .../telemetry/inbox/2026-W38/<hive>.jsonl" + printed `git add && git commit && git push` command when run inside this VULYK checkout; against a scratch dir with no local checkout saw the `gh pr create` recipe instead - neither path executed git/gh
ASK 3: GREEN - anonymized: only codes/numbers, no personal files - run: bash scripts/telemetry.sh bundle; bash scripts/telemetry.sh check <bundle> saw: bundle row has exactly the 10 contract keys (v, code, value, threshold, vulyk, tier, model, agent, week, hive); spec/story/ref (path-bearing) dropped; check exits 0 on the bundle and tests/telemetry.test.sh shows check rejecting any string with `/`, `\`, `@` or whitespace
ASK 4: GREEN - wrapped into the VULYK framework so it isn't lost - run: bash tests/telemetry.test.sh; CLAUDE.md ## Commands row saw: 125/125 checks pass (one earlier run showed 1 failure - traced to my own concurrent manual edit of CLAUDE.md's Telemetry row in COURT during testing, not a real defect; reverted and reran clean 4x); wired via Stop/SessionEnd hooks, /vulyk-evolve step, install.sh, docs/telemetry.md
ASK 5: GREEN - public repo documents the collection/PR/weekly-clean-update story - run: grep -n telemetry README.md CHANGELOG.md; cat telemetry/inbox/README.md saw: README "Anomaly telemetry — opt-in, anonymized, never automatic" section describing the installer question, PR push, and `/vulyk-evolve` weekly distill/clear; CHANGELOG entries for the same; telemetry/inbox/README.md repeats the contract; CI job telemetry-inbox in .github/workflows/ci.yml checks every inbox file
ASK 6: GREEN - installer/upgrade asks consent, explains, defaults off - run: bash install.sh /tmp/vulyk-install-test --telemetry ask < /dev/null; bash install.sh /tmp/vulyk-install-test2 --telemetry on < /dev/null saw: no-tty path appends `Telemetry | off` silently with no question printed (piped stdin never answers); explicit --telemetry on writes `Telemetry | on` and wires anomaly-scan.sh into settings.json; tests/telemetry.test.sh's terminal-question checks (print_telemetry_explanation, default off, byte-identical on repeat upgrade) all pass except the one skipped for lack of a `script` pty on this machine (N/A, not a defect)
UNASKED: none
BREACH: none
