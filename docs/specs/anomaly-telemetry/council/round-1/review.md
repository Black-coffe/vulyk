<!-- seat: review · model: unknown · round: 1 · head: 4a6afd0 · pack: 9b30072f04a4 · attempt: 1 · recorded: 2026-09-15T06:38:02Z · verdict: BLOCK -->
VERDICT: BLOCK

Reviewed: docs/specs/anomaly-telemetry (plan, brief, stories 01-05, journal), diff b15b4ba..4a6afd0 (32 files), ADR-001, ADR-005. Ran only what the diff made suspicious: `scripts/telemetry.sh scan` against the live transcript and with no transcript (timing), `handoff.py measure` on a real subagent file, the evolve step's awk against the committed log, a `record` dedupe probe, and a tally of real `.meta.json` agentType values. Two hypotheses were checked and retracted: the (code, ref) dedupe has no prefix collision (the grep needle carries the closing quote), and A12 holds - 196 of ~213 real meta files carry a framework agent name as `agentType`.

## Critical

1. `scripts/telemetry.sh` (`cmd_scan` -> `detect_agents "$(dirname "$transcript")"`) - **worker** - subagent transcripts live at `<project>/<session-id>/subagents/` (recon/hooks-and-stats.md section 6, in story 02's map slice; confirmed: 233 files under four session dirs on this machine, none under `<project>/subagents/`), so two of the five detectors (`agent_prefix_high`, `agent_empty`) never fire in real use - 0 agent rows among the 91 committed, despite 122 `cycle-clerk` transcripts and the ~55k-token clerk prefix being the anomaly the brief opens with. `tests/telemetry.test.sh` case 10 puts `subagents/` beside `main.jsonl`, so the suite is green with the bug (test theater). Condition: `scan` must resolve subagents at `<dirname>/<transcript basename without .jsonl>/subagents/` (recursively, workflow subdirs included), and the fixture must mirror the real layout so case 10 fails when the path is wrong.

## Major

2. `.claude/commands/vulyk-evolve.md:48` - **worker** - `code=substr($0, RSTART+7, RLENGTH-8)` is off by one for the 8-character prefix `"code":"`; run against `memory/stats/anomalies.jsonl` it yields `"scope_breach` with a leading quote, so the enum-driven zero-fill prints `0` for every code. Condition: the 7-day count table must key on the bare code so the real log produces `anomalies (7d): scope_breach 90`.

3. `docs/telemetry.md:210-217` - **worker** - the env vars named in the public table (`VULYK_TELEMETRY_CONTEXT_HIGH`, `VULYK_TELEMETRY_AGENT_PREFIX_HIGH`, `VULYK_TELEMETRY_COUNCIL_ROUNDS_HIGH`, `VULYK_TELEMETRY_STAGE_LONG`) do not exist; the script reads `VULYK_ANOMALY_CONTEXT_PCT`, `VULYK_ANOMALY_CONTEXT_TOKENS`, `VULYK_ANOMALY_AGENT_PREFIX_TOKENS`, `VULYK_ANOMALY_COUNCIL_ROUNDS`, `VULYK_ANOMALY_STAGE_HOURS` (plan A2). Invented fact on a public page. Condition: every env var the docs name must be one the script reads.

4. `docs/telemetry.md:313-316`, `telemetry/inbox/README.md:6`, `README.md:169` - **plan** - the docs state that `/vulyk-evolve`, run in the VULYK repo, "reads the week's rows across telemetry/inbox/ ... then clears the directory"; no story implements that step (story 03's evolve step reads only `memory/stats/anomalies.jsonl`), and nothing sits in `## Descoped` or `## Plan deltas`. Silent narrowing of ask 5 into a documentation claim. Condition: either a story gives `/vulyk-evolve` the inbox read-and-clear step, or the docs say a maintainer does it by hand, and the plan records which.

5. `.claude/hooks/anomaly-scan.sh` + `scripts/telemetry.sh` `cmd_scan` - **plan** - a scan with no transcript takes 31 s on this Windows machine (per-row `jq`/`date`/`grep` process spawns over 97 scope rows and 54 journal lines), and it runs on every `Stop` and `SessionEnd` in the Queen's session and, via `wire_hook`, in every upgraded hive; once finding 1 is fixed it adds one python start per subagent file per Stop (233 here). The plan specifies a full rescan per Stop with no incremental marker, so the cost is unbounded in log size. Severity assumes a Windows hive, which is the maintainer's own configuration today. Condition: a Stop-hook scan must be bounded - one jq pass per stats file, subagents of the current session only, or a last-scanned marker - so a turn is not held for tens of seconds.

6. `scripts/telemetry.sh` `detect_agents` (`agent_empty`) - **plan** - the contract defines `agent_empty` on the last assistant entry with no completion test; the Queen's `Stop` fires while the background Workflow's workers are mid-turn (the normal state in this repo), so a running agent whose last entry is a `tool_use` is recorded as `agent_empty` and the (code, ref) dedupe makes the false row permanent. Latent until finding 1 is fixed. Condition: `agent_empty` must be recorded only for a transcript that has finished (SessionEnd-only, an mtime/age guard, or a completion marker), or its ref must allow a later correction.

7. `scripts/cycle.sh:1574-1581`, `scripts/ship-check.sh:111`, commit `1a91924` - **plan** - the hook writes a committed file that no cycle verb commits: `close-story` adds only story files and `scope.jsonl`, `open-round` ignores it through `is_paperwork_path`, but `ship-check` fails stage 03 on any `git status --porcelain` output. Every new row after the last commit makes the ship gate NOT READY until someone hand-commits - which `1a91924` (author Andrei, not `cycle.sh --commit`) already is, against ADR-001's invariant "paperwork commits are made only by `cycle.sh --commit`". Condition: a cycle verb must own the hook-written log (or ship-check's clean-tree test must pass it through `is_paperwork_path`) so a Stop hook can never leave the tree in a state the gates refuse.

## Minor

8. Commit `1a91924` (`memory/learnings/2026-09-14_222936.md`, `memory/stats/skills.json`) - **plan** - two files no story names, one of them an empty learnings stub, committed on the spec branch. Condition: files outside every story's list travel in a separate housekeeping commit or not at all; the empty stub is deleted, not committed.

9. `scripts/telemetry.sh` `detect_scope` - **plan** - 90 of 91 committed rows are `scope_breach`: `scope.jsonl` holds one row per scope-check run and, in a shared-tree wave, `out_of_scope` counts sibling stories' uncommitted files (story 05's own Findings say so), so the detector as specified is noise. Condition: `scope_breach` dedupes per story rather than per timestamp, and the plan records that parallel waves inflate the value until scope-check can tell sibling work apart.

10. `install.sh` `ensure_telemetry_row` - **worker** - when the Profile block has no markers the function returns silently after the terminal question was asked and answered, dropping the owner's answer with no message; ADR-005 D4.9 says warn. Condition: when the row cannot be written, the installer prints a warning naming the row and the value it would have written.

11. `docs/adr/005-installer-upgrade-contract.md` - **plan** - the plan's A6 carves one exception to "never touches a filled Profile block" and `ci.yml` renames the case "(except the Telemetry row)", but the ADR is not amended. Condition: ADR-005 records the Telemetry-row exception.

12. `docs/specs/anomaly-telemetry/anomaly-telemetry-05-consent-profile-install.md` `## Implementation notes` - **worker** - "it runs in the telemetry-inbox CI job" about the pseudo-terminal case is a coverage claim nobody has observed green (skipped locally; no CI run on this branch), and it is the case ask 6 leans on. Condition: the claim is backed by a CI log or restated as unverified.

13. Stories 02 and 04 `## Implementation notes` - **plan** - both record their production code as "already present, uncommitted, in the working tree" with no author; 188 lines of `telemetry.sh`, the hook and the `settings.json` wiring first reach git in 02's commit. Condition: the journal or story records who produced that code (a first attempt that died vs. the Queen's hand, Law 5).

14. `scripts/telemetry.sh` `cmd_check` (`sed "s#^#$f:#"`) and `cmd_publish` (`cd %s && git add ...` unquoted) - **worker** - a path with `#`, `&` or a space breaks the check prefix or the printed recipe. Condition: both must survive such paths.

15. `scripts/telemetry.sh` `spec_tier` - **worker** - a `spec` field from `council.jsonl` is used as a path segment unvalidated. Condition: the slug must match `^[A-Za-z0-9._-]+$` before it builds a path.

16. `.claude/hooks/handoff.py` `HOOK_MODES` - **worker** - `measure` is listed as a hook mode, so `main()` reads stdin and runs `detect_window` before dispatching; the script wrapper passes `< /dev/null`, but a direct `python handoff.py measure <f>` from a terminal blocks. Condition: `measure` must not consume stdin.
