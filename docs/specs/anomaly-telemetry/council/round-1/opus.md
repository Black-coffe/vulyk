<!-- seat: opus · model: claude-opus-5[1m] · round: 1 · head: 4a6afd0 · pack: 9b30072f04a4 · attempt: 1 · recorded: 2026-09-15T06:37:56Z -->
COUNCIL: anomaly-telemetry · round 1 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-1
VERDICT: RED
ASSUMED CONFIG: Profile is still the blank installer template (Stack/Test framework/Client path all `<fill in>`); only the new `| Telemetry | off ... |` row is filled. Judged as a shell+python+markdown toolkit driven from a POSIX shell on Windows.
RAN: telemetry.sh enum|agents|consent|record|scan|bundle|check|publish (against a sandbox hive via VULYK_HIVE, never writing in COURT); install.sh fresh install, --upgrade, --upgrade --telemetry on, --upgrade --check; anomaly-scan.sh with a hook payload; handoff.sh measure on a synthetic transcript + subagent; git status/gh pr create on the printed recipe; targeted greps of README/CHANGELOG/ci.yml/vulyk-evolve.md/hooks-reference.md
PATH: none named (Client path is `<fill in>`); walked the CLI surfaces instead - install.sh into a sandbox target and scripts/telemetry.sh's eight verbs end to end
ASK 1: GREEN - every VULYK terminal monitors anomalies and keeps a log - run: `install.sh <tgt>` then stripping anomaly-scan from settings and `install.sh <tgt> --upgrade`; `telemetry.sh scan --transcript <synthetic>`; hook with a payload saw: hook wired on Stop+SessionEnd on fresh install and re-wired on upgrade (grep -c anomaly-scan 0 -> 2); scan wrote context_high 200010/140000, agent_prefix_high 65000/50000 (agent cycle-clerk - the brief's own 55k clerk anomaly), agent_empty, stage_long 96/24, scope_breach; three consecutive scans left 4 rows (ref dedupe holds); hook exits 0 silently on a missing transcript
ASK 2: RED - weekly send, locally into the main project, from other machines as a pull request - run: `telemetry.sh publish` with consent on, no local checkout, then the printed recipe in a git worktree saw: local branch works (copied to telemetry/inbox/2026-W38/<hive>.jsonl + `git add && git commit && git push`), but the cross-machine branch prints only `cp <bundle> /path/to/vulyk/telemetry/inbox/...` then `cd /path/to/vulyk && gh pr create ...` - no branch, no `git add`, no commit, no push, so after the cp `git status --porcelain` is `?? telemetry/` and `gh pr create` has nothing to open a PR from. docs/telemetry.md:105-108 repeats the same incomplete instruction. The owner's named path for other machines ("как pull request") does not produce a pull request as printed.
ASK 3: GREEN - maximally anonymized logs, no personal files - run: recorded a row with --spec secret-client-name --story "C:/Users/Andrei/secret", then `bundle` + `check`; `check` on a poisoned file saw: bundle rows carry exactly the 10 keys, codes and numbers only - ts, spec, story, ref all dropped; hive is 12 hex; `check` rejected an `agent:"/home/andrei"` row ("a string value carries a path...") and an 11-key row; the committed memory/stats/anomalies.jsonl (91 rows) greps 0 hits for Andrei|C:/|/Users/|@|backslash and its full bundle passes check rc=0
ASK 4: GREEN - properly wrapped into the framework so it does not get lost - run: greps over CLAUDE.md, install.sh, ci.yml, CHANGELOG.md, README.md, bootstrap/interview.md saw: Profile row + `tests/telemetry.test.sh` Commands row in the constitution, installer copies+wires on install and upgrade, telemetry/ deliberately excluded from hives, CI job `telemetry-inbox` plus five Profile-row assertions, CHANGELOG Unreleased entry, bootstrap/interview.md reports (not re-asks) the consent value, docs/telemetry.md linked from README, evolve and the inbox README
ASK 5: GREEN - the public repo states logs are collected, pushable as a PR, cleaned weekly with an update - url: README.md:243-258 saw: "Anomaly telemetry - opt-in, anonymized, never automatic": what a row holds, the full enum, "Never included: file paths, story/spec slugs, dispatch names, emails, free text", "you push the PR into telemetry/inbox/ yourself", "distilled and cleared weekly by /vulyk-evolve ... feeding releases"; telemetry/inbox/README.md and docs/telemetry.md say the same
ASK 6: GREEN - install or upgrade asks in the terminal, explains, says off by default - run: `install.sh <tgt>` (fresh), `--upgrade` with the row deleted, `--upgrade --telemetry on`, `--upgrade --check --telemetry off`, plus a harness over telemetry_decide saw: fresh install writes `| Telemetry | off ...`; an upgraded hive with no row gets `profile row CLAUDE.md: Telemetry = off` and nothing else in the constitution changes; `--telemetry on` flips it and `telemetry.sh consent` then prints on; `--check` only prints "would set"; with no terminal (my environment has no openable /dev/tty) the question is skipped and off is written, which is the safe default. The prompt text itself ("Telemetry (optional, off by default)" ... "Enable telemetry? [y/N]" ... "Nothing is ever sent") is present and only the y|Y|yes path sets on - I could not press the key, the tty branch is unexercised here.
UNASKED: (1) Week boundary loses data: `publish`/`bundle` default to the *current* ISO week while /vulyk-evolve counts a rolling 7 days, so a weekly run on a Monday bundles only that Monday - run: backdated a row to 2026-09-10, `bundle` (empty of it) vs `bundle --week 2026-W37` (has it) saw: the previous week's rows never travel unless a human passes --week by hand, silently. (2) anomaly-scan.sh is the only shipped hook missing from docs/hooks-reference.md's table - the canonical place a maintainer looks for it. (3) The 90 scope_breach rows already in memory/stats/anomalies.jsonl were all stamped with today's ts from historical scope.jsonl, so a backfill lands in one week's bundle and will read as a spike; (4) context_high dedupes per session ref, so a session that keeps growing after its first record is never re-measured - deliberate and cheap, but worth the owner knowing.
BREACH: none
