<!-- seat: opus · model: claude-opus-5[1m] · round: 2 · head: 988605a · pack: b0e5e31fff8f · attempt: 1 · recorded: 2026-09-15T07:12:32Z -->
COUNCIL: anomaly-telemetry · round 2 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-2
VERDICT: RED
ASSUMED CONFIG: single local hive, no DB, no deploy; the Profile block is still `<fill in>` in every row except `| Telemetry | off - ... |`; Client path unfilled, Browser MCP untouched (haiku's row)
RAN: telemetry.sh enum|agents|consent|record|bundle|check|scan|publish (--dry-run and real) in a scratch hive copy; handoff.sh measure on synthetic main+subagent transcripts; .claude/hooks/anomaly-scan.sh with a Stop payload and with garbage stdin; install.sh fresh / --upgrade / VULYK_TELEMETRY=on / patched-tty y|n|Enter; git clone twice into one dir; gh repo fork --help; gh pr create --help; targeted greps
PATH: none named - Profile `Client path` is still `<fill in>`; the reachable surface walked instead was the CLI one (scripts/telemetry.sh verbs, the Stop/SessionEnd hook, install.sh consent), end to end
ASK 1: GREEN - every VULYK terminal monitors anomalies and keeps a log - run: `echo '{"transcript_path":...}' | CLAUDE_PROJECT_DIR=$S bash .claude/hooks/anomaly-scan.sh` then `wc -l memory/stats/anomalies.jsonl` saw: rc=0 and 6 rows - context_high 200010/140000, agent_prefix_high 90000/50000, agent_empty, council_rounds_high 4/3, stage_long 32/24, scope_breach 2; a second scan added 0 rows (ref dedupe); garbage stdin -> rc=0 silent; hook is wired on Stop and SessionEnd in .claude/settings.json and install.sh wire_hook puts both rows into a freshly installed hive (verified in the installed target's settings.json); driver_refused/driver_relaunched are recorded by vulyk-build.md:22,48 and vulyk-resume.md:17; anomalies.jsonl is not gitignored in an installed hive (`git check-ignore` rc=1), so it commits as answer 1 promised
ASK 2: GREEN - weekly send, local into the repo, cross-machine as a PR - run: `telemetry.sh publish --dry-run` with and without telemetry/inbox + an origin naming the slug saw: local route writes telemetry/inbox/2026-W38/<hive>.jsonl and prints `cd <repo> && git add ... && git commit ... && git push`; the other route prints fork -> cd -> `git switch -c telemetry/<week>-<hive>` -> mkdir -> cp -> git add -> git commit -> `git push -u origin` -> `gh pr create --repo Black-coffe/vulyk --head ...`, i.e. round 1's untracked-copy hole is closed and the paste ends in an open PR; nothing is sent (no git/gh call exists in the script); a bare run covers the previous ISO week and the current one, so a Monday run does not drop the week it reports on
ASK 3: GREEN - maximally anonymized logs, no personal files - run: recorded rows carrying `--spec secret-client-project --story secret-client-project-03 --ref session:/Users/Andrei/private/path.jsonl --agent queen-planner-secret-dispatch-name`, then `telemetry.sh bundle` + `check` saw: bundle rows hold only `v code value threshold vulyk tier model agent week hive` - ts, spec, story and ref never emitted, the dispatch name collapsed to the fixed agent set, hive = 12 hex of sha256(toplevel); `check` passed and is run by publish before any copy and by the CI `telemetry-inbox` job over every inbox file
ASK 4: GREEN - wrapped into the framework so it cannot get lost - run: greps + the installer run saw: hook in settings.json (Stop+SessionEnd) and wired by install.sh into hives; `| Anomaly telemetry contract tests | bash tests/telemetry.test.sh |` in CLAUDE.md ## Commands; `| Telemetry |` row inside the VULYK:PROFILE markers; docs/telemetry.md; telemetry/inbox/README.md; CI job `telemetry-inbox` plus five Telemetry-row assertions in the installer job; bootstrap/interview.md reports the value instead of re-asking; vulyk-evolve.md step 2 prints the 7d per-code counts
ASK 5: RED - the public repo says logs are collected, can be pushed as a PR, and are cleared weekly with an update - run: `grep -c -i inbox .claude/commands/vulyk-evolve.md` -> `0`; `grep -rn inbox --include='*.sh' --include='*.md'` outside docs/README/tests saw: no code or command anywhere distils or clears telemetry/inbox. The collection and PR halves are written and true (README.md:243-258). The weekly half is written as a statement of fact about a named command - README.md:257 "The inbox is distilled and cleared weekly by `/vulyk-evolve` in the VULYK repo" and docs/telemetry.md:141 "`/vulyk-evolve` ... reads the week's rows across `telemetry/inbox/` ... then clears the directory" - and `/vulyk-evolve` has no inbox step at all: its new telemetry step reads only `memory/stats/anomalies.jsonl`. The public promise a contributor's PR is accepted on is unbacked by the shipped command.
ASK 6: GREEN - install/update asks, explains, default off - run: patched-tty install.sh with `y`, `n`, bare Enter; real `install.sh` fresh, `--upgrade` after deleting the row, `VULYK_TELEMETRY=on` saw: the prompt prints the six-line explanation (what is logged, "Never paths, slugs, story names, prompts, emails or any free text", "Nothing is ever sent", "Default: off") then `Enable telemetry? [y/N] `; y -> `| Telemetry | on`, n and bare Enter -> `off`; with no terminal nothing is asked and the row is written `off`; an --upgrade over an already-answered hive asked 0 times and kept `on`; an old hive with no row gets `off` appended silently; `--telemetry ask` is the documented re-ask
UNASKED: two hardening gaps. (1) `vulyk` is the one bundle string not bound to a fixed set: with `.claude/vulyk-version` = `1.2.3-acme-payroll-client`, `bundle` emitted `"vulyk":"1.2.3-acme-payroll-client"` and `check` PASSED - the semver test is a prefix test with no `$`, and a suffix without `/`, `@` or whitespace rides through the "no free text at all" promise; anchor it. (2) the cross-machine recipe is weekly but only works once: line 1 is `gh repo fork ... --clone -- vulyk-telemetry`, and a second week's paste dies on `fatal: destination path 'vulyk-telemetry' already exists and is not an empty directory` (verified with a local double `git clone`) - the recipe should branch on an existing clone.
BREACH: none
