<!-- seat: review · model: unknown · round: 2 · head: 988605a · pack: b0e5e31fff8f · attempt: 1 · recorded: 2026-09-15T07:12:36Z · verdict: BLOCK -->
VERDICT: BLOCK

Reviewed: docs/specs/anomaly-telemetry (plan incl. A14/A15 and Plan deltas, brief, story 06, journal, round-1 seat files), diff 4a6afd0..988605a (story 06: scripts/telemetry.sh, tests/telemetry.test.sh, docs/telemetry.md, story file, scope.jsonl) on top of b15b4ba..988605a, ADR-001. Ran only what the diff made suspicious: `bash -n` on the two touched scripts; `publish --dry-run` and `bundle --out` from a scratch hive whose path holds an apostrophe, `&` and `#` (recipe printed correctly, `shq` escapes the apostrophe with the standard quote-backslash-quote-quote sequence, one week, exit 0); `gh repo fork --help` (`--clone` is boolean, `--` forwards git-clone args - the worker's claim holds); `scripts/ship-check.sh` (stage 03 OPEN on an uncommitted `memory/stats/anomalies.jsonl` row). Story 06's verification claims were spot-checked case by case: (a) `expect_order` on nine needles fails if any line is missing or reordered, (b) the needles carry the quotes so unquoting breaks them and `check`'s prefix is asserted with the `#` path, (c) the backdated row asserts both weeks and `--week` one, (d) the shim list now covers `clone`/`fork` - the claims hold. Story 06 itself is sound and closes ask 2; the BLOCK rests on the round-1 critical that is still in the tree with nothing in `## Descoped`.

## Critical

1. `scripts/telemetry.sh:373` (`detect_agents "$(dirname "$transcript")"`) and `:262` - **plan** - unchanged since round 1: subagent transcripts live at `<project>/<session-id>/subagents/`, not beside the main transcript, so `agent_prefix_high` and `agent_empty` never fire in a real hive (0 agent rows in the committed log; the 55k-token clerk that opens the brief is undetected) and `tests/telemetry.test.sh:379` still builds the wrong layout so the suite is green with the bug. The plan's `## Plan deltas` says "the Queen decides routing" but no story was cut and `## Descoped` is empty, so ask 1 ships narrower than confirmed with nothing on the record. Condition: `scan` must resolve subagents at `<dirname>/<transcript basename without .jsonl>/subagents/` (recursively) with a fixture that mirrors that layout, or the plan must record the two detectors as descoped for this ship.

## Major

2. `.claude/commands/vulyk-evolve.md:48` - **plan** - unchanged: `substr($0, RSTART+7, RLENGTH-8)` keeps the leading quote of the code, so the 7-day table prints `0` for every enum code against the real log. Condition: the evolve step keys its count on the bare code so the committed log yields `scope_breach <n>`.

3. `docs/telemetry.md:17-21` - **plan** - unchanged: the public table names `VULYK_TELEMETRY_CONTEXT_HIGH`, `VULYK_TELEMETRY_AGENT_PREFIX_HIGH`, `VULYK_TELEMETRY_COUNCIL_ROUNDS_HIGH`, `VULYK_TELEMETRY_STAGE_LONG`; the script reads `VULYK_ANOMALY_CONTEXT_PCT/_TOKENS`, `VULYK_ANOMALY_AGENT_PREFIX_TOKENS`, `VULYK_ANOMALY_COUNCIL_ROUNDS`, `VULYK_ANOMALY_STAGE_HOURS`. Story 06 owned this file but its acceptance said "no other section changes" and its Non-goals list skipped finding 3 without assigning it, so the worker could not act. Condition: every env var the public docs name is one the script reads.

4. `docs/telemetry.md:143`, `README.md:257`, `telemetry/inbox/README.md:6` - **plan** - unchanged: the public docs promise `/vulyk-evolve` reads `telemetry/inbox/` and clears it weekly; no story implements it and `## Descoped` is empty. Condition: either a story gives `/vulyk-evolve` the inbox read-and-clear step, or the docs say a maintainer does it by hand and the plan records which.

5. `.claude/hooks/anomaly-scan.sh` + `scripts/telemetry.sh` `cmd_scan` - **plan** - unchanged: a full rescan of every stats file on every `Stop`/`SessionEnd` (31 s measured in round 1 on this Windows hive; grows with the log and, once finding 1 is fixed, with the subagent count). Severity assumes a Windows hive, which is the maintainer's own configuration. Condition: a Stop-hook scan is bounded (one jq pass per file, current session's subagents only, or a last-scanned marker).

6. `scripts/telemetry.sh` `detect_agents` (`agent_empty`, :283) - **plan** - unchanged and latent behind finding 1: a subagent mid-turn at `Stop` time records a permanent false `agent_empty` row via the (code, ref) dedupe. Condition: `agent_empty` is recorded only for a finished transcript, or its ref permits later correction.

7. `memory/stats/anomalies.jsonl` (uncommitted row ts `2026-09-15T07:00:13Z`, written before `open-round` at 07:01:33Z), `scripts/ship-check.sh` stage 03 - **plan** - unchanged and now evidenced on this round: the hook writes a tracked file that `open-round` waves through via `is_paperwork_path` but `ship-check` reports `working tree is not clean`; nothing in `cycle.sh` commits it, so the ship gate stays NOT READY until a hand commit that ADR-001 forbids. Condition: a cycle verb owns the hook-written log, or ship-check's clean-tree test passes it through `is_paperwork_path`.

## Minor

8. `scripts/telemetry.sh:592` (`recipe_pr` called once per week) and story 06 `## Implementation notes` ("the second one's fork/clone is a no-op") - **worker** - with rows in both default weeks and no local checkout, the second block's `gh repo fork ... --clone -- 'vulyk-telemetry'` fails against the directory the first block created, and its `git switch -c` branches off week 1's branch, so PR 2 carries week 1's commit; the note overstates the outcome. Condition: the two-week PR path either shares one clone/branch step or the second block starts from `cd <dir> && git switch <default>` so each PR holds exactly one week.

9. `scripts/telemetry.sh:427-430` (`cmd_bundle --out` with no `--week`) - **plan** - two weeks are appended into one `--out` file while the contract says "each week emitted separately"; a hand-copied `--out` file into one `telemetry/inbox/<week>/` path mixes weeks. Condition: the contract states whether `bundle --out` may hold rows of two weeks, and the docs say so.

10. `scripts/telemetry.sh:534` (`gh repo fork <slug> --clone -- <dir>`) and `:542` (`gh pr create --repo <slug> --head <branch>` with no `owner:` prefix) - **worker** - `--help` confirms `--clone` is boolean and `--` forwards git-clone arguments, but a positional clone directory is not a "flag", and `--head` without an owner relies on gh picking the fork from the pushed tracking branch; neither was run, yet `docs/telemetry.md:108` asserts the nine lines "end in an open PR when pasted as they are". Condition: the recipe is pasted once against a real fork (or the docs mark it as untested) before the sentence stands.

11. Story 06 `memory/stats/scope.jsonl` row `out_of_scope:1` - **plan** - the one out-of-scope path is the hook-written `anomalies.jsonl` in the shared tree, the same sibling noise round 1 finding 9 named; the `scope_breach` detector then records it as an anomaly of story 06. Condition: `scope_breach` dedupes per story and the plan records that hook-written paperwork inflates it.

12. Round-1 minors carried unchanged, none routed in `## Plan deltas` beyond "the Queen decides": commit `1a91924` unnamed files (**plan**); `install.sh` `ensure_telemetry_row` silent drop of the answered value without markers (**worker**); ADR-005 not amended for the Telemetry-row exception (**plan**); story 05's "runs in the telemetry-inbox CI job" coverage claim with no CI run on the branch (**worker**); stories 02/04 "already present, uncommitted" with no author (**plan**); `spec_tier` uses an unvalidated slug as a path segment, `scripts/telemetry.sh:290` (**worker**); `handoff.py measure` listed in `HOOK_MODES` so a direct terminal call blocks on stdin (**worker**). Conditions as stated in round-1 review items 8, 10, 11, 12, 13, 15, 16.
