<!-- seat: review · model: unknown · round: 3 · head: 34acc61 · pack: 7260d521a997 · attempt: 1 · recorded: 2026-09-15T08:41:07Z · verdict: BLOCK -->
VERDICT: BLOCK

Reviewed: docs/specs/anomaly-telemetry (plan incl. A16/A17, Contracts, `## Descoped`, `## Plan deltas`; brief; stories 07-10; journal; round-2 review), diff b15b4ba..34acc61 with round 3's own commits 0d08859 (07), 8d2ab1e (09), 49ea682 (08), 5300d6a (10), 4a88a0e / 34acc61 (paperwork), ADR-001, ADR-005. Scope: every file in each story commit is named in that story's `## Files` (plus `memory/stats/scope.jsonl`, which `close-story` writes); no Law 3 violation. `docs/wiki/` does not exist. Ran only what the diff made suspicious, on a `git archive 34acc61` scratch copy so the real log stayed untouched: `scan --transcript <real 73-subagent session>` three times with a python-counting shim (25.7 s, 26.5 s; 74 python starts each run, every run), `scan` with no transcript (1.2 s), `handoff.sh measure --sidechain` over the 73 real subagent files (max `first_prefix` 36 631, zero above the 50 000 default, zero `last_has_text=false`). Story 08's suite cases were read against the implementation: case 10 uses the real `<sid>/subagents/**` layout with a control file at the old path, counts python starts, and asserts `--final` gating and dedupe - those pass for the reason claimed. Story 09's cycle cases and story 07's inbox cases assert observable state (porcelain, HEAD, `git show --stat`, exact TSV) - not theater.

## Critical

1. `scripts/telemetry.sh:277-320` (`detect_agents`), story 08 `## Implementation notes` "new code 19.6 s on the first run, 1.1 s in steady state" - **plan** - the bound only skips a subagent whose `agent_prefix_high` ref is already in the log, and a subagent under threshold is never recorded, so it is re-measured (one python start) on every `Stop`; measured on this hive with a real 73-subagent session: 74 python starts and ~26 s per scan, on every run, versus the 1.2 s the story timed on the no-transcript path that skips `detect_agents` entirely. Severity assumes the maintainer's own configuration (Windows hive, Tier 3 builds with 50-90 subagents per session, `Stop` hook wired on every hive by `wire_hook` regardless of consent) - that is the configuration round 1 measured 31 s in, so round-2 Major 5 is not resolved and the story's timing claim retired it on the wrong path. Condition: a `Stop`-hook scan with a transcript must not start python for a subagent file it has already measured and found under threshold (the acceptance criterion's "already present under both agent codes" cannot express that; A17's rejection of any per-machine marker needs revisiting or an in-log "measured, no anomaly" tombstone), and the recorded timing must be of `scan --transcript <real session>`.

## Major

2. `docs/telemetry.md:113-124` (the fenced recipe) and `:113` "nine lines" - **plan** - story 08 changed the recipe to fork, `cd`, `base="$(git rev-parse --abbrev-ref HEAD)"`, then per week `git switch "$base"` + the seven lines (11 lines for one week), but the public block still shows the pre-08 nine lines with no `base` line and no `git switch "$base"`; story 10 owned the file and had story 08's notes in its map slice, but its acceptance named only (a)-(d) and none of them is the block. Condition: the recipe block and its line count in `docs/telemetry.md` match what `publish` prints for one week and say how a second week's block differs.

3. `scripts/ship-check.sh:111-137`, `scripts/scope-check.sh:76-79`, `scripts/lib.sh:42-48` - **plan** - `memory/stats/skills.json` is written by the `PostToolUse` hook in every hive, story 09 now hides it from every story's `out_of_scope`, but it is not in `is_paperwork_path`, so `ship-check` stage 03 still fails `working tree is not clean` on it and no `cycle.sh` verb stages it - the exact shape of round-2 Major 7, for the other hook-written file (commit 4a88a0e is this round's hand commit of it). Story 09's non-goals forbade widening the whitelist, so the worker could not. Condition: either `memory/stats/skills.json` reaches git through a `cycle.sh --commit` verb and passes stage 03 the way `anomalies.jsonl` now does, or `## Descoped` records that a used skill leaves the ship gate NOT READY until a hand commit.

## Minor

4. `docs/adr/005-installer-upgrade-contract.md:231-232` "alongside the Telemetry row's own row-level carve already named at D4.9's table intro" - **worker** - the ADR contains no mention of Telemetry before the amendment (grep: first hit is the amendment heading at :228), so the sentence cites a carve that does not exist. Condition: the amendment describes only what the ADR text actually contains, or the D4.9 intro is amended to carry the carve it is said to name.

5. `.claude/hooks/anomaly-scan.sh:6` (ROOT from `CLAUDE_PROJECT_DIR`) vs `scripts/telemetry.sh:29` (ROOT from `VULYK_HIVE`, else the cwd's git toplevel) - **worker** - the hook resolves the hive one way and the script it execs another; story 08's worker hit the mismatch (the suite had been appending to the real repo's log) and fixed it in the suite by exporting `VULYK_HIVE`, not in the hook. Harmless when the hive is its own git toplevel; a hive nested in an outer repository logs, reads consent and hashes its `hive` id from the outer repo. Condition: the hook hands the script the root it resolved (export `VULYK_HIVE="$ROOT"`), so the two agree for every layout.

6. `scripts/telemetry.sh:436-445` (`scope:<story>` ref) and plan `## Descoped` fifth bullet - **plan** - the first scan on any hive with an existing log records one new `scope:<story>` row beside the old `scope:<ts>:<story>` rows (measured: 93 -> 153 rows on this hive's log, 59 of them the migration), so every story with a historical breach counts twice in `/vulyk-evolve`'s next table; the Descoped line discounts pre-story-09 rows but not this duplication. Condition: `## Descoped` (or the evolve step) records that old-ref and new-ref rows for one story are the same anomaly.

7. `scripts/telemetry.sh:198` (`VULYK_ANOMALY_AGENT_PREFIX_TOKENS=50000`), plan A2 - **plan** - over 73 real subagents of a Tier-3 session on this hive the maximum first-turn prefix is 36 631, so at the default the "subagent dearer than N tokens" detector records nothing this project produces; A2 calls the number calibration, but the ship leaves the brief's opening anomaly at zero rows on real data. Condition: the plan states the default was checked against a real session and either keeps it with that number on the record or lowers it.

8. `scripts/ship-check.sh:120-127` - **plan** - the pass-through admits every `memory/stats/*` path on `is_paperwork_path`, which includes `council.jsonl`, `human.jsonl`, `acceptance.jsonl`, `ship.jsonl`, `scope.jsonl` - cycle-written, not hook-written - so a `record-seat` never followed by `judge --commit` now passes stage 03 as "hook-written stats pending" where it blocked before. Condition: the stage-03 pass-through names only the files a hook writes, or its message stops calling cycle-written files hook-written.

9. `install.sh:367-370` - **plan** - the marker-less warning fires with `TEL_WANT=off` on every non-interactive `--upgrade` of a marker-less hive where nobody answered anything (A10 (5) sets the default), so a CI-driven upgrade prints "not writing | Telemetry | off |" each run; the ADR amendment says the exception applies "when the installer's consent question was asked or answered". Condition: the warning is emitted only when an answer or an explicit `--telemetry`/`VULYK_TELEMETRY` value would have been written, or the docs say it also names the default.

10. `tests/cycle.test.sh:175-245`, story 09 acceptance "a verb without `--commit` stages nothing new" - **worker** - no case asserts the negative (a `--commit`-less verb leaving the dirty log unstaged); the claim rests on reading `commit_paperwork`. Condition: one case runs a verb without `--commit` with a dirty log and asserts the log's porcelain line is unchanged.

11. `scripts/telemetry.sh:783` (`for w in $weeks`) - **worker** - week directory names are word-split before `git rm -r -- telemetry/inbox/$w`; a merged directory whose name carries whitespace is not cleared and `die`s mid-loop after earlier weeks were staged. Condition: `--clear` iterates the week list line by line.
