<!-- seat: opus · model: claude-opus-5[1m] · round: 3 · head: 34acc61 · pack: 7260d521a997 · attempt: 1 · recorded: 2026-09-15T08:41:02Z -->
COUNCIL: anomaly-telemetry · round 3 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-3
VERDICT: GREEN
ASSUMED CONFIG: Profile is still installer placeholders (`<fill in>`) for Stack/runner/tests/Client path; the only filled row is `| Telemetry | off - ... |`. Judged as: shell+Python+markdown toolkit, no runner, no client path, single local hive + a public GitHub repo.
RAN: telemetry.sh enum|agents|consent|record|scan|bundle|check|publish|inbox (read-only verbs in COURT; write verbs against a scratch hive copied out of COURT); install.sh fresh + --upgrade + --check + --telemetry on/off into scratch dests; handoff.sh measure on synthetic main/subagent transcripts; ship-check.sh; a sourced telemetry_decide harness with a stubbed tty
PATH: none named - Profile's Client path is `<fill in>`; walked the CLI surface instead: install.sh consent prompt -> Profile row -> scan hook -> anomalies.jsonl -> bundle -> check -> publish recipe -> inbox distil/clear
ASK 1: GREEN - every VULYK terminal monitors anomalies and keeps a log - run: `bash scripts/telemetry.sh scan --transcript <synthetic> --final` in a scratch hive; `jq 'del(.hooks.Stop[0].hooks[1])' settings.json && bash install.sh <hive> --upgrade` saw: anomaly-scan.sh is wired on both Stop and SessionEnd, shipped by a fresh install and re-wired on upgrade (anomaly-scan refs 0 -> 2); a scan recorded context_high 150010/140000, agent_prefix_high 65000/50000, agent_empty, council_rounds_high 3/3 and 59 scope_breach rows into memory/stats/anomalies.jsonl; a second identical scan added 0 rows (ref dedupe) and took 1.5 s on a 68-row log - bounded, unlike round 2's 31 s
ASK 2: GREEN - weekly send, local into the main project, PR from other machines - run: `bash scripts/telemetry.sh publish --week 2026-W38` with and without `VULYK_LOCAL` saw: with a local checkout it wrote `<repo>/telemetry/inbox/2026-W38/<hive>.jsonl` and printed a `git add/commit/push` recipe; with none it printed a complete `gh repo fork --clone` -> `git switch -c telemetry/<week>-<hive>` -> `cp` -> `commit` -> `push` -> `gh pr create` recipe, one clone shared across weeks, every path single-quoted; `--dry-run` copies nothing; no `git push`/`gh` call exists in the script - it only prints. `/vulyk-evolve` step 2 drives it on the weekly cadence CLAUDE.md already mandates
ASK 3: GREEN - maximally anonymized, no personal files - run: `telemetry.sh bundle --out b.jsonl && jq 'keys' b.jsonl && telemetry.sh check bad.jsonl` saw: bundle rows carry exactly the 10 keys `v code value threshold vulyk tier model agent week hive`; `ts`, `spec`, `story`, `ref` never leave `bundle_emit`; hive is 12 hex of sha256(toplevel). The gate rejected, by line: a path in a string value, an extra `ts` key, an extra `extra:"user@example.com"` key, an unknown code, tier 9 / model `gpt` / week `38` / hive `zz`, and non-JSON - rc=1. `publish` runs `check` before anything is copied and aborts on failure
ASK 4: GREEN - properly wrapped in VULYK so it does not get lost - run: `grep -n -i telemetr CLAUDE.md`; `grep -n anomalies scripts/*.sh`; `bash scripts/ship-check.sh docs/specs/anomaly-telemetry` saw: constitution carries the `| Telemetry |` Profile row and a `| Anomaly telemetry contract tests |` Commands row; scope-check.sh and ship-check.sh explicitly exempt the hook-written anomalies.jsonl and cycle.sh stages it with its own commits; `/vulyk-evolve` owns both the hive step and the repo inbox step; `.github/workflows/ci.yml` job `telemetry-inbox` runs the suite and `check`s every merged bundle; docs/telemetry.md is the written contract
ASK 5: GREEN - the public repo says logs are collected, can be PR'd, cleaned weekly, shipped in the update - url: COURT/README.md:243-261 saw: "Anomaly telemetry - opt-in, anonymized, never automatic", **Off by default**, the full 10-key schema and the 8-code enum inline, an explicit "Never included: file paths, story/spec slugs, dispatch names, emails, free text, or file contents", the `telemetry/inbox/` PR route, and the weekly distil-into-CHANGELOG-then-delete promise. run: `telemetry.sh inbox --clear` in a scratch repo saw: the per-week/per-code table printed, the week directory staged `D`, `telemetry/inbox/README.md` surviving
ASK 6: GREEN - install/update asks in the terminal, explains it, says off by default - run: sourced `print_telemetry_explanation` + a `telemetry_decide` matrix with a stubbed tty; `bash install.sh <dest>` fresh, `--upgrade`, `--check`, `--telemetry on|off` saw: a 6-line plain-language explanation ("Telemetry (optional, off by default) ... Never paths, slugs, story names, prompts, emails or any free text. Nothing is ever sent ... Default: off.") then `Enable telemetry? [y/N]` - read from /dev/tty, so a `curl | bash` install is still asked. Anything but y/yes = off. Edge cases all hold: an already-answered hive is not re-asked on upgrade; a pre-telemetry hive with no row IS asked, and writes `off` when there is no terminal; `--check` never asks and prints `would set`; ci.yml:356 fails the build if the installer ever asks with no terminal
UNASKED: (1) The `agent` token never reaches a bundle in real use. `bundle_emit`'s `IFS="$(printf '\t')" read` collapses consecutive tabs - tab is IFS whitespace in bash - so when `model` is empty the `agent` value shifts into `model` and is then dropped by `in_set "$model" "$MODELS" || model=""`. `detect_agents` never passes `--model`, so every `agent_prefix_high`/`agent_empty` row bundles as `"agent":""`. Proven: `record agent_prefix_high 99999 50000 --agent worker-code` alone -> `"agent":""`; the same row with `--model sonnet` added -> `"agent":"worker-code"`. tests/telemetry.test.sh:157 ("the agent token does travel") passes only because its fixture sets a model. Loss, not a leak - ask 3 is safe - but README:250 and docs/telemetry.md promise a field the wire never carries, and `/vulyk-evolve` loses the "which agent burns its prefix" signal. Same pattern in detect_council/detect_scope, harmless there. (2) `docs/telemetry.md` ships to no hive - the manifest of a fresh install has 0 `docs/` paths - yet the consent prompt says "Details: docs/telemetry.md", the installed Profile row cites it, and `/vulyk-evolve` calls it the schema source. It matches VULYK's existing habit (docs/cycle.md, docs/token-economy.md are cited and unshipped too), but here it is the one pointer offered to a user being asked for consent. (3) Nothing nudges the weekly run: the whole "раз в неделю" promise rests on a human remembering `/vulyk-evolve`; `default_weeks` softens a late run to two weeks, but a month of silence loses the older weeks with no warning.
BREACH: none
