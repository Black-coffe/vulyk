<!-- seat: sonnet · model: claude-sonnet-5 · round: 3 · head: 34acc61 · pack: 7260d521a997 · attempt: 1 · recorded: 2026-09-15T08:40:56Z -->
COUNCIL: anomaly-telemetry · round 3 · seat sonnet
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-3
VERDICT: GREEN
ASSUMED CONFIG: single-node CLI/shell+Python toolkit, no server, no build (per Profile Commands note)
RAN: full suite (shell/py/json/hook-status) + bash tests/telemetry.test.sh (189 checks, 0 failed) + manual scripts/telemetry.sh record/bundle/check/publish (local & PR recipe) + install.sh fresh/--telemetry on/--upgrade
PATH: CLI only - scripts/telemetry.sh verbs, .claude/hooks/anomaly-scan.sh Stop/SessionEnd hook, install.sh consent prompt, /vulyk-evolve step, README.md section
ASK 1: GREEN - all VULYK terminals monitor anomalies and keep a log file - run: jq '.hooks' .claude/settings.json (anomaly-scan.sh wired on Stop+SessionEnd); bash scripts/telemetry.sh record context_high 150000 140000 --model sonnet --ref test:manual1 (VULYK_HIVE=scratch) saw: row appended to memory/stats/anomalies.jsonl; dup record with same ref did not grow the file (idempotent)
ASK 2: GREEN - weekly send: local commit locally, PR from other machines - run: VULYK_HIVE=scratch VULYK_LOCAL=<scratch-repo> bash scripts/telemetry.sh publish (and without VULYK_LOCAL) saw: local path wrote telemetry/inbox/<week>/<hive>.jsonl and printed `git add/commit/push` (not run); no-local path printed full `gh repo fork/clone/switch/commit/push/pr create` recipe; no git/gh command actually executed in either case
ASK 3: GREEN - logs maximally anonymized, no personal files - run: bash scripts/telemetry.sh bundle --out scratch/bundle.jsonl && bash scripts/telemetry.sh check scratch/bundle.jsonl saw: bundle row `{"v":1,"code":"context_high","value":150000,"threshold":140000,"vulyk":"0.0.0","tier":0,"model":"sonnet","agent":"","week":"2026-W38","hive":"da11cfc507e4"}` (10 fixed keys, codes/numbers/hash only); check exit 0; CHECK_JQ rejects any string value containing `/`, `\`, `@` or whitespace
ASK 4: GREEN - correctly wrapped into the VULYK framework so it isn't lost - run: grep Telemetry CLAUDE.md Profile row, Commands table row "Anomaly telemetry contract tests"; grep -n telemetry .claude/commands/vulyk-evolve.md saw: Profile row present, dedicated Commands row, /vulyk-evolve step 2 runs consent/publish and step for inbox distil-and-clear, docs/telemetry.md (163 lines) is the full contract
ASK 5: GREEN - public GitHub repo documents the collection/PR/weekly-cleanup story - run: sed -n '240,262p' README.md saw: "### Anomaly telemetry — opt-in, anonymized, never automatic" section describing off-by-default, the 10-key schema, the installer question, and the weekly /vulyk-evolve inbox distil-and-clear cycle
ASK 6: GREEN - install/update asks the user about telemetry, explains it, default off - run: bash install.sh <scratch> (no tty) then bash install.sh <scratch2> --telemetry on then bash install.sh <scratch2> --upgrade saw: fresh install with no terminal writes `Telemetry | off ...` silently (no question printed, matches contract - the question only fires on a real tty, per print_telemetry_explanation + telemetry_tty guard in install.sh, also exercised by tests/telemetry.test.sh's "no terminal: no question/explanation printed" checks); `--telemetry on` sets the row to `on`; a later `--upgrade` leaves the answered row byte-identical (on)
UNASKED: none
BREACH: none
