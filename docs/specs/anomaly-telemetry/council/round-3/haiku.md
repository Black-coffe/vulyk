<!-- seat: haiku · model: claude-sonnet-5 · round: 3 · head: 34acc61 · pack: 7260d521a997 · attempt: 2 · recorded: 2026-09-15T08:40:49Z -->
COUNCIL: anomaly-telemetry · round 3 · seat haiku
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-3
VERDICT: GREEN
ASSUMED CONFIG: single hive, CLI-only toolkit, no server/DB; Client path unfilled in Profile so judged via the repo's own `## Commands` table (scripts/*.sh, install.sh) as a person reaching VULYK would
RAN: bash scripts/telemetry.sh (enum|consent|scan|bundle|check|publish); bash install.sh <tmp> --telemetry ask (non-tty); bash install.sh <tmp> --telemetry on; grep for "Telemetry"/"pull request" in README.md and docs/telemetry.md; read memory/stats/anomalies.jsonl and telemetry/inbox/README.md
PATH: CLI only — ran scripts/telemetry.sh subcommands and install.sh against a scratch target directory, then read the shipped docs (README.md, docs/telemetry.md) a GitHub visitor would see
ASK 1: GREEN - all VULYK terminals monitor anomalies and keep a log file - run: bash scripts/telemetry.sh enum saw: 8 detector codes (context_high, agent_prefix_high, agent_empty, council_rounds_high, stage_long, driver_refused, driver_relaunched, scope_breach); run: tail -5 memory/stats/anomalies.jsonl saw: real scope_breach/council_rounds_high entries already logged with ts, code, value, story, ref
ASK 2: GREEN - weekly send to the VULYK repo: local = commit into own repo, remote machines = pull request - run: bash scripts/telemetry.sh publish saw: "telemetry: off (Profile row Telemetry) - nothing to send" (prints, sends nothing, per docs/telemetry.md "Sending a bundle - always a pull request, never automatic"); url: docs/telemetry.md#L95 saw: "Sending a bundle - always a pull request, never automatic" and telemetry/inbox/README.md describing the inbox + gh pr create path
ASK 3: GREEN - logs maximally anonymized: context anomalies, time overruns, VULYK redundancy, no personal files - run: bash scripts/telemetry.sh bundle saw: lines like {"v":1,"code":"council_rounds_high","value":3,"threshold":3,"vulyk":"0.0.0","tier":4,"model":"","agent":"","week":"2026-W38","hive":"4ec42a3169dc"} — only codes/numbers/hash, no story names, no free text, no file paths
ASK 4: GREEN - properly wrapped into the VULYK framework so it isn't lost - run: bash install.sh /tmp/tmp.H6bV1TgG6U --telemetry ask < /dev/null saw: installer output "copy scripts/telemetry.sh", "reset CLAUDE.md '## Profile block' -> placeholders", and grep on the installed CLAUDE.md returned "| Telemetry | off - anonymized weekly anomaly bundle (codes and numbers only, docs/telemetry.md); on = /vulyk-evolve prints the send command, never sends |" — the feature ships through the same installer path as every other VULYK row, not a side file that gets forgotten
ASK 5: GREEN - public GitHub repo documents that logs are collected, can be pushed as a PR, and are cleaned weekly with an update - url: README.md#L243 saw: "### Anomaly telemetry — opt-in, anonymized, never automatic" section explaining consent prompt, PR flow, and weekly `scripts/telemetry.sh inbox` distillation into "per-week, per-code counts" that "ship with the next release", linking docs/telemetry.md for the full contract
ASK 6: GREEN - install/update asks the user in the terminal whether to enable telemetry, explains it, defaults off - run: bash install.sh /tmp/tmp.nvtYaVaKJ5 --telemetry on saw: installer accepted the flag and wrote "| Telemetry | on - ..." into CLAUDE.md, confirming the consent mechanism wires through to the Profile row; run: bash install.sh ... --telemetry ask < /dev/null (no tty) saw: it fell back to "off" silently rather than hanging — consistent with README's documented "installer asks once — Enable telemetry? [y/N], default off"; could not drive an interactive y/N prompt from this non-tty shell, so the interactive wording itself is taken from README.md#L254 rather than observed live
UNASKED: none
BREACH: none
