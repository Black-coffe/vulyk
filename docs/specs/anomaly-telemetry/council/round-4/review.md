<!-- seat: review · model: unknown · round: 4 · head: 3984652 · pack: 07a6a02fb6fb · attempt: 1 · recorded: 2026-09-15T09:56:27Z · verdict: PASS -->
VERDICT: PASS

Reviewed: docs/specs/anomaly-telemetry (plan incl. A18, Contracts "Seen-list" and "Hook-written log ownership", `## Descoped`, `## Plan deltas`; brief incl. the owner's reopen line; stories 11-12; journal; round-3 review), diff b15b4ba..3984652 with round 4's own commits b4c5401 (paperwork), b8a5844 (11), e3fccde (12), 3984652 (open-round), ADR-001. Scope: b8a5844 touches `scripts/telemetry.sh`, `tests/telemetry.test.sh`, its story file and `memory/stats/scope.jsonl` (close-story's write); e3fccde touches exactly its five named files minus `scripts/lib.sh` (left untouched, said so) plus story file and scope.jsonl; no Law 3 violation. `docs/wiki/` does not exist. Ran only what the diff made suspicious, on a `git archive 3984652` scratch copy so the real log stayed untouched: `scan --transcript` over the real 73-subagent session `7670018b` with an argv-logging python shim - **22 402 ms / 74 starts cold, 6 865 ms / 1 start, 7 107 ms / 1 start with `--final`; zero subagent basenames reach python after the first run; seen-list 73 lines, 3 fields each**. The story's timing claim (22.6 s / 74, 6.8 s / 1, 7.1 s / 1) holds to the number, and round-3 Critical 1's condition is met on the path it named. `docs/telemetry.md:117-128` was compared line by line with `recipe_pr()` (`scripts/telemetry.sh:700-724`): fork, `cd`, `base=`, `git switch "$base"` + seven lines = 11, placeholders and quoting identical; the second-week sentence matches the loop. Story 12's cycle cases assert observable state (scope-check's `! path` line, ship-check's stage-03 text on a real untracked file) - not theater. Also ran the hook itself in the scratch hive with four `CLAUDE_PROJECT_DIR` forms and with a Windows-only PATH while chasing finding 1.

## Critical

None.

## Major

1. `memory/stats/anomalies.jsonl:154-155` (uncommitted in the real hive, mtime 09:45:27Z, i.e. written during this round by a `scan` I did not run) and `scripts/telemetry.sh:189` (`"$(now_ts)"` substituted unchecked) - **plan** - two `scope_breach` rows for stories 11 and 12 carry `"ts":""`; every one of the 153 committed rows has a timestamp, and I could not reproduce the empty `ts` from the Bash tool (hook via stdin payload with `CLAUDE_PROJECT_DIR` in backslash, forward-slash, POSIX and unset forms all stamp; a Windows-only PATH writes nothing at all). A row without `ts` has no week (`week_of "" = ""`, `:102-106`), so `bundle`/`publish` silently drop it and no local `check` ever sees it - the anomaly exists in the log and never leaves the hive. Severity assumes what is on disk: some real hook environment on this machine produces these rows today. Condition: `record` must never write a row whose `ts` is empty (stamp with a fallback or refuse and stay silent), and the producer of the two rows at lines 154-155 must be identified before the log is committed by the next `cycle.sh --commit` verb.

2. `scripts/telemetry.sh:332` (`basename` per file), `:334` (`wc -c` per file via `file_bytes`), story 11 notes "The residual ~7 s is `find` + 73 `wc -c` + `record`/`jq`" - **plan** - the story's bar was python starts and it cleared it, but the steady-state `Stop` scan still costs ~6.9 s on this hive's real session against 1.2 s with no transcript (round-3 measurement): two process spawns per subagent (~150 at ~45 ms on Windows) on every turn end, in a hook that blocks the turn. Severity assumes the maintainer's configuration (Windows hive, Tier-3 sessions with 50-90 subagents); on Linux the same loop is well under a second. Condition: the per-subagent steady-state cost of `detect_agents` must not include a process spawn per file (one `find -printf` size+path listing, or parameter expansion for the basename), and the plan records the residual budget it accepts for a `Stop` scan.

## Minor

3. `scripts/telemetry.sh:312-316`, `:376-377`, story 11 notes line 54 "A cached line the batch pass cannot parse falls back to a per-file jq, so a corrupt list degrades to the old cost, never to a wrong answer" - **worker** - the batch pass maps an unparseable JSON to an empty object (`try ... catch`) and still emits a line, so `fline` is non-empty (`0 0 false ""`), the per-file fallback never runs, no re-measure happens, the detectors see "no anomaly", and the corrupt line is rewritten into the new list unchanged; the same holds for a line whose JSON is the empty object that `measure` prints for a transiently unreadable file (cached with its byte size, never measured again unless the file grows). The claim is overstated on both halves. Condition: a seen-list line whose JSON lacks `first_prefix` (empty object or unparseable) counts as not measured and the file is passed to `measure` again.

4. `tests/telemetry.test.sh:490-576` (case 10b), story 11 notes line 56 "the cached JSON is proven to drive `agent_prefix_high` and `--final` `agent_empty`" - **worker** - only the `agent_empty` half is proven: the over-threshold fixture's `agent_prefix_high` row is written by the fresh measure on scan 1, so on scans 2-3 the log-ref skip (`:357-359`) fires before the cached fields are read; no assertion shows a cached line alone producing an `agent_prefix_high` row. Condition: either a case where the cached JSON alone yields the prefix row (log emptied between scans, file unchanged), or the note claims only the `agent_empty` half.

5. `docs/specs/anomaly-telemetry/anomaly-telemetry-12-recipe-docs-and-scope-gate.md:44-47` - **plan** - all four acceptance boxes are unchecked under `status: done`, and the fourth names "the story-09 scope case that asserted `skills.json` excluded, which is inverted, not deleted" - `tests/cycle.test.sh:175-215` never had such a case (story 09 asserted only `anomalies.jsonl` with `out_of_scope:0`); the worker added new cases and wrote "left unchanged" without saying the criterion described a case that did not exist. Condition: the story's criteria describe the suite as it is, ticked or annotated where the text was wrong.

6. `docs/telemetry.md` (no mention; grep `seen` = 0), plan `## Contracts` "Seen-list" - **plan** - the public "what is collected / where it lives" document does not name the new per-machine cache `.vulyk/telemetry/seen/<sid>` that holds per-subagent measure JSON (incl. the raw `agentType`) and transcript basenames; story 11 was forbidden the docs and story 12's docs scope was the recipe block only, so nobody owned the sentence. Condition: `docs/telemetry.md` names the cache, that it is gitignored and never bundled, and how to clear it.

7. `scripts/telemetry.sh:394-397` - **plan** - one seen-list file per session, written forever, with no retention (the `.vulyk/telemetry/<week>-<hive>.jsonl` bundles accumulate the same way); harmless per file (~15 KB) but unbounded. Condition: the plan records the accepted growth or a verb/step prunes files whose session transcript no longer exists.

8. `memory/stats/scope.jsonl:107-109` (`out_of_scope` 6, 7 for story 11 and 1 for story 12) and the two `scope_breach` rows of finding 1 - **plan** - two stories built in one worktree see each other's dirty files at `close-story`, so the scope gate records a breach for both and the hook turns it into permanent anomaly rows (values 6 and 1) that `/vulyk-evolve` will count; `## Descoped` discounts only pre-story-09 hook-written paths, not this shape. Condition: `## Descoped` (or the evolve step) records that a breach whose paths are a concurrent story's own `## Files` is a pipeline artefact, or `close-story` scopes its diff to the story's own commit.

9. `memory/stats/anomalies.jsonl` (0 `context_high` rows) vs the scratch scan of session `7670018b` recording `context_high` 271 320 > 140 000 with ref `session:7670018b-...` - **plan** - the same real session scanned from the Bash tool produces a context row the real hive's hooks never produced; whether the real hook resolves a window (70 % of 1M) that the scratch copy cannot, or `detect_context` never ran for that session, is not diagnosed here - together with finding 1 it says the hook environment and the Bash-tool environment disagree and only the latter has ever been measured. Condition: one recorded run of `anomaly-scan.sh` from a real `Stop` payload in this hive, with its rows and `ts` shown, before the ship claims ask 1 delivered.

10. `tests/cycle.test.sh:249-252` - **worker** - the scope-check case greps for the literal path in the command's full output rather than in the `scope.jsonl` row it writes (as the story-09 case above it does); today the only line that can carry a path is the `  ! path` breach line so the assertion is real, but it will pass on any future informational line naming the file. Condition: the case reads `out_of_scope` from the `scope.jsonl` row for the story like its story-09 sibling.
