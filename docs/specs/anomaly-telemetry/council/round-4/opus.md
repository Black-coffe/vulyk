<!-- seat: opus · model: claude-opus-5[1m] · round: 4 · head: 3984652 · pack: 07a6a02fb6fb · attempt: 1 · recorded: 2026-09-15T09:56:23Z -->
COUNCIL: anomaly-telemetry - round 4 - seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/anomaly-telemetry/round-4
VERDICT: RED
ASSUMED CONFIG: Profile rows are still installer placeholders (`<fill in>`) except `| Telemetry | off - ... |`; judged as the VULYK source repo itself - single machine, no deploy target, no Browser MCP.
RAN: telemetry.sh consent|enum|agents|record|scan|bundle|check|publish|inbox against a scratch fixture hive (VULYK_HIVE/VULYK_LOCAL); install.sh fresh install + 4 upgrades into a scratch project; anomaly-scan.sh with a stripped PATH; targeted reads of install.sh, scope-check.sh, lib.sh, .claude/settings.json, .github/workflows/ci.yml, README.md, docs/telemetry.md, docs/hooks-reference.md, docs/command-reference.md, .claude/commands/vulyk-evolve|build|resume|bootstrap.md
PATH: none named (Client path is `<fill in>`); walked the CLI surface instead - bash scripts/telemetry.sh end to end, plus the Stop/SessionEnd hook against a synthetic transcript with two subagent files
ASK 1: GREEN - every VULYK terminal monitors and keeps a log - run: install.sh into a tmp project, then deleted both anomaly-scan entries from .claude/settings.json and re-ran `install.sh --upgrade` twice saw: hook wired at Stop and SessionEnd, occurrences 0 -> 2 -> 2 (idempotent re-wire); `telemetry.sh scan --transcript <fixture>` appended context_high 200010/140000 and agent_prefix_high 65000/50000 to memory/stats/anomalies.jsonl in 1.4 s, a re-scan added nothing; the same scan produced council_rounds_high (4/3, tier read from plan.md), stage_long (72 h/24) and scope_breach; driver_refused/driver_relaunched have record call sites in vulyk-build.md:22,48 and vulyk-resume.md:17; the hook is fail-open (PATH without jq -> rc=0, nothing written); round-3 repair 1 holds - with python removed from PATH the second scan still ran off .vulyk/telemetry/seen/<sid> (the under-threshold subagent cached, seen file byte-identical), and `--final` then recorded agent_empty exactly once.
ASK 2: GREEN - weekly send to the VULYK repo, locally or as a PR - run: `telemetry.sh publish` with consent off, then on, with and without VULYK_LOCAL saw: off -> "telemetry: off (Profile row Telemetry) - nothing to send"; on + local checkout -> wrote <repo>/telemetry/inbox/2026-W38/<hive>.jsonl and printed the git add/commit/push recipe; on + no checkout -> the 11-line "gh repo fork ... gh pr create" recipe, every path single-quoted, base= captured once so a second week cannot branch off the first; nothing is executed (no git push, git commit or gh call exists in the script); cadence is /vulyk-evolve step 2 (vulyk-evolve.md:53,55), default window = previous ISO week + current; the repo side `telemetry.sh inbox` distilled "2026-W38 context_high 1 1" / "2026-W38 scope_breach 1 1" and `--clear` stages the git rm.
ASK 3: RED - anonymized logs carrying the context / time / VULYK-redundancy anomalies - run: `telemetry.sh record agent_empty 3 0 --agent worker-code --ref agent:custom2` then `telemetry.sh bundle --week 2026-W38` saw: local row "agent":"worker-code", bundle row "model":"","agent":"" - the agent token is silently dropped. Cause: bundle_emit's `while IFS=<tab> read -r ts code value threshold vulyk tier model agent` - tab is IFS whitespace in bash, so consecutive tabs collapse (printf 'a\t\tb\n' piped into read -r x y z gives x=[a] y=[b] z=[]) and an empty model shifts the last field off the end. detect_agents never passes --model (telemetry.sh:391,395), so every real agent_prefix_high / agent_empty row loses its agent token on the way into the bundle; the same row recorded with --model sonnet bundles correctly ("agent":"worker-test"), which is why docs/telemetry.md's example hides it. No leak - it over-drops - but the one dimension that answers which caste is the VULYK redundancy never leaves the machine, while README.md:250 and docs/telemetry.md both promise it does. The rest of the ask held: the bundle drops ts/spec/story/ref (a --story value holding a path and a quote bundled with no trace of it); `check` rejected an 11th key ("key set is not the 10 bundle keys") and "model":"/home/andrei" ("a string value carries a path, an address or whitespace"); a custom .claude/agents/*.md name never reached a bundle row; hive is 12 hex of a sha256 of the toplevel path.
ASK 4: GREEN - wrapped into VULYK so it is not lost - run: `install.sh --upgrade` after touching the installed copies; reads of .gitignore, ci.yml, scope-check.sh, lib.sh, CLAUDE.md saw: "update scripts/telemetry.sh" and "update .claude/hooks/anomaly-scan.sh" (framework-owned, so fixes reach existing hives); the consent row survives an upgrade untouched once answered; .vulyk/ is gitignored so seen-lists and bundles are never committed while memory/stats/anomalies.jsonl is; CI job telemetry-inbox runs the suite and `check` over telemetry/inbox/**/*.jsonl; the constitution carries both the Telemetry Profile row and a tests/telemetry.test.sh Commands row; round-3 repair 3 holds - scope-check.sh:74-78 excludes only memory/stats/anomalies.jsonl and records that skills.json "counts like any other path".
ASK 5: GREEN - the public repo states the collection, the PR route and the weekly clean + update - url: README.md:243-261 ("Anomaly telemetry - opt-in, anonymized, never automatic") saw: the 10 bundle keys and the full code enum inline, "Never included: file paths, story/spec slugs, dispatch names, emails, free text, or file contents", "Enable telemetry? [y/N]", "you push the PR into telemetry/inbox/ yourself", and the weekly loop - inbox distilled into that run's CHANGELOG entry, emptied week directories staged for deletion, "so the improvements ship with the next release"; telemetry/inbox/README.md and docs/telemetry.md repeat it at contract depth.
ASK 6: GREEN - install/upgrade asks in the terminal, explains, default off - run: `install.sh <tmp> --telemetry ask` with stdin from /dev/null, then upgrades with the row on, with the row deleted, and with `--telemetry on` saw: the fresh non-tty install wrote "| Telemetry | off - ... |" and `telemetry.sh consent` -> off; a deleted row came back as off on --upgrade; an existing "on" was left untouched (asked once, never overwritten); --telemetry on flipped it. The prompt itself is telemetry_decide over /dev/tty (never the stdin the copy loops consume): a six-line explanation ending "Nothing is ever sent ... Default: off" followed by "Enable telemetry? [y/N]" - no tty was reachable in this court, so that branch is read, not run.
UNASKED: The reference docs drifted from the feature. docs/hooks-reference.md tabulates all seven other hooks and never mentions anomaly-scan.sh - the one new hook that fires on every Stop and writes a committed file; docs/self-evolution.md, which /vulyk-evolve's command-reference entry points at, has no telemetry or inbox step at all. And docs/telemetry.md is never installed into a hive (install.sh:636 copies only docs/wiki, docs/specs, docs/adr), yet the hive's own Telemetry Profile row, the installer's prompt ("Details: docs/telemetry.md") and vulyk-evolve.md:43 all send the reader to that path - the person deciding whether to opt in is pointed at a file their project does not have. Separately, and honestly labelled in docs/telemetry.md: the fork/PR recipe has never been run against a real fork (gh pr create --head <branch> from a fork clone assumes gh resolves the head repo from the branch's remote).
BREACH: none
