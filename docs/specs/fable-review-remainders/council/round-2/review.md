<!-- seat: review · model: unknown · round: 2 · head: e166c48 · pack: 37a505564b66 · attempt: 1 · recorded: 2026-09-14T14:49:36Z · verdict: PASS -->
VERDICT: PASS

Scope: every file in `git diff 43a2baa..e166c48` is named by a story (01: install.sh, ci.yml · 02: scripts/cycle.sh, ADR-001 · 03/07/08: vulyk-cycle.js · 04/09: vulyk-build.md, vulyk-review.md, the three council-*.md, lead-review.md · 05: tests/council.test.sh · 06/07/08: tests/driver.test.sh) or is Queen/hook paperwork (plan.md, journal.md, council/round-1 and round-2, recon/06-attempt-2-kept.patch, memory/learnings, memory/stats). No Law 3 violation. Round-2 commits 995765b and 88686a8 touch only the files stories 08 and 09 name.

Round-1 fixes verified: critical 1 - `.claude/workflows/vulyk-cycle.js:35-42` `reasonFor` returns null for any non-empty string, the worker path at `:186-198` sends every non-empty return to `close-story` and derives `worker returned no report` from exit 4 + `error === 'returned: missing'` (the string `scripts/cycle.sh:1497` emits, recorded in ADR-006 `:95`); the seat path at `:241-251` derives it from `record-seat` exit 4 on a return the classifier called usable; ADR-006 `:135` and C11 hold again without amendment, and `worker-code.md:18` / `worker-test.md:21` are true again (major 3). Major 2 - `vulyk-build.md:68-84` now runs `close-story` on every non-empty return and maps exit 4 `returned: missing` to the same string. Minor 4 - closed on the record in the second plan delta. Minor 7 - `tests/driver.test.sh` scenario (z) asserts `--file .vulyk/reports/demo/round-1/sonnet.attempt-2.md`.

Verification spot-checks: ran `bash tests/driver.test.sh` once (the one suite story 08 rewrote alongside the code it proves) - exit 0, every line `ok`, including the restored ADR-006 scenario "no STATUS: line but close-story ok: closes, nothing logged". Traced v2/v3/x2/x3/y3/z through the stub by hand: each would go red if the driver skipped `close-story` on prose (v2's queue underruns into a TypeError), doubled the reason on an empty seat return (x3), or heredoc'd the re-ask (z). `trace-check.sh`: 0 unfound, 0 storyless; the 2 uncovered brief lines are the verbatim request and the confirmation sentence, as in round 1. On-disk evidence for the plan's cwd assumption: `.vulyk/reports/fable-review-remainders/round-1/{haiku,sonnet,opus,review}.attempt-1.md` exist at the hive root and are byte-identical after the header to the recorded seat files; `round-2/haiku.attempt-1.md` appeared at the hive-root path while this review ran. The driver log itself is not on disk for me to read, so "no `file:` fallback" is inferred, not seen.

## Critical

None.

## Major

1. `.claude/commands/vulyk-build.md:88` (`dispatch:<seats>` row) - **plan** - the fallback loop names two of C3's three seat reasons (`seat <seat> returned empty - ...`, `seat <seat> returned no report`) and never `seat <seat> threw: <message>` / `reviewer threw: <message>`, which the Workflow driver logs at `vulyk-cycle.js:36,241-242`; story 04's acceptance criteria asked the three strings of the `build:<wave>` row only and story 09's of the empty and no-report cases only, so no story told a worker. Ask 5 names seats and the reviewer, and C3's own sentence says "04 mirrors in the fallback loop". Condition: the `dispatch:<seats>` row must name a dispatch that errored out as `seat <seat> threw: <message>` (`reviewer threw: ...` for `lead-review`) before `record-seat` is called, with the empty-body recording unchanged, so both drivers name the same three reasons for a seat.

2. `.claude/commands/vulyk-build.md:88` and `.claude/commands/vulyk-review.md:38-42` - **worker** - both fallback texts give the Tier 4 *first* reviewer dispatch a report path ("every prompt but the Tier 4 second reviewer's ends with ...") and `vulyk-build.md`'s "Record, good case: `... --file <path>`" carries no Tier 4 exclusion of its own, while C2 and the Workflow driver (`vulyk-cycle.js:118-122`, `:226`) give *neither* Tier 4 reviewer a path and record the fold by heredoc only; `vulyk-review.md:60-66` says the fold "never gets a report path", contradicting its own step 3. Following the build row literally at Tier 4 records `review.attempt-1.md` - one reviewer's unfolded report - and drops the second reviewer's verdict, defeating "the stricter of the two". Major *if* a Tier 4 spec is ever driven through the fallback loop (the path `/vulyk-build` refuses without `--fallback`); harmless at Tier 1-3, which is every spec so far. Condition: at Tier 4 no reviewer dispatch in either fallback text may carry a report path, and the review seat's record step must say the fold goes through the heredoc only, matching C2 and the Workflow driver.

## Minor

3. `docs/specs/fable-review-remainders/fable-review-remainders-08-driver-third-reason-from-the-verbs.md` AC 1 vs `vulyk-cycle.js:104-113` - **plan** - the criterion "grep for STATUS:/VERDICT: finds only comments" is not met: `foldReviews` still holds four `^VERDICT:` regexes, which the same story's non-goals forbid touching; the worker recorded the contradiction in its notes and chose the non-goal. Condition: the story's criterion must be amended on the record to exempt `foldReviews`' first-line parse (the driver's one recorded prose read, `:97`), so the AC checkbox and the code agree.

4. `vulyk-cycle.js:251` vs story 08 non-goal "the fold's `record-seat` exit 4 keeps today's handling with no new log line" - **plan** - a Tier 4 fold is a non-empty string, so `reason === null` and a `record-seat` exit 4 on it logs `reviewer returned no report`; that matches C3 (which excludes nothing at Tier 4) but not the story's non-goal, and no scenario covers the fold path. Condition: C3 or the story must say which of the two is intended at Tier 4, and one driver scenario must assert it (a `NO VERDICT` fold recorded by heredoc, exit 4, and the presence or absence of the reviewer no-report line).

5. `.github/workflows/ci.yml:345-346` - **worker** - the step greps the Browser MCP row out of the source `CLAUDE.md` into `browser_mcp_row` and then runs `grep -qF "$browser_mcp_row"` on the target: if the source row were ever absent or renamed, `grep -qF ""` matches any line and D4.7's content check passes vacuously. Today the source has exactly one such row (`CLAUDE.md:131`), so the check is real. Condition: the step must fail when the source row is not found, before comparing.

6. `tests/council.test.sh:2105-2106`, `:2440-2444`, `:2559-2563` - **worker** - when `pin_cycle` fails, the `mktemp` copies `$OLDCYCLE` (`OLDCYCLE=""` discards the path), `$OLDCYCLE2`, `$OLDCYCLE3` are never removed; only the `$OLDCYCLE4` site removes on both branches. Round-1 minor 6 named two of the three sites; the plan defers it to the next circle. Condition: every pinned temp copy is removed on both branches.

7. `vulyk-cycle.js:225-231` (`record`) - **plan** - the `--file` call is made whatever the dispatch returned, so an attempt file left by an earlier run of the same round number is recorded as this dispatch's report even after the line above logged a throw or an empty return (round-1 minor 5, deferred to the next circle on the record). Narrow in practice: a seat that already has a seat file is not re-dispatched, and a stale file exists only when a seat wrote it and the record never landed - in which case recording it is usually right. Condition: as in round 1 - a relaunched round must not record a report file this dispatch did not write, or the plan must record that a leftover attempt file from the same round is an accepted report.

8. `docs/specs/fable-review-remainders/plan.md:15` (`## Assumptions`, the seat's cwd) - **plan** - the line still reads "still unverified"; the on-disk evidence above (four round-1 files at the hive-root path, byte-identical to the recorded seats; the round-2 haiku file at the same path) settles the cwd question even though the driver log's absence of a `file:` fallback is not on disk for a reviewer to read. Condition: the assumption line must record what was observed and what remains inferred, so the next circle does not re-open it.

9. `.claude/commands/vulyk-review.md:60-66` - **plan** - the one-round review command records seats by `--file` with the heredoc fallback but names none of C3's three reasons for a dead seat or reviewer dispatch; story 09 explicitly excluded the file ("major 2 names only the build loop"), and C3 speaks of "the fallback loop's stop text" only. Condition: the plan must state whether `/vulyk-review`'s seat handling is a driver under R6 (then it names the same three reasons) or is exempt, on the record.

10. `docs/specs/fable-review-remainders/fable-review-remainders-09-fallback-loop-same-bound-for-no-report.md` `## Requirements` - **plan** - the second quote, "Оба драйвера и три описания становятся правдой без правки.", is the owner's `release` answer, borrowed here to anchor the R6 "same bound" rule for ask 5; trace-check accepts it, but it makes the release answer look like a requirement on the fallback loop's miss classification. Condition: the story's requirement quotes must come from the ask the story serves, or the plan must say R6 is the source.

11. `install.sh:220-227` - **plan** (pre-existing, outside ask 1, round-1 minor 8) - on a target missing both blocks, Profile takes the EOF path and lands after `## Compact instructions` while Commands lands before it; byte identity holds. Condition: none for this round - recorded for the next circle.

12. `.claude/commands/vulyk-build.md:88` - **worker** - "logged `seat <seat> returned empty - ...` before `record-seat` is called, as today": the log line is new in this spec, so "as today" is false prose. Condition: the sentence must not claim the log line pre-existed.
