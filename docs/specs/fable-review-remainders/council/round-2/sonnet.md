<!-- seat: sonnet · model: claude-sonnet-5 · round: 2 · head: e166c48 · pack: 37a505564b66 · attempt: 1 · recorded: 2026-09-14T14:49:26Z -->
COUNCIL: fable-review-remainders · round 2 · seat sonnet
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/fable-review-remainders/round-2
VERDICT: GREEN
ASSUMED CONFIG: VULYK's own repo - shell + Python + markdown toolkit, no compiler, no build; suite = the four quiet commands run together
RAN: git ls-files '*.sh' | xargs -n1 bash -n; python -m py_compile .claude/hooks/*.py; git ls-files '*.json' | xargs -n1 jq -e .; bash .claude/hooks/handoff.sh status; bash tests/cycle.test.sh; bash tests/council.test.sh; bash tests/driver.test.sh; manual install.sh --upgrade Profile-restore run; manual claim/pause/release run
PATH: none: library only (shell toolkit, no running client)
ASK 1: GREEN - install.sh --upgrade inserts Profile/Commands byte-identical on both anchor and EOF paths; D4.7 checks row content - run: manual `install.sh $target --upgrade` after stripping Profile block, then `diff` of Browser MCP row; also ci.yml lines 323-346 do the same via `bash tests/council.test.sh`'s sibling CI job (not run here, code-read only) saw: "PASS: browser mcp row byte-identical" / "IDENTICAL"; install.sh:209-228 shows one `printf '%s\n' "$block"` feeding both the anchor-insert and the append(EOF) branch
ASK 2: GREEN - LR31 scenario in tests/council.test.sh: ready+not-ready story share a wave, wave_stories names only the ready one - run: bash tests/council.test.sh saw: "ok LR31: ready A and not-ready B (blocked_by todo/blocked C) -> wave_stories names only A" and "ok LR31: blocker C done -> both A and B are listed"
ASK 3: GREEN - pinned-source probes fail the suite (fail=1) on git-show error/empty and on a non-ok [branch] result; council CI job uses fetch-depth 0 - run: bash tests/council.test.sh; grep fetch-depth .github/workflows/ci.yml saw: pin_cycle (L2073-2090) sets fail=1 and prints "[<sha>] unavailable: <reason>" on non-zero exit or empty result; check_probe_result (L2092-2102) sets fail=1 on any non-ok [branch] result; full suite run exited 0 with all [branch] probes "ok" and only pre-fix [ca0c74a^] probes FAIL as expected; ci.yml:115 "fetch-depth: 0" under the council job
ASK 4: GREEN - release is not pause_guard-gated and exits 0 on a paused spec; ADR-001 names release among the exempt verbs - run: manual fixture - claim docs/specs/rel1, pause --commit, then release stampA saw: "pause exit: 0" / "cycle: rel1 - released" / '{"ok":true,"verb":"release","exit":0,"next":"released"}' / "release exit: 0" / "DRIVER removed"; docs/adr/001-cycle-state-contract.md:161 "`pause`, `resume`, `release` are exempt"; tests/council.test.sh Story 17 also runs "release under PAUSE exits 0, ok:true, DRIVER gone" - ok
ASK 5: GREEN - driver distinguishes worker threw / empty return (turn-cap, names agent+maxTurns) / no-report text, for workers, council seats and reviewer - run: bash tests/driver.test.sh saw: "ok C3 worker threw: stop.error carries it...", "ok C3 worker empty: names worker-code and maxTurns 90, logged per miss", "ok C3 worker no report: close-story exit 4 returned: missing is the reason", "ok C3 seat empty: names council-sonnet and maxTurns 60, still recorded", "ok C3 reviewer empty: names lead-review and maxTurns 60, still recorded" (all ok, suite exit clean)
ASK 6: GREEN - record-seat accepts --file <path> (stdin still works); driver writes seats/reviewer to a stamped path and falls back to heredoc if absent - run: bash tests/driver.test.sh; grep -n -- "--file" scripts/cycle.sh saw: "ok C2 record good: exactly one record-seat --file call, no heredoc delimiter", "ok C2 record fallback: exit 2 file: falls back to the stamped heredoc with the chat reply", "ok record-seat proceeds with the matching --stamp (seat file written)" (tests/council.test.sh Story 17); scripts/cycle.sh:1302-1333 defines --file parsing in cmd_record_seat
UNASKED: none
BREACH: none
