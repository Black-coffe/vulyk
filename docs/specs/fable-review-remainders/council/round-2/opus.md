<!-- seat: opus · model: claude-opus-5[1m] · round: 2 · head: e166c48 · pack: 37a505564b66 · attempt: 1 · recorded: 2026-09-14T14:49:33Z -->
COUNCIL: fable-review-remainders · round 2 · seat opus
MODEL: claude-opus-5[1m]
COURT: E:/Projects/vulyk/.vulyk/court/fable-review-remainders/round-2
VERDICT: GREEN
ASSUMED CONFIG: none given - every Profile row in COURT/CLAUDE.md is still `<fill in>` (shell+Python+markdown toolkit, no runner)
RAN: install.sh into two scratch targets + --upgrade on both (anchor path and anchor-renamed EOF path), block byte-diff vs source; cycle.sh status --json on a hand-built LR31 fixture with and without the blockers_done filter; pin_cycle/check_probe_result extracted and run against a bogus sha; cycle.sh claim/pause/release with matching and foreign stamps; node on the extracted reasonFor/CAPS; cycle.sh open-round + record-seat --file vs stdin in a scratch repo
PATH: none named - Profile Client path is `<fill in>`; walked the CLI surfaces instead (install.sh --upgrade, scripts/cycle.sh verbs, .claude/workflows/vulyk-cycle.js in node)
ASK 1: GREEN - --upgrade inserts the Profile block byte-identically on both paths; D4.7 checks the Browser MCP row's content - run: install.sh into t1 (Profile section stripped) and t2 (stripped + `## Commands` renamed to force the EOF branch), then `cmp` of the `## Profile`..`PROFILE:END` region against source CLAUDE.md saw: "ANCHOR PATH: byte-identical" and "EOF PATH: byte-identical", the `| Browser MCP |` row with its escaped `\|` intact on both; ci.yml:345-346 does `browser_mcp_row="$(grep '| Browser MCP |' CLAUDE.md)"` + `grep -qF "$browser_mcp_row" "$target/CLAUDE.md"`, i.e. content, not the row count
ASK 2: GREEN - LR31 reds against a pre-fix cycle.sh; only the ready story is named - run: built the three-story fixture (ready todo A, blocked C, todo B blocked_by C) and ran `cycle.sh status docs/specs/lr31w --json | jq -c .wave_stories`, then removed the `[ "$blockers_done" -eq 1 ] &&` guard at cycle.sh:347 and re-ran saw: post-fix `[{... lr31w-01 ...}]` (only A); filter removed -> `[lr31w-01, lr31w-03]`, so the LR31 assertion (`== [A]`) fails on pre-fix code; with C flipped to done the fixed script lists both. tests/council.test.sh:1417-1465 asserts exactly that pair, and probe_lr31wall is the only name in `PIN_MUST_FAIL` for the eb3203a replay (L2559)
ASK 3: GREEN - a non-ok branch probe and an unavailable pinned source both set fail=1; council job clones full history - run: extracted `pin_cycle`/`check_probe_result` verbatim into a scratch repo and called them saw: `pin_cycle deadbee1` -> "  [deadbee1] unavailable: fatal: invalid object name 'deadbee1'." with rc=1 and fail=1 (caller then skips that block); `check_probe_result branch probe_x FAIL` -> `::error::` + fail=1; `... branch probe_x ok` -> fail=0; pinned FAIL with empty PIN_MUST_FAIL stays print-only. All four pinned replays (3e200bb, b9f36e8, eb3203a, $PRESHA11) go through pin_cycle; .github/workflows/ci.yml:112-115 gives the `council` job `fetch-depth: 0`
ASK 4: GREEN - release is not pause-gated, exits 0 on a paused spec; ADR-001 lists it; suite fixes it - run: `cycle.sh claim docs/specs/rel1 STAMP1`, `cycle.sh pause`, then `cycle.sh release docs/specs/rel1 STAMP1`; then DRIVER rewritten to `stamp=cccc3333` and released with dddd4444 saw: release under PAUSE -> "released", `{"ok":true,...,"exit":0}`; foreign stamp under PAUSE -> `{"ok":false,"exit":2,"error":"held by cccc3333"}` with DRIVER kept; `claim` under PAUSE still exit 3 next:paused. cycle.sh:2065 carries the "No pause_guard (C4/ADR-001 D2)" comment and cmd_release has no pause_guard; docs/adr/001-cycle-state-contract.md:161 reads "`pause`, `resume`, `release` are exempt"; tests/council.test.sh:2610-2624 is the scenario, plus probe_pauserelease in the wall probes
ASK 5: GREEN - three distinct dispatch-failure reasons for workers, seats and reviewer, with agent and cap, tested - run: `node` over the extracted CAPS + reasonFor from .claude/workflows/vulyk-cycle.js:26-42 saw: `{threw:'boom'}` -> "worker threw: boom"; `''`/`'   '`/null/undefined -> "worker returned empty - turn cap suspected (worker-code, maxTurns 90 in .claude/agents/worker-code.md)"; non-empty string -> null (handed to the verb, whose exit 4 becomes "<who> returned no report"); same shape with "seat sonnet"/council-sonnet 60 and "reviewer"/lead-review 60. Call sites: worker at L186, every seat and the reviewer at L241. CAPS matches the maxTurns of every agent file I checked (worker-code/test 90, council-* 60, lead-review 60, cycle-clerk 5, drone-scout 15). tests/driver.test.sh covers all three per role: threw (405, 522, 613, 638), empty (534, 581, 624, 648), no report (554, 582, 664, 684)
ASK 6: GREEN - record-seat --file works beside stdin, seats write the driver-given path outside docs/specs, driver falls back to the heredoc - run: scratch repo, `cycle.sh open-round docs/specs/rf2`, then `record-seat ... 1 haiku --file .vulyk/reports/rf2/round-1/haiku.attempt-1.md` and the same body on stdin as `sonnet`; also a non-existent path saw: both `{"ok":true,...,"exit":0}`, and `diff` of the two recorded bodies -> "BODIES IDENTICAL"; missing path -> `{"ok":false,"exit":2,"error":"file: .vulyk/reports/rf1/round-1/nope.md"}` with no attempt file left behind (empty file behaves the same per cycle.sh:1368-1375). Driver: reportPath() = `.vulyk/reports/<slug>/round-<N>/<seat>.attempt-<K>.md` (L93, `.vulyk/` is gitignored at .gitignore:33, outside docs/specs), writeReportNote appended to every seat and single-reviewer prompt (L95/L123 - my own dispatch carried it), record() tries --file then falls back to the heredoc only on `exit 2` + `error` starting `file: ` (L225-231); the Tier 4 folded review goes straight to the heredoc. Suite: council.test.sh:971-999 (--file body byte-identical to the stdin twin, missing and empty paths) and driver.test.sh:710-757 (one --file call, no heredoc; fallback on `file: ` exit 2; attempt-2 path)
UNASKED: the driver's CAPS table (vulyk-cycle.js:26) hand-copies maxTurns out of .claude/agents/*.md and nothing enforces the copy - driver.test.sh hard-codes "maxTurns 90"/"maxTurns 60" on both sides, so raising a cap in an agent file and forgetting CAPS keeps the suite green while the turn-cap error prints a stale number, which is the one number this ask exists to make actionable; a generated table or a test that reads the agent frontmatter would close it (I verified today's values agree). Smaller: reasonFor classifies any non-string, non-null return (e.g. `{}`) as "returned no report" without consulting a verb - untested, harmless today; and `--file ${reportPath(...)}` is interpolated unquoted into the clerk's command line, so a slug containing a space would split the argument (no such slug exists).
BREACH: none
