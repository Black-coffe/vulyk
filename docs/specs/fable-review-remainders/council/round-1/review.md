<!-- seat: review · model: unknown · round: 1 · head: a2178bb · pack: 74d6ccf14ab4 · attempt: 1 · recorded: 2026-09-14T14:20:05Z · verdict: BLOCK -->
VERDICT: BLOCK

Scope: every touched file is named by a story or is Queen/hook paperwork (plan.md, journal.md, council/round-1/ROUND, recon/06-attempt-2-kept.patch, memory/learnings, memory/stats). No Law 3 violation. The `pause_guard` header comment at scripts/cycle.sh:112 sits inside a named file.

Verification claims spot-checked: story 01's hand smoke reproduced in mktemp dirs (anchor and EOF targets both cmp-identical to the shipped CLAUDE.md Profile block, D4.7 Browser MCP row grep passes on both, empty stderr); story 05's `[branch]`/`PIN_MUST_FAIL`/`pin_cycle` machinery read and consistent with the recorded scratch-copy runs; story 06's pre-story red list is honest (v3 recorded as "throws", not forced). No suite re-run.

## Critical

1. `.claude/workflows/vulyk-cycle.js:30-33` (`MARKER`, `reasonFor`) and `:184-193` - **plan** - the Workflow driver now decides whether `close-story` runs by grepping the worker's chat prose for `/^STATUS:/m`, which contradicts the recorded invariant in `docs/adr/006-worker-status-channel.md:135` ("Neither driver reads a worker report to make a decision; the report's `STATUS:` line is for the log and the Queen's context only"), `:17-18` ("the driver must not parse prose, so it cannot grep the report for `STATUS:`") and autonomous-cycle C11 ("never parses prose", `docs/adr/001-cycle-state-contract.md:145`); neither ADR is amended, the plan lists ADR-006 as governing, and the delta of 2026-09-14 cut story 07 without checking it. Consequence: a worker that did the work and wrote `returned: DONE` but whose final message lacks a line-anchored `STATUS:` (bold, indented, or the line in an earlier turn) is now a miss that skips `close-story` - the disk truth ADR-006 built is ignored - and after two such returns the story is `blocked` and the on-disk work re-done or discarded; before this diff `close-story` closed it from `returned:`. The `tests/driver.test.sh:190-199` scenario ADR-006 names as "the assertion that the driver did not read the prose" had `STATUS: DONE` prepended to its fixture and no longer asserts that. Condition: the third reason must be reachable without the Workflow driver deciding on a worker report's prose (for example, derived from `close-story`'s own exit 4 `returned: missing` - the channel ADR-006 built for exactly a worker that returned text without finishing), or ADR-006's invariant and C11 must be amended on the record by the owner in the same story that changes `.claude/agents/worker-code.md:18`, `worker-test.md:21` and the fallback loop's "never for this decision" sentence.

## Major

2. `.claude/commands/vulyk-build.md:68-88` (`build:<wave>` row) - **plan** - the fallback driver files `worker returned no report` under `close-story` exit 4 ("the existing classification, unchanged") and keeps "reading `STATUS:` by eye is allowed only for the one-line log, never for this decision", while the Workflow driver skips `close-story` entirely on a missing `STATUS:`; the two drivers now apply different bounds to the same return (the R6 rule of the autonomous-cycle plan: the two drivers share a bound). Story 04 was written before the delta and the delta did not reopen it. Condition: both drivers must classify a non-empty return without a report marker identically, and the fallback row must say whether `close-story` runs on it.

3. `.claude/agents/worker-code.md:18`, `.claude/agents/worker-test.md:21` - **plan** - the sentence "`close-story` and the driver read this key, never your report's prose, to decide whether the story closes" is false after story 07, and the worker is not told that a line-anchored `STATUS:` in its final message is now load-bearing (story 07's `## Files` names neither file). Condition: the worker contract must state what the Workflow driver reads from the return and how it must be formatted, or the driver must stop depending on it (see 1).

## Minor

4. `.claude/workflows/vulyk-cycle.js:221` - **plan** - plan C2 specifies the good-case call as `record-seat ... --model <id> --stamp <stamp> --file <path>`; the driver sends no `--model` (story 03 notes explain the driver has no model id for a seat), and no `## Plan deltas` entry records the contract change. Condition: C2's `--model <id>` must either be struck from the contract on the record or supplied by the driver.

5. `.claude/workflows/vulyk-cycle.js:219-226` (`record`) - **plan** - the `--file` call is made regardless of what the dispatch returned, so a `.vulyk/reports/<slug>/round-<N>/<seat>.attempt-<K>.md` left by an earlier run of the same round number (a relaunch after a stop at the same HEAD) is recorded as this dispatch's report even when the line just logged says the seat threw or returned empty. Minor because it needs a relaunch within one round with a leftover file; C2 says nothing about clearing. Condition: a relaunched round must not record a report file this dispatch did not write, or the plan must record that a leftover attempt file from the same round is an accepted report.

6. `tests/council.test.sh:2440-2444`, `:2559-2563` - **worker** - when `pin_cycle` fails, `$OLDCYCLE2` / `$OLDCYCLE3` mktemp files are never removed (the `rm -f` sits inside the success branch only; the `$OLDCYCLE4` site gets it right). Condition: every pinned temp copy is removed on both branches.

7. `tests/driver.test.sh` scenario (z) (`C2 prompt`) - **worker** - asserts the re-ask prompt names `attempt-2` but not that the second `record-seat` call carries `--file .vulyk/reports/demo/round-1/sonnet.attempt-2.md`; C2's "attempt 2 gets the `attempt-2` path" is proven for the prompt only. Condition: one assertion that the record call after an exit 4 re-ask names the attempt-2 path.

8. `install.sh:220-227` - **plan** (pre-existing, outside ask 1) - observed in the smoke: when both the Profile and the Commands blocks are absent, Profile takes the EOF path and lands after `## Compact instructions` while Commands lands before it (Profile at :164, Commands at :115 in the target). Byte identity holds; ordering on the double-missing target is a separate defect not in this brief. Condition: none for this round - recorded so the next circle knows.

9. `docs/specs/fable-review-remainders/plan.md:15` (`## Assumptions`, the seat's cwd) - **plan** - the first live round is this one; at the time of this review `.vulyk/reports/` existed neither at the hive root nor under `.vulyk/court/*/`, so the assumption "a repo-relative report path written by the seat is the path the clerk reads" is still unverified. Condition: the driver log of this round must show one `record-seat --file` call per seat with no `file:` fallback before the assumption is treated as true.
