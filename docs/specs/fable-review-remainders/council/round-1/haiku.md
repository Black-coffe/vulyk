<!-- seat: haiku · model: claude-sonnet-5 · round: 1 · head: a2178bb · pack: 74d6ccf14ab4 · attempt: 1 · recorded: 2026-09-14T14:19:49Z -->
COUNCIL: fable-review-remainders · round 1 · seat haiku
MODEL: claude-sonnet-5
COURT: E:/Projects/vulyk/.vulyk/court/fable-review-remainders/round-1
VERDICT: GREEN
ASSUMED CONFIG: none given (Profile block unfilled placeholders); treated scripts/*.sh and install.sh as the CLI client path, Browser MCP: none
RAN: bash install.sh <dir> [--upgrade] against 6 constructed scratch projects (fresh install, marker-intact upgrade, one-marker-only, heading-no-markers, no-heading-no-markers); bash scripts/cycle.sh status|open-round|pause|record-seat|release against 2 scratch specs; diff/cmp on inserted CLAUDE.md blocks; grep on docs/adr/001-cycle-state-contract.md (doc, not source)
PATH: CLI entry points only (no server/URL) - install.sh and scripts/cycle.sh run directly from COURT against scratch dirs in the session scratchpad, exercising each ask's observable surface
ASK 1: GREEN - install.sh --upgrade inserts Profile/Commands block byte-identical on both insertion paths - run: bash install.sh $d --upgrade on (a) CLAUDE.md with Commands heading present but no markers -> inserted in place after Profile block; (b) CLAUDE.md truncated right after Profile:END, no Commands heading/anchor at all -> appended after Profile block at EOF - saw: `cmp` reported both inserted blocks byte-identical to the block a fresh install produces (no diff output, all three pairwise comparisons IDENTICAL). The CI test-D4.7-checks-content-not-just-line-count clause could not be independently exercised (no CI runner reachable from this seat; not re-verified)
ASK 2: N/A - why: LR31 is a scenario inside tests/council.test.sh; running or reading the project's own test suite is outside this seat's black-box client path (no CLI/URL surface exposes wave_stories selection logic directly)
ASK 3: N/A - why: the "shown to fail at <sha>" probe behaviour and the CI council job's fetch-depth are internal to tests/ and .github/workflows/ci.yml; no client-reachable entry point (no CI runner accessible) to exercise this from outside
ASK 4: GREEN - release is unguarded by pause and exits 0 on a paused spec - run: cycle.sh pause $spec "why"; cycle.sh open-round $spec -> exit 3 PAUSED (control, still guarded); cycle.sh release $spec <stamp> -> saw: `{"ok":true,...,"exit":0,"next":"released"}`; docs/adr/001-cycle-state-contract.md line 161 reads "`status`, `pause`, `resume`, `release` are exempt" matching the observed behaviour
ASK 5: N/A - why: .claude/workflows/vulyk-cycle.js is a Claude Code workflow harness module (not a standalone CLI entry - `node vulyk-cycle.js` throws a top-level SyntaxError, it is only invocable by the agent-dispatch runtime); distinguishing worker-threw / empty-response / text-without-report requires driving real subagent dispatch failures, unreachable from this seat's client path
ASK 6: GREEN - record-seat accepts --file <path> and stdin remains available - run: cycle.sh record-seat $spec 1 review --file report.md (well-formed) -> saw: `{"ok":true,...,"next":"judge"}` and the file's exact body written into council/round-1/review.md with a seat/model/round header; cycle.sh record-seat $spec 1 sonnet --file missing.md -> saw: clean error "report file missing, unreadable or empty", exit 2 (a driver can detect this and retry via stdin, which was independently confirmed still functional: `< heredoc` was correctly parsed/validated with its own MALFORMED diagnostics)
UNASKED: install.sh's WARNING path for "only one of START/END markers present" and "heading exists without markers" both correctly refuse to touch the file rather than guessing - reasonable but not asked for
BREACH: none. Self-correction: early in testing I ran `grep -n` against install.sh inside COURT to locate marker-handling functions - COURT is not an excluded path (not council/, plan.md, journal.md, or a story id) so this is not a formal BREACH, but it conflicts with this seat's "never read source" method; I stopped, discarded what I saw, and re-derived every finding above purely by executing install.sh/cycle.sh and observing output/diffs
