VERDICT: PASS

Reviewed `vulyk/v0-12-0-remainders` at `c71ad5f` against base `bb5537e` (44 files, +3004/-233), all 17 stories, `plan.md`, `brief.md`, ADR-001/004/005/006. Ran the full integration gate locally (`bash -n`, `py_compile`, `jq`, `handoff.sh status`, `tests/cycle.test.sh`, `tests/driver.test.sh`, `tests/council.test.sh`): every step exit 0, zero `::error::`, every `[branch]` probe `ok`. Two things below were reproduced by hand in `mktemp -d` scratch repos, not inferred. No critical finding: every ask is delivered in code; the majors are a corrupted table row on the installer's main insert path, two verification claims that do not hold as stated, and one contract sentence that is false.

## Major

1. `E:\Projects\vulyk\install.sh:221` — **plan** — Reproduced: on the anchor-insert path (the common case, `## Commands` present in the target) the block goes through `awk -v block=` a second time, so the Browser MCP row's `\|` becomes `|` (row gains two spurious cells) and gawk prints `warning: escape sequence '\|' treated as plain '|'` to the owner; the EOF path (`printf '%s\n'`) is byte-correct. Condition: an inserted Profile/Commands block must be byte-identical to the source section on both insertion paths, and D4.7 must assert row *content*, not only row count (the story's own K7/D3 silently assumed `awk -v` round-trips arbitrary text; `print_profile_placeholder` at `install.sh:161` already double-escapes for one pass, not two).

2. `E:\Projects\vulyk\tests\council.test.sh:1385` (`lr31w`) and `:2384` (`probe_lr31wall`) — **plan** — The LR31 scenario has one `todo` story whose blocker is `blocked` and no ready story in the wave, so the pre-fix loop never reaches the `WAVE_STORIES=$wave_files` branch and yields `[]` either way; story 16's notes admit `probe_lr31wall` is `ok` at `eb3203a`. The code fix at `scripts/cycle.sh:345-360` is right on read, but no test in the suite would go red if it were reverted. Condition: the LR31 scenario must put a ready story and a not-ready `todo` in the same wave and assert `wave_stories` names only the ready one (the criterion text in stories 08/16 prescribed the non-triggering case).

3. `E:\Projects\vulyk\tests\council.test.sh:2027,2115-2124,2355,2458,2687` — **plan** — The "shown to fail at `<sha>`" regression proof for stories 14-17 is print-only: each probe runs as `$(probe_x)` inside an `echo`, so neither a `[<sha>] ok` nor a `[branch] FAIL` ever sets `fail=1`; and `git -C "$SRC" show 3e200bb:scripts/cycle.sh` has no error handling, so under CI's `actions/checkout@v4` default `fetch-depth: 1` (`.github/workflows/ci.yml:113-116`) the pinned copy is an empty file and every `[<sha>]` line prints `FAIL` for the wrong reason while the job stays green. Condition: a `[branch]` probe result other than `ok` must fail the suite, and the pinned-version probes must either fail the suite when the pinned source cannot be produced or be skipped with a printed reason (CI needs `fetch-depth: 0` or the probes need a guard). The four stories' `## Implementation notes` record hand-observed labels, which is exactly what their criteria asked for, but nothing on disk enforces them.

4. `E:\Projects\vulyk\scripts\cycle.sh:2047` vs `E:\Projects\vulyk\.claude\commands\vulyk-build.md:84` and story 13 non-goal `:29` — **plan** — Reproduced: `release` is `pause_guard`ed, so on a paused spec it answers `{"ok":false,"verb":"release","exit":3,"next":"paused",...}`, while `/vulyk-build` step 3 and story 13 state it is "harmless, exit 0" there. K3 lists PAUSE only on `claim`; story 11 and story 13 were told different things. The Workflow driver is unaffected (`vulyk-cycle.js:229` swallows it); the fallback loop is told to expect exit 0 and would print a spurious `paused:` error on every paused terminal. Condition: `release` after `pause` must either be exempt from the PAUSE guard (it removes nothing that `pause` did not already remove) or the two command-file sentences must say exit 3 — one of the two, recorded in K3.

## Minor

5. `E:\Projects\vulyk\scripts\cycle.sh:99` shadows `E:\Projects\vulyk\scripts\lib.sh:66` — **plan** — Two `marker()` definitions with different semantics (last vs. first match) now coexist; any lib.sh consumer reading `**Council:**` would get the oldest round. No script outside `cycle.sh` reads that marker today, so this is dormant. Story 01's worker flagged it rather than escalating because `lib.sh` was not in its file list. Condition: one `marker()` with the last-match rule, in `lib.sh`, and the `cycle.sh` copy removed.

6. `E:\Projects\vulyk\install.sh:538-541,553-556` — **worker** — `ensure_marked_block` and `report_fill_status` run in the "already a constitution" branch unconditionally, so a plain re-install (no `--upgrade`) into an existing hive also inserts blocks and prints fill status; ADR-005 D3 and K7 scope both to `--upgrade`. Condition: the block pass and the fill report run only when `$UPGRADE` is set, or ADR-005 D3 is amended to say "any run against an existing constitution".

7. `E:\Projects\vulyk\.claude\agents\cycle-clerk.md:14-16`, `worker-code.md:6`, `worker-test.md:6` — **plan** — Queen hand-edits recorded in `## Plan deltas` (2026-09-13); `worker-*.md` are files story 09 names, so this is a recorded Law 5 deviation, not a hidden one. Condition: none beyond the record that exists; noting for the ledger.

8. `E:\Projects\vulyk\docs\specs\v0-12-0-remainders\plan.md:44-65` — **plan** — The `## Stories` index still shows 05, 08, 11, 12, 13, 15, 16, 17 as `todo`; all eight are `done` in their frontmatter and committed. Condition: the index reflects frontmatter status at review time.

9. `E:\Projects\vulyk\docs\specs\v0-12-0-remainders\plan.md:115` (`## Descoped` empty) — **plan** — Three narrowings live only in worker notes: story 14 dropped the plan-side `round 10` prose-anchor assertion of its LR21 criterion (`:879`); story 15's r2m7 criterion (a court `pre-commit` hook exiting 1) is unreachable by construction since the commit runs `--no-verify`, so the worker used a different trigger (`:951`); story 16 recorded LR31 as "observed ok pre-story" (`:1026-1035`). Condition: each shrunk requirement gets a `## Descoped` or `## Plan deltas` line with the authorizing quote, or the criterion is restored.

10. `E:\Projects\vulyk\.claude\commands\vulyk-build.md:27-32` — **plan** — Step 1 now claims for both branches and the Workflow driver claims again with the same stamp (idempotent, harmless) and releases in its `finally`; but if the `Workflow` tool refuses to launch the script (the CRLF refusal the deltas record), nothing releases and the next launch with a fresh stamp gets `held by`, and step 4's `stop.verb === 'claim'` text does not cover a launch that never produced a stop. Condition: the Workflow branch releases (or does not claim in step 1) when the tool call itself fails before the script runs.

11. `E:\Projects\vulyk\install.sh:471-478` — **plan** — Removal trusts every old-manifest line for `rm -f "$DEST/$f"` after `owned()`, and `owned()` is a prefix match, so `.claude/agents/../../x` is "owned" and deletes outside the hive. Only reachable through a tampered committed manifest, so severity is minor. Condition: a manifest line containing a `..` segment or a leading `/` is never a removal candidate.

12. `E:\Projects\vulyk\install.sh:481-491` — **plan** — The no-manifest `unlisted (kept)` walk lists every non-shippable file under OWNED trees, including `.claude/hooks/__pycache__/*.pyc`, on a first upgrade — noise that K6 as written asks for. Condition: files `shippable()` returns 1 or 2 for are excluded from the `unlisted (kept)` report.

13. `E:\Projects\vulyk\scripts\cycle.sh:1965,1985` — **plan** — `pause`/`resume` journal `driver released` under a new stage word `driver`, outside the vocabulary `journal.sh:12-13` names; `journal.sh` does not validate, so it works. Condition: the stage word is named in K3 or the journal vocabulary note, so the next spec's LR22/r2m18 journal-vocabulary work does not trip on it.

## Checked and clean

Scope: every code file touched is named by a story except the three Queen hand-edits above (recorded) and hook-written `memory/` files. Security: the `## Commands` whitelist is still byte-for-byte per cell before `bash -c` (`cycle.sh:1509`); `claim`/`release` json-escape the stamp; `driver_guard` runs after `pause_guard` and before any write on all four verbs (verified by reading each call site). `returned:` gate on a template-shaped story returns `returned: missing` (reproduced). `tier` in `status --json` is a bare number, so the driver's `=== 4` guard is real. `docs/wiki/` holds only `.gitkeep`. ADR-001's exit-code line and D2 paragraph match K1/K2 after story 03; nothing in ADR-004/005/006 is contradicted except item 6.