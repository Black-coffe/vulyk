# Autonomous cycle: the council replaces the human gate (plan)

**Tier:** 4 · **Spec slug:** `autonomous-cycle` · **Brief:** [brief.md](brief.md)
**Governed by:** ADR-001 [docs/adr/001-cycle-state-contract.md](../../adr/001-cycle-state-contract.md) (proposed - the mechanics; wins over the grill record where they disagree) · design record [docs/grill/2026-09-12-autonomous-cycle-council.md](../../grill/2026-09-12-autonomous-cycle-council.md) (Decision log 1-14 + Act 2 amendments) · CLAUDE.md `## The cycle`, `## Token economy`, `## Commands`
**Depends on:** v0.11.0 six-stage cycle (`2a35d15`, 2026-09-05): `scripts/ship-check.sh`, `human-check.sh`, `acceptance-log.sh`, `state.sh`, `tests/cycle.test.sh`, `.github/workflows/ci.yml` job `cycle`

**Tier 4, because** the change is the architecture of the cycle itself: a new state contract (ADR-001) with four new committed files and one new ledger; a new script that every driver and every command routes through; three agents replacing one plus a clerk; a Workflow driver that must behave identically to a session fallback; every one of the four cycle commands rewritten and two added; the installer, the constitution and eight docs. It touches every layer VULYK has, and the recon (`recon/*.md`) shows the existing gates are coupled to the exact markers being replaced. Second reviewer per the Tier 4 rule: `opus` beside a Fable gate.

## Goal

Replace stage 05 (the mandatory owner look) and the single `drone-acceptance` with a three-seat blind council (Haiku black-box, Sonnet ask-by-ask, Opus intent) that judges only the owner's own words (`brief.md` `## Asks`), runs in a reduced git worktree so it cannot see the hive's stories, and whose round verdict is computed by a model-free script (`scripts/cycle.sh judge`) from labelled, evidenced seat reports. Move the human to one stop at the start (a one-round grill inside `/vulyk-plan` that ends with `## Asks` and `**Briefed:**`), and move the loop build -> gates -> council -> repair out of the Queen's session into a Workflow script with a session fallback, both driven by the same `cycle.sh status --json` / `next` contract. Ceiling three rounds, early escalation at half the asks RED, `PAUSE` semaphore for a human who wants in, journal on disk mirrored to the terminal, `council.jsonl` for the rollback metric, local merge in `/vulyk-ship` with publish printed and never pressed. Ship as v0.12.0.

## Assumptions

Conflicts resolved (ADR wins on mechanics, owner wins on intent):
- **Round counter** = number of `docs/specs/<slug>/council/round-*` directories (ADR D1), not rows in `council.jsonl` (grill Act 2, row 7/4). The row is the close marker; the directory is the open marker.
- **`/vulyk-resume`** relaunches the Workflow fresh and never passes `resumeFromRunId` (ADR D2); the grill's "`/vulyk-resume` делает `resumeFromRunId`" (Act 2, PAUSE row) is dropped - a cached `status` replayed after a human touched the tree is the wrong answer.
- **Verdict script** is `scripts/cycle.sh judge` (ADR), not `council-log.sh --verdict` (grill Act 2, row 7). One script, one CLI.
- **PAUSE** is honoured by every mutating verb of `cycle.sh` (ADR D2), not only "between phases and before commits" by the Workflow (Act 2) - the stronger rule contains the weaker.
- **Seat count**: the request text says "два агента Haiku"; the owner's Q6 answer ("3 места, 3 модели, 3 разных угла") and the grill anti-scope ("второй Haiku" not done) supersede it. Stories quote Q6.
- **Who judges**: the request and Q7 say "Королева судит по доказательствам"; Q15 accepts "скрипт судит раунды". Stories implement Q15: the script judges rounds, the Queen only reads escalations and the final report.
- **"Зелёный = единогласие трёх"** (Q7/grill 7) is read as ADR D4: every seat `GREEN` or `N/A` and review `PASS`. `N/A` with a reason is consent, per Act 2 row "6, 14" and Q15 ("N/A").
- ADR D2's Workflow snippet maps the `review` seat to `` agentType: `council-${seat}` ``; there is no `council-review` agent. `## Contracts` fixes it: seat `review` -> `lead-review` at `top_model`.

Design assumptions not decided by any input (veto here):
- **Shared functions move to `scripts/lib.sh`** (`pack_fingerprint`, `paperwork_only`, `marker`) - created by story 01, consumers migrated in story 02. Rationale under `## Tradeoffs`.
- **Driver mode detection** in `/vulyk-build` and `/vulyk-plan`: the `Workflow` tool is present in the session's tool list -> Workflow driver; otherwise fallback. The SessionStart brief can only report the CLI gate (`claude --version` >= 2.1.154); the Pro `/config` flag is not visible to a hook, so the brief says "Workflow: CLI ok - enable in /config on Pro" or "Workflow: unavailable (CLI < 2.1.154)".
- **Black-box is a mandate, not a tool list.** `council-haiku` keeps `Read` (it needs `brief.md` and the Profile) and `Bash` (the Client path); "код не читает" is enforced by its prompt and the `BREACH:` line. A `Bash` seat can `cat` anything; pretending otherwise would be a false guarantee.
- **Tier 1 path**: `/vulyk-plan` detects Tier 1 (or is told `--tier 1`), writes `brief.md` with the task phrase as `## Request` and the single item of `## Asks`, one story, `plan.md` with `**Briefed:** via mini-brief, <owner>, <date>`, and launches the driver - no grill, one council round (Q13).
- **`claude -p` / no-question mode**: `/vulyk-plan` answers its own grill with the recommended option, marks each `## Answers` entry `(assumed)` and writes `**Briefed:** via grill (assumed), <owner>, <date>`.
- **Escaped defect** (Q14): a bug brief whose header carries `**Escaped from:** <slug>` where `<slug>` has a `GREEN` council row. The grill asks the question when the request is a bug report; `/vulyk-evolve` counts the lines. No other tracing mechanism exists or is invented.
- **`reopen` ceiling** is persisted as `docs/specs/<slug>/council/CEILING` (one integer, absent = 3), copied by `open-round` into `ROUND`. The ADR says "+3 in the next ROUND" without naming where the number waits.
- **`in-progress`**: the recon did not say who writes `status: in-progress` today. The driver and `close-story` treat `todo` and `in-progress` alike (dispatch / close), so the answer changes wording, not design - listed under recon questions.
- **ship-check test cases** (READY on Briefed+GREEN, NOT READY on RED, the two `Checked:` overrides, `paperwork_only` accepting council commits) live in `tests/cycle.test.sh` (story 02) rather than `tests/council.test.sh` as ADR "Tests that must exist" lists them - the wave-2 file boundary decides, the tests exist either way and CI runs both.
- **Release paperwork** (story 13) authors the `## [0.12.0]` entry, `VERSION` and `CITATION.cff` as a story so a worker writes it and lead-review reads it; `/vulyk-ship` on this spec finds version and CHANGELOG already current and makes no second bump. `CITATION.cff` is synced by hand this once; no sync script - nobody asked for one.
- `acceptance-log.sh` and `drone-coverage` stay: the former reads pre-0.12 specs (ADR D4 "as today"), the latter checks the plan against `## Asks` (Act 2 row 9). `drone-acceptance.md` is deleted, not kept as an alias.
- `memory/stats/council.jsonl` gets no skeleton (precedent: the four existing series have none); every reader tolerates a missing file.
- `human_gate: blocking | deadline:<h> | off` (grill "rejected/kept as a mode") is **not** in v0.12.0 - the grill marks it optional and no owner answer asks for it.
- Docs follow-up spec: **none needed** - 13 stories fit the budget, all docs are in stories 11-12.
- Three brief blockquotes are process instructions already carried out before planning, not build requirements, and no story quotes them: "прогнать бриф через свежего адверсариального ревьюера" (done - the adversarial record exists), "А потом запустить проверку двух платформенных допущений" (done - `## Evidence` line 3), "Запускай прямо сейчас". `trace-check.sh`'s forward coverage will list them as uncovered; that is expected.
- `.claude/workflows/` was created empty at plan time (2026-09-12, by the Queen) so `wave-check.sh` resolves story 06's path; git does not track the empty directory, story 06 fills it.
- **Coverage findings resolved at the approval stop (2026-09-12, owner: "одобряю с предложенными правками"):** (a) Q3's "список критериев приёмки … как чеклист" is superseded by Act 2 row 9 (`## Asks` is the checklist) and by the court's worktree blindness (plan.md is pruned) - the owner chose Q15 "все технические фиксы"; the court judges `## Asks` only. (b) The three seat mandates of Q6 (Haiku black-box on the Client path; Sonnet suite + one run per ask; Opus intent and edge cases) are acceptance criteria of story 05 and named in C10 below. (c) "грилевание должно быть максимально приятным, простым и понятным" is an acceptance criterion of story 08 (one question per turn, three plain sentences, recommended first with a recon-grounded why, silence is safe, `## Asks` read back). (d) Q10's "SessionStart-бриф ругается, если сессия идёт не на топ-модели" already exists (`top-model-brief.sh:36-38`); story 07 keeps that clause unchanged. (e) The brief's "два агента Haiku" is superseded by the owner's Q6. (f) `scripts/lib.sh` extraction has no brief line behind it and stays as a planner design choice (Tradeoffs) - approved.

## Stories

**Wave 1**
- `autonomous-cycle-01-cycle-tracer` — tracer: `scripts/lib.sh`, `scripts/cycle.sh` (`status --json`, `judge`, `escalate`), `scripts/journal.sh`, `tests/council.test.sh` over hand-written fixtures (GREEN / evidenced RED / ceiling / half / env / na / owner REJECTED), CI job.

**Wave 2**
- `autonomous-cycle-02-gates-learn-council` — `ship-check.sh` reads `Briefed:` and the council row; `human-check.sh` becomes the override; `state.sh` gains `04-council:<v>` and `paused`; four scripts source `lib.sh`; `templates/plan.md` placeholders; `tests/cycle.test.sh` extended and green.
- `autonomous-cycle-03-cycle-intake-and-pause` — `cycle.sh record-seat` (D3 validation, taint, attempts, MALFORMED), `briefed`, `branch`, `pause`/`resume`, PAUSE guard on every mutating verb; tests.
- `autonomous-cycle-05-council-agents` — `council-haiku.md`, `council-sonnet.md`, `council-opus.md`, `cycle-clerk.md`; `drone-acceptance.md` deleted; `drone-coverage.md` judges against `## Asks`.
- `autonomous-cycle-07-install-and-session-brief` — `install.sh`: `.claude/workflows` in `OWNED`, the two `Bash(...)` allow rules, `top-model.sh --apply`, gitignore lines; `.gitignore`; `top-model-brief.sh` reports the Workflow CLI gate.

**Wave 3**
- `autonomous-cycle-04-cycle-rounds-in-git` — `cycle.sh open-round` (court worktree, STALE, ceiling exit 6), `judge` removes the court, `close-story`, `reopen`, `--commit`; tests.
- `autonomous-cycle-06-workflow-driver` — `.claude/workflows/vulyk-cycle.js`: the logic-free loop over `status.next`.
- `autonomous-cycle-08-plan-with-grill` — `/vulyk-plan`: grill after recon per `templates/grill.md` (new), `## Asks`, `cycle.sh briefed`, Tier 1 mini-brief, launches the driver.
- `autonomous-cycle-09-build-review-pause-resume` — `/vulyk-build` (driver + mode detection), `/vulyk-review` (one round, no stage-05 stop), `/vulyk-pause`, `/vulyk-resume` (new).
- `autonomous-cycle-10-ship-status-evolve-bootstrap` — `/vulyk-ship` (council row, local merge, publish printed), `/vulyk-status` (council stats, driver mode, unpushed merges), `/vulyk-evolve` (three weekly numbers), `/vulyk-bootstrap` (`--apply`, council wording).
- `autonomous-cycle-14-install-shippable-runtime-files` — plan delta 2026-09-12: `shippable()` refuses every path VULYK's own `.gitignore` ignores; install-smoke proves it (blocked_by 07)

**Wave 4**
- `autonomous-cycle-11-docs-constitution-and-cycle` — `CLAUDE.md` (cycle table, routing matrix, Profile row `Browser MCP`), `docs/cycle.md`, `docs/pipeline.md`, `bootstrap/interview.md`.
- `autonomous-cycle-12-docs-guides` — `docs/architecture.md`, `docs/command-reference.md`, `docs/getting-started.md`, `docs/model-cascade.md`, `docs/token-economy.md` (honest cost line), `README.md`.
- `autonomous-cycle-15-status-carries-the-worker` — plan delta 2026-09-12 (2): `status --json` `wave_stories` become `{file, story, worker, repeat}` objects; the driver routes `agentType` from `worker` (blocked_by 04, 06)

**Wave 5**
- `autonomous-cycle-13-release-0-12-0` — `CHANGELOG.md` `## [0.12.0]`, `VERSION`, `CITATION.cff` sync, `memory/memory.md` pointer.
- `autonomous-cycle-16-install-profile-rows` — plan delta 2026-09-12 (3): the installer's Profile placeholder carries every row of the constitution's table; install-smoke asserts the row count (blocked_by 11)

**Wave 6**
- `autonomous-cycle-17-stale-means-code-moved` — plan delta 2026-09-13 (4): one `round_is_stale` helper on `paperwork_only`; the cycle's own commits never stale a round (blocked_by 15)
**Wave 7**
- `autonomous-cycle-18-council-scales-with-tier` — plan delta 2026-09-13 (5), ask 13: required seats by tier (C15) - Tier 1 one Sonnet seat, Tier 2 sonnet+opus+review, Tier 3-4 the full court; routing matrix states the agent cost per tier (blocked_by 17)

**Wave 8** — repair after council round 1 (plan delta 6, 2026-09-13); `scripts/cycle.sh` + `tests/council.test.sh` are sequenced through waves 8-10, everything else runs beside wave 8
- `autonomous-cycle-19-status-after-committed-verbs` — R1, R2, R3, R7, R12 (`tier` key), R21 (script side), R25: every `status --json` derivation goes through `round_is_stale`, an exhausted seat leaves `missing`, `reopen` writes `council/REOPEN`, no default tier, `round_dir` names the open round; the test drives the real verbs with `--commit` and asserts `status` after each.
- `autonomous-cycle-22-driver-acts-on-exit-codes` — R5, R6, R9 (no `round_dir` to seats), R11 (per-call delimiter), R12 (second reviewer), R14 (API verified): `.claude/workflows/vulyk-cycle.js`.
- `autonomous-cycle-23-commands-follow-the-driver` — R5, R6, R9, R11, R19, R20: `/vulyk-build` fallback and wake-up, `/vulyk-review` dispatches `missing`, `/vulyk-pause` wording.
- `autonomous-cycle-24-court-is-an-honour-clause` — R3 (`lead-review.md` VERDICT line), R15, R19, R26: three seat prompts, `lead-review.md`, ADR-001 D1/D2/D4/D5/D6, `docs/command-reference.md`, one sentence of `CLAUDE.md`.
- `autonomous-cycle-25-release-notes-tell-the-truth` — R13, R21 (template), R22: `CHANGELOG.md` 0.12.0 rewritten to the council that ships, `templates/plan.md` Tier `<1|2|3|4>`, install-smoke asserts the two allow rules.

**Wave 9**
- `autonomous-cycle-20-seat-parsing-and-verdict-rows` — R8, R9, R10, R16, R24: one evidence rule for `record-seat` and `judge`, path-anchored taint, `half` floor of two, ABSENT-without-RED → `env`, RED exits 0 (blocked_by 19).

**Wave 10**
- `autonomous-cycle-21-verbs-own-their-git` — R4, R5 (script side), R11 (`close-story` gating), R15 (reduction commit), R17, R18, R23: `scope.jsonl` committed and whitelisted, `open-round` records the ceiling, `escalate` stands alone, git failures surface, verification lines come from `## Commands` and each runs alone, anchored whitelist in `lib.sh` (blocked_by 20).

**Wave 11** — repair after council round 2 (plan delta 7, 2026-09-13); four stories, `## Files` disjoint, all beside each other
- `autonomous-cycle-26-driver-fails-closed` — R28, R29, R30, R31 (`args.stamp` guard), R32: `.claude/workflows/vulyk-cycle.js` — two reviewers fold only when both open with `VERDICT:`, a blank reaches `record-seat` as a failing report; an empty worker report is a miss under the two-attempt map; one repair per round, a second `repair` for the same round ends the run; no `clerk()` inside a `pipeline` stage.
- `autonomous-cycle-27-emit-escapes-verdict-is-line-one` — R33, R28 (script side, N-m4 folded), R30 (`status` `review` key): `scripts/cycle.sh`, `tests/council.test.sh` — `emit` escapes every value, `record-seat … review` reads line 1 only, `status --json` names the newest row's review verdict.
- `autonomous-cycle-28-commands-random-stamp-bounded-repair` — R31, R29, R30 (fallback side): `/vulyk-build`, `/vulyk-review` — stamp from `/dev/urandom`, honest delimiter wording, an empty worker is a miss, one repair per round, the wake-up step reads the new stops.
- `autonomous-cycle-29-paperwork-about-the-driver` — R34, R35 (+ lead-review r2 minors 12 and 13, N-m6 folded): ADR-001 D2 sketch removed, D1 lists every file, `## Consequences` matches D5; story 22's R14 bullet replaced by a real fetch record.

## Contracts

**C1. Script conventions.** Every script runs with cwd = hive root and locates siblings via `$(dirname "$0")` (`cycle.sh` calls `"$(dirname "$0")/journal.sh"`, `scope-check.sh`, sources `lib.sh` with a `# shellcheck source=scripts/lib.sh` directive). `scripts/lib.sh` exports exactly: `pack_fingerprint <spec-dir>` (verbatim from `ship-check.sh:21-37`), `paperwork_only <root> <from> <to>` (verbatim from `ship-check.sh:42-56` with the whitelist extended to `*/plan.md`, `*/journal.md`, `*/council/*`, `memory/stats/human.jsonl`, `memory/stats/acceptance.jsonl`, `memory/stats/ship.jsonl`, `memory/stats/council.jsonl`), `marker <plan.md> <Name>` (from `ship-check.sh:59-64`), `now_ts` (`date -u +%Y-%m-%dT%H:%M:%SZ`), `slug_of <spec-dir>` (basename). Ledger paths are repo-relative from cwd, as today.

**C2. `scripts/cycle.sh <verb> <spec-dir> [...]`.** Verbs, preconditions and effects exactly as ADR-001 D2: `status [--json]` · `briefed [--commit]` · `branch [--commit]` · `close-story <story-file> [--commit]` · `open-round [--commit]` · `record-seat <spec> <N> <haiku|sonnet|opus|review> [--model <id>] < report` · `judge [--commit]` · `escalate [--commit]` · `reopen <spec> "<decision>" [--commit]` · `pause [why]` · `resume`. Exit codes: 0 ok · 1 usage · 2 precondition (stderr names it) · 3 paused · 4 malformed / red verification · 5 stale · 6 escalate. Every mutating verb checks `<spec>/PAUSE` first (`status`, `pause`, `resume` exempt). **The last stdout line of every verb, on every exit code, is one JSON object** `{"ok":true|false,"verb":"<verb>","exit":<n>,"next":"<next>"[,"error":"<one line>"]}`; `status --json` prints only its object. `--commit` commits the paperwork it wrote as `vulyk(<slug>): <verb> <detail>`.

**C3. `status --json` object.** `{"spec":"docs/specs/<slug>","slug":"<slug>","stage":"<state.sh stage>","next":"<next>","briefed":bool,"approved":bool,"branch":"vulyk/<slug>|null","head":"<sha7>","pack":"<fp12>","stories":{"todo":n,"in-progress":n,"done":n,"blocked":n},"wave":n|null,"wave_stories":["docs/specs/<slug>/<file>.md",...],"round":n,"ceiling":n,"open":bool,"court":"<abs path>|null","missing":["haiku","sonnet","opus","review"],"stale":bool,"verdict":"GREEN|RED|ESCALATE|STALE|null","red":[n,...],"round_dir":"docs/specs/<slug>/council/round-N|null","paused":bool,"shipped":bool}`. `next` is derived first-match: `shipped` (Shipped marker) · `paused` (PAUSE) · `briefed` (neither Briefed nor Approved) · `branch` (no Branch) · `build:<wave>` (lowest wave with a `todo`/`in-progress` story whose blockers are done; `wave_stories` lists them) · `close-story:<file>` (an `in-progress` story whose wave has no `todo` left) · `dispatch:<missing>` (open round, `head` in ROUND == HEAD, seats missing) · `judge` (open round, nothing missing) · `escalated` (newest row ESCALATE) · `green` (newest row GREEN, `pack` current, `head` == HEAD or paperwork only since) · `repair` (newest row RED and no commit since) · `open-round` (no row, or newest row STALE/RED with commits since, or GREEN gone stale).

**C4. Files.** `docs/specs/<slug>/council/round-N/ROUND`: five `key=value` lines `head=`, `pack=`, `opened=`, `court=`, `ceiling=`. Seat file `council/round-N/<seat>.md`: first line `<!-- seat: <seat> · model: <id|alias|unknown> · round: <N> · head: <sha7> · pack: <fp12> · attempt: <k> · recorded: <ts> -->`, then the report verbatim; a rejected attempt is `<seat>.attempt-K.md`; a seat is ABSENT when `<seat>.md` is missing and `<seat>.attempt-2.md` exists. `council/CEILING`: one integer (absent = 3). `docs/specs/<slug>/PAUSE`: first line `<who> · <why> · <ts>`; gitignored. Court: `.vulyk/court/<slug>/round-N/` (gitignored), a detached worktree at `head` with `docs/specs/<slug>/` reduced to `brief.md`. `memory/stats/council.jsonl` row, flat, keys in this order: `ts, spec, round, verdict, head, pack, asks, red, red_unevidenced, na, review, haiku, haiku_model, sonnet, sonnet_model, opus, opus_model, attempts, escalate, note` (values per ADR D1; `escalate` is `"ceiling"|"half"|"env"|null`; `note` redacted through `scripts/redact.sh`).

**C5. Seat report** (ADR D3, verbatim labels, 40 lines max): `COUNCIL: <slug> · round <N> · seat <haiku|sonnet|opus>` / `MODEL:` / `COURT:` / `VERDICT: GREEN | RED | N/A` / `ASSUMED CONFIG:` / `RAN:` / `PATH:` / one `ASK <n>: GREEN | RED | N/A - <ask, short> - run: <cmd> saw: <output> | url: <where> saw: <what> | why: <reason>` per item of `## Asks` / `UNASKED:` / `BREACH:`. `record-seat` rejects (exit 4 `MALFORMED`) a missing label, an uncovered or extra ask number, `GREEN`/`RED` without `run:`+`saw:` or `url:`+`saw:`, `N/A` without `why:`, `VERDICT` inconsistent with the ASK lines, or a taint (`<slug>-NN`, `plan.md`, `journal.md`, `council/` anywhere in the body). For seat `review` it stores the whole report and extracts only `PASS`/`BLOCK` (first line matching `^(PASS|BLOCK)\b` or `^VERDICT: (PASS|BLOCK)`).

**C6. Verdict** (ADR D4, first match wins): PAUSE -> exit 3 · `**Checked:** REJECTED` newer than `ROUND.opened` -> RED, `repair` · three seats ABSENT -> ESCALATE `env` · |RED_e| >= ceil(A/2) -> ESCALATE `half` · review BLOCK or RED_e ∪ RED_u non-empty -> RED, and ESCALATE `ceiling` when N >= C · all seats GREEN/N/A and review PASS -> GREEN · all seats N/A and PASS -> GREEN with `na:3`. On attempt 2 an unevidenced RED stays RED but lands in `red_unevidenced` (not counted for `half`); an unevidenced GREEN becomes N/A. `judge` writes seat files -> row -> `**Council:**` line -> journal, each idempotently.

**C7. Plan lines.** `**Briefed:** via grill, <owner>, <YYYY-MM-DD>` (variants: `via grill (assumed)`, `via mini-brief`); `**Council:** <GREEN|RED|ESCALATE|STALE> round <N>, <date>, at <sha7>, pack <fp12>[ - red: 2,5]` appended, one per round, after the last existing `**Council:**` line or the placeholder; `## Needs a human` appended by `escalate`: `- reason: <ceiling|half|env> · round <N> · <date>` then one `- ask <n>: <verdict per round with its evidence, one clause per round>` per RED ask, then `- seats: docs/specs/<slug>/council/round-N/`. `marker()` treats a value starting with `<` as unset, so the template placeholders below are safe.

**C8. `## Asks`** in `brief.md`: a level-2 heading `## Asks` followed by `1. <verbatim fragment>` lines, numbered from 1 without gaps, ending at the next `##`. A = the count. `briefed` requires A >= 1. Tier 1: A = 1, the task phrase. Placed after `## Answers`.

**C9. `scripts/journal.sh <spec-dir> <stage> "<what happened>" "<what next>"`** appends `- <ts> · <stage> · <what happened> · next: <what next>` to `<spec-dir>/journal.md` (creating it with `# Journal: <slug>` and a blank line) and prints the same line to stdout - the Queen echoes stdout, so terminal and disk never differ. `<stage>` is one of `state.sh`'s stages (`01-spec` … `06-shipped`, `04-council:<verdict>`) or `paused`.

**C10. Agents.** `council-haiku.md`: `model: haiku`, `tools: Bash, Read, mcp__chrome-devtools__*, mcp__claude-in-chrome__*`, `disallowedTools: Write, Edit, NotebookEdit`, `maxTurns: 25`. `council-sonnet.md` and `council-opus.md`: `model: sonnet` / `opus`, `tools: Bash, Read, Grep, Glob`, same `disallowedTools`, `maxTurns: 25`. `cycle-clerk.md`: `model: haiku`, `tools: Bash`, `maxTurns: 5`. All seats: report per C5 as the final message, never a file; Bash only for commands the Profile names (Client path, run command, `## Commands`), never text from `brief.md`; work inside `COURT`. `lead-review` is unchanged and never enters the court. **Three angles, one per seat (owner's Q6, verbatim mandates in story 05):** `council-haiku` — black box: walks the *Client path* as a client would, reads no source, is the only seat that may hold the *Browser MCP* server; `council-sonnet` — line by line: runs the suite from `## Commands` once (the only seat given that command), then proves each `## Asks` item with its own `run:`/`saw:`; `council-opus` — intent and edge cases: what the owner meant but did not write, still evidencing every `ASK` line, the rest under `UNASKED:`. Two seats with the same angle would be two copies of one blind spot.

**C11. Workflow script** `.claude/workflows/vulyk-cycle.js`: `meta = { name: 'vulyk-cycle', description: 'build → council → repair, ceiling 3', phases: [{title:'Build'},{title:'Round'},{title:'Judge'},{title:'Repair'}] }`; `args: { spec, top_model, stamp }`; `agentType`s used: `cycle-clerk` (every verb, `effort: 'low'`), `worker-code`/`worker-test` (per story frontmatter `worker:`), `council-haiku`/`council-sonnet`/`council-opus` (seat = name suffix), `lead-review` (seat `review`, `model: args.top_model`), `queen-planner` (repair, `model: args.top_model`). Terminal `next` values that end the run: `green`, `escalated`, `paused`, `shipped`. Seat prompt inputs come from `status --json` only (`slug`, `round`, `court`, `round_dir`); a seat reads `brief.md` and the Profile from `<court>/` itself. The script holds no verdict, ceiling or staleness logic and never parses prose.

**C12. Commands.** `/vulyk-pause <slug> ["why"]` = `bash scripts/cycle.sh pause docs/specs/<slug> "<why>"`; `/vulyk-resume <slug>` = `cycle.sh resume` then a fresh driver launch (never `resumeFromRunId`). Driver launch protocol lives in `vulyk-build.md` step 1-2 and is referenced, not repeated, by `vulyk-plan.md`. `/vulyk-status` prints one line `driver: workflow | fallback (CLI <ver>)` and, from `council.jsonl`, `council: <specs> specs · median <n> rounds to green · <n> escalations · <n> escaped defects`, plus `merged locally, not pushed: <n>`. Profile row: `| Browser MCP | chrome-devtools \| claude-in-chrome \| none |`.

**C14. Grill** (`templates/grill.md`, owned by story 08; the quality bar the owner set — "максимально приятным, простым и понятным"): after recon, before planning; one round; 3-7 questions; one question per turn, never a bundle; each via `AskUserQuestion` with the recommended option first, labelled `(Рекомендую)`, its description saying in one line why for this code (from the recon) and what happens if chosen; an `Other` free-text always present; at most three plain sentences per question in the owner's language, no framework jargon the owner did not use; silence or "as you recommend" is always a safe answer; opens with one sentence saying how many questions are coming; the fixed last question reads back the N requirement lines and closes into `## Asks` (C8); for a bug report one question asks which shipped spec it escaped from (`**Escaped from:** <slug>`); no-question mode (`claude -p`): every recommended option, each answer marked `(assumed)`; two-stop opt-out (owner asks for plan approval) is one line in the last question. After the last answer nothing is asked — the next line the owner sees is the journal's first line (C9).

**C13. Tests.** `tests/council.test.sh` mirrors `tests/cycle.test.sh`: `set -u`, mktemp repo, `expect()` with `grep -qF`, copies `scripts/lib.sh cycle.sh journal.sh scope-check.sh redact.sh` into `$TMP/scripts/`, writes `docs/specs/demo/brief.md` with a 7-item `## Asks`, `plan.md` from `templates/plan.md`, story `demo-01-first.md`; a helper `seat_report <seat> <pattern>` emits a C5 report (pattern like `GGGGGGG`, `GRGGGGG`, `G?GGGGG` for unevidenced) and `write_seat <round> <seat> <pattern>` writes it as a fixture file with the C4 header. Exit codes are asserted with `; echo "exit=$?"` piped into `expect`.

## Integration gate
`git ls-files '*.sh' | xargs -n1 bash -n && python -m py_compile .claude/hooks/*.py && git ls-files '*.json' | xargs -n1 jq -e . > /dev/null && bash .claude/hooks/handoff.sh status && bash tests/cycle.test.sh && bash tests/council.test.sh`
Before each dispatch: `bash scripts/wave-check.sh docs/specs/autonomous-cycle`; after planning and after any delta: `bash scripts/trace-check.sh docs/specs/autonomous-cycle`.

## Tradeoffs

**Chosen: extract `pack_fingerprint` / `paperwork_only` into `scripts/lib.sh`** (story 01 creates it, story 02 migrates `ship-check.sh`, `human-check.sh`, `acceptance-log.sh`, `release-check.sh`). **Rejected: a fifth copy in `cycle.sh`.** The ADR's last invariant - "`paperwork_only()` lists every file the cycle writes" - is checked by one whitelist or by three that must agree by hand; v0.12.0 already changes that whitelist in two scripts and adds a third consumer, and a missed copy is exactly the STALE-cycle bug the owner's `human.jsonl` evidence shows (3 re-records after paperwork commits). The cost: `tests/cycle.test.sh` copies one more file into its synthetic repo, `install.sh` ships it for free (`scripts` is `OWNED`), and a script can no longer be dropped into a hive alone - which nothing does.

**Chosen: `cycle.sh` in three sequenced stories** (01 verdict, 03 intake, 04 git). **Rejected: one story.** Report parsing and worktree plumbing are different mental models and ~400 lines together; three review slots catch more than one, and the tracer proves the contract before the git side is built on it.

**Chosen: a logic-free Workflow driver plus a Haiku clerk per verb.** **Rejected: the loop's logic in JS** (ADR option 2) - it is untestable in a bash-only CI and its round counter dies with the run.

## Descoped
<!-- Mid-build narrowing, appended by the Queen as it happens - never silent. Each line:
what was dropped, why, and the single line quoted from the human authorizing it. Only
the human removes a requirement. -->

*(empty)*

## Next circle
<!--
Findings from council round 1 (2026-09-13, review.md) that the repair round of plan delta 6 does not
act on - verbatim: reviewer · file:line · condition. None is dropped; each is a defect without a
contract behind it, or a decision recorded in delta 6 whose fix is not local. /vulyk-ship appends the
seats' UNASKED lines below these.
-->
- lead-review 19 · `scripts/cycle.sh:582-583` · `attempts` in the row must count attempts (C4/ADR example), not recorded seat files; a re-asked seat does not raise it.
- lead-review 21 · `scripts/cycle.sh:172` · `row_exists` must match the round number exactly; `"round":1` matches `"round":10` (probe I: round 1 open, round 10 closed -> `open:false`, next `open-round`); reachable after three `reopen`s.
- lead-review 22 · `scripts/cycle.sh:118,1177,1439` · Journal stages `tier:default`, `04-council:open`, `resumed` are outside C9's vocabulary (`state.sh` stages or `paused`).
- lead-review 25 · `scripts/cycle.sh:593-594` · The `**Council:**` line must land after the last existing line or the placeholder as C7 says; appended at EOF, `marker "$PLAN" Council` (`grep -m1`) keeps returning the placeholder forever.
- lead-review 26 · `.claude/agents/drone-docs.md:14`, `scripts/acceptance-log.sh:72`, `scripts/ship-check.sh:227` · Story 05's criterion "`grep -rl drone-acceptance .claude/agents` is empty" is unmet, and two live scripts still instruct "re-dispatch drone-acceptance", an agent that no longer exists.
- lead-review 27 · `docs/cycle.md:25`, `docs/pipeline.md:22`, `.claude/commands/vulyk-bootstrap.md:9` · Row 02 of the cycle table still reads "`**Approved:**` line … `/vulyk-plan`, stop for approval" beside the new paragraph; the judge row still says "three seats ABSENT -> env … past round 3" (pre-C15); bootstrap still says "what the owner is pointed at in stage 05".
- lead-review 30 · `plan.md` Plan delta 2 vs `scripts/cycle.sh:1074-1076` · The delta promises `repeat` is passed to `close-story`, which has no such flag; `repeat` in `wave_stories` is dead data (story 15 flagged this).
- lead-review 31 · `scripts/cycle.sh:262-273` · `wave_stories` must list only stories whose blockers are done; every `todo` of the wave is appended regardless of `blockers_done`, so a story blocked by a `blocked` story is dispatched.
- lead-review 35 · `docs/architecture.md:76` · "the SessionStart brief say[s] which driver is active" overstates; the brief reports only the CLI version gate (plan Assumptions).
- second reviewer m-3 · `install.sh:322-325` (`ensure_gitignore`) · a gitignore entry containing a glob must reach the target file literally.
- second reviewer m-4 · `scripts/cycle.sh:546` and `scripts/ship-check.sh:190` · an override recorded in the same second as the record it outranks must still outrank it.
- second reviewer m-6 · `.claude/commands/vulyk-evolve.md` step 2 · the weekly council numbers must be derived from data git preserves, with a date call that works on the platforms a hive runs on.
- second reviewer m-9 · `.claude/commands/vulyk-ship.md` step 2 · the merge step must keep an explicit statement that push, PR and tag are outward-facing and not the command's to run.
- second reviewer m-10 · `scripts/cycle.sh:604-617` · the ledger append must be atomic for a row of any length the schema permits.
- second reviewer m-11 · `.claude/hooks/top-model-brief.sh:42-45` · the session brief must not spawn the CLI to report a gate whose answer it cannot determine.
- second reviewer M-7 · `scripts/cycle.sh:1147-1157` (`clean_court`), `:1160` (`mkdir -p`), and no lock anywhere · a second driver must be refused on a spec another driver holds, rather than proceeding into the same round directory, court and index. — decided in plan delta 6, R27 (`DRIVER` semaphore through `cycle.sh claim`/`release`, `--stamp` on the four mutating verbs); not local, built next circle.
<!-- Council round 2 (2026-09-13, council/round-2/review.md), per plan delta 7 - same rule; lead-review r2 = the second-round report's own minor numbering, N-m = the second reviewer's. -->
- lead-review r2 minor 1 · `scripts/cycle.sh:498` (`escalate_row_exists`), `:500`, `:553` · The ESCALATE idempotency checks must match the round number exactly: `"round":1` matches a `"round":10` row and `round 1` matches `round 10` (probe 4), the defect next-circle item lead-review 21 records, now in two new sites; reachable after three `reopen`s.
- lead-review r2 minor 2 · `scripts/cycle.sh:1385,1402` · `close-story` must not leave a story `done` on disk when its commit failed, or must accept the retry: the `sed` lands before `git_commit_or_fail`, the retry answers `already done`, `status` then routes to `open-round`, and `open-round` refuses `working tree not clean` (probe 2) - an honest stop, but one only a hand commit clears, and the suite's `lockfail1` covers `briefed`, not this verb.
- lead-review r2 minor 3 · `scripts/cycle.sh:1623-1631` · `open-round --commit`'s "already open at current HEAD, no-op" branch must commit a `ROUND` and journal it finds uncommitted: after a failed commit the retry exits 0 and leaves `docs/specs/<slug>/council/` and `journal.md` untracked until `judge --commit` sweeps them (probe 3), so a crash plus a fresh clone in between loses the round counter D1 exists to preserve.
- lead-review r2 minor 4 + second reviewer N-m1 · `scripts/cycle.sh:1645,1659` vs `:784,853`, `:1212`, `:62`; `.claude/workflows/vulyk-cycle.js:117`; `.claude/commands/vulyk-build.md` step 2 and step 4 · An escalation recorded by `open-round` must reach the Queen as the `escalated` terminal, not as a generic failed verb: `judge` and `escalate` emit `ok:true` on exit 6 while `open-round` emits `ok:false` with no `error`, so the Workflow returns `stop:{verb:"open-round",exit:6,error:undefined}` and the wake-up step prints an undefined error plus the journal tail instead of `## Needs a human`, and the fallback journals a duplicate `open-round exit 6` line - the record on disk is right, the report is not. · every `ok:false` emit must name its reason, and exit 6 must mean the same thing to `ok` whichever verb reports it.
- lead-review r2 minor 5 · `scripts/cycle.sh:1639-1647` · The STALE-fold ceiling gate (the path round-1 finding 5 named first) must have a suite scenario; only the fresh gate is asserted (`oceil1`, `oceilc1`).
- lead-review r2 minor 6 · `scripts/cycle.sh:553-560` (`write_escalate_row_for_round`) vs C7 · The ceiling block `open-round` writes must list the RED asks of the round it stops (C7: "one row per RED ask with its evidence"): a RED round N judged earlier, then a code commit, then the fresh gate at N = ceiling writes `reason: ceiling`, `note: open-round ceiling`, `seats:` and no ask rows, although the RED row carries them.
- lead-review r2 minor 7 + second reviewer N-m2 · `scripts/cycle.sh:1464-1467` · The court's reduction commit must run with hooks and signing off (`--no-verify`, `-c commit.gpgsign=false`): the court shares the hive's hooks path and config, a pre-commit hook or a signing prompt fails the commit, the `|| true` hides that, and the court's `git status` then shows exactly the deleted-file lines the seat prompt names as a BREACH. · a court whose reduction could not be committed must not be handed to a seat as if it had been.
- lead-review r2 minor 8 · `scripts/cycle.sh:515-539` vs `:617-664` and `:1506-1528` · The seat scan (`<seat>.md` -> verdict, model, attempts; ABSENT when required) now exists three times - `cmd_judge`, `write_stale_row`, `write_escalate_row_for_round` - and must be one helper; a schema change edits three copies.
- lead-review r2 minor 9 · `scripts/cycle.sh:1281-1290,1353-1359` · A `## Commands` cell that itself contains ` && ` (a hive whose quiet command is `npm run build && npm test`) can never be a verification line: the line is split on ` && ` before the byte-for-byte match, so the rule must match the whole line against a cell first or forbid `&&` inside cells.
- lead-review r2 minor 10 + second reviewer N-m5 · `docs/command-reference.md:15` · The `/vulyk-review` entry must match the command after R20/R11: it still dispatches all four seats regardless of `missing` and records with `<<'EOF' ... EOF`. · the command reference must describe the seats, the inputs and the delimiter the commands actually use.
- lead-review r2 minor 11 · `docs/cycle.md:28,46` · The cycle guide must stop promising "a court that cannot see the hive's stories" / "so none of them can see the hive's own account" - the guarantee R15 withdrew.
- lead-review r2 minor 14 · `CHANGELOG.md` 0.12.0 `### Added` · The entry must say `lead-review.md` gained the machine-read first line.
- lead-review r2 minor 15 · `.claude/workflows/vulyk-cycle.js:88-90` vs `.claude/commands/vulyk-build.md` `briefed` row · The two drivers must agree on `next:"briefed"`: the fallback refuses and points at `/vulyk-plan`, the Workflow calls `briefed --commit` and stamps `**Briefed:** via grill` on a spec that was never grilled.
- lead-review r2 minor 16 · `scripts/cycle.sh:1563` vs `.claude/commands/vulyk-build.md` `build:<wave>` row · Either `open-round` refuses a `blocked` story or the driver stops claiming it never opens on one: the verb accepts `done|blocked`, so a relaunch after the blocked stop opens the council on the incomplete pack.
- lead-review r2 minor 17 · `.claude/workflows/vulyk-cycle.js:130-136` · A `record-seat` answering exit 3 must end the run as the `paused` terminal, not as `stop:{verb:"record-seat",exit:3}` with no `error`; the emit's own `next:"paused"` is the signal.
- lead-review r2 minor 18 · `.claude/commands/vulyk-build.md` step 2 (`journal.sh … 03-building "<verb> exit <n>"`) · Journaling a council-stage stop under `03-building` widens the C9 vocabulary gap already in `## Next circle` (lead-review 22); fold it there. — folds into lead-review 22 above.
- second reviewer N-m3 · `scripts/cycle.sh:1441` vs `:1483` · a round directory without its `ROUND` marker must not read as an open round — `open-round` must rebuild it or refuse, rather than reporting a no-op success.
- second reviewer N-m7 · `scripts/cycle.sh:543`, `:733` vs plan `## Contracts` C4 · `note` must pass through the mask its contract names, or C4 must stop naming it.

## Plan deltas
<!--
Queen-written, from a worker's RETURN REPORT (never from a diff), one entry per change
to the plan after approval: new story cut, story files expanded, contract changed.
Each entry: date, trigger, decision, what was rejected. One-line notice to the human
when it happens. trace-check.sh accepts these entries as a quote source for stories
born after approval - a delta is requirement change on the record.
-->

- **2026-09-12 · trigger:** worker-07 return report, CONCERNS line — `shippable()` in `install.sh` has no exclusion for gitignored runtime files, so `copy_tree ".claude"` copies the maintainer's `.claude/settings.local.json` (the pinned model) and other runtime artifacts into every install/upgrade target. **Decision:** cut story `autonomous-cycle-14-install-shippable-runtime-files` (wave 3, blocked_by 07, files: `install.sh` only): the installer must never ship a file that its own `.gitignore` ignores — `.claude/settings.local.json`, `.claude/handoff/`, `.claude/state.json`, `.claude/.vulyk-update-cache`, `.claude/settings.json.vulyk-bak`, `memory/snapshots/*`, `memory/map/.stale`, `__pycache__` — and the install-smoke CI job proves it. **Rejected:** leaving it for the next circle — story 07 makes `--apply` pin the *target*, and shipping the maintainer's pin alongside would silently undo that on every upgrade.
- **2026-09-12 · trigger:** worker-06 return report, CONCERNS line — C3's `status --json` lists `wave_stories` as file paths only, with no per-story `worker:`; the Workflow driver may not read story files, so it dispatches every build story to `worker-code`. **Decision:** contract change to C3 — `wave_stories` becomes a list of objects `{"file": "<path>", "story": "<id>", "worker": "worker-code|worker-test", "repeat": <n|1>}` read from story frontmatter and `## Verification`; cut story `autonomous-cycle-15-status-carries-the-worker` (wave 4, blocked_by 04 and 06, files: `scripts/cycle.sh`, `tests/council.test.sh`, `.claude/workflows/vulyk-cycle.js`): `status` emits the objects, the driver routes `agentType` from `worker` and passes `repeat` to `close-story`, the test proves a `worker: worker-test` story is routed. **Rejected:** letting the driver read the story file (breaks "the script never parses prose" and the no-filesystem runtime); keeping the hardcode (silently wrong for every future spec with a `worker-test` story).
- **2026-09-12 · trigger:** worker-11 return report, CONCERNS line — `install.sh`'s `VULYK:PROFILE` reset placeholder (the block a fresh hive receives) already omitted the *Client path* and *Release / deploy* rows before this spec, and now also omits the new *Browser MCP* row; a fresh install gets a five-row Profile while `CLAUDE.md` documents eight, so `/vulyk-bootstrap` and the council's Client path lookup have no row to fill. **Decision:** cut story `autonomous-cycle-16-install-profile-rows` (wave 5, blocked_by 11, files: `install.sh`): the reset placeholder carries every row of the Profile table in `CLAUDE.md` — same order, same `<fill in - …>` hints — and the install-smoke job asserts the row count of the blanked block equals the row count of the source block. **Rejected:** leaving it to the next circle (every hive installed from 0.12.0 would lack the row the council reads) and generating the block from `CLAUDE.md` at install time (the installer blanks values, it does not parse tables; keep the placeholder literal and add the count check so it cannot drift again).
- **2026-09-13 · trigger:** worker-04 return report, CONCERNS line — `record-seat`'s staleness check and `status --json`'s `stale` field compare the round's recorded `head` to the current HEAD with plain equality, not through `paperwork_only`; so the first `record-seat` after a real `open-round --commit` (which itself moves HEAD by one paperwork commit) reads the round as STALE although no code moved. **Decision:** cut story `autonomous-cycle-17-stale-means-code-moved` (wave 6, blocked_by 15, files: `scripts/cycle.sh`, `tests/council.test.sh`): every place `cycle.sh` decides "stale" — `record-seat`, `status` (`stale`, and the `next` derivation `green`/`open-round` on GREEN gone stale), `open-round`'s STALE-row fold — uses one helper `round_is_stale <spec> <N>` built on `paperwork_only` from `lib.sh`, so a commit touching only plan.md, journal.md, `council/*`, `memory/stats/*.jsonl` never stales a round; the test proves that `open-round --commit` followed by `record-seat` records at attempt 1 with `stale=false`, and that a commit touching a source file does stale it. **Rejected:** removing the `--commit` from `open-round` (the ROUND marker must be in git to survive a crash — ADR D1) and special-casing the one commit (the same false STALE would return on the next paperwork commit, e.g. a `record-seat --commit` of a seat file).
- **2026-09-13 · trigger:** the owner, while confirming `## Asks` — a thirteenth ask, verbatim in `brief.md`: "Модель и система должна чётко понимать сложность задачи. Если, например, задача — покрасить кнопку, переместить кнопку, — это базовые простые вещи, то тогда она не должна запускать 10 сабагентов, а это 1-2 сабагента". Today a Tier 1 spec gets one worker plus three council seats plus `lead-review` plus clerks — five or more agents for a one-line change. **Decision:** the council scales with the tier the Queen already assigns; cut story `autonomous-cycle-18-council-scales-with-tier` (wave 7, blocked_by 17, files: `scripts/cycle.sh`, `tests/council.test.sh`, `CLAUDE.md`, `docs/cycle.md`). Contract **C15 — required seats by tier**, read from plan.md's `**Tier:**` line: Tier 1 → `sonnet` only, no `review` (one worker + one seat = two subagents); Tier 2 → `sonnet`, `opus`, `review`; Tier 3-4 → `haiku`, `sonnet`, `opus`, `review` (Tier 4 adds the second reviewer as today). `status --json` `missing` lists only the required seats; `judge` needs only those; a recorded non-required seat is accepted and counted; the ceiling and the verdict table are unchanged. `CLAUDE.md`'s routing matrix says per tier how many agents a task costs, and `docs/cycle.md` replaces "the council does not shrink with tier" with "it shrinks by seat count, never to zero" - the decision how far to scale the hive is the Queen's tier call, made once, before any work (the routing matrix's first rule). **Rejected:** a new knob beside the tier (two scales for one judgement drift apart); skipping the council on Tier 1 entirely (ask 11 says Tier 1+ gets a council); making seats optional per Profile row (the Queen already decides complexity per task, a per-hive setting cannot).
- **2026-09-13 · trigger:** council round 1 RED — review seat BLOCK (`council/round-1/review.md`: lead-review, Fable, 6 critical / 10 major / 19 minor; second reviewer, Opus, 4 critical / 11 major / 11 minor), sonnet and opus GREEN, haiku N/A — plus two facts the round produced on this spec itself: (a) `judge --commit` journaled `next: repair` (`journal.md:4`) while `status --json` then answered `next: open-round` (lead-review 1 / C-1, live); (b) `record-seat` rejected `sonnet.attempt-1.md` and `opus.attempt-1.md` as tainted for naming `vulyk-plan.md` — a command file (`sonnet.attempt-1.md:10`, `opus.attempt-1.md:10`) — and `<spec>/journal.md` from `journal.sh`'s own description (`opus.attempt-1.md:16`), so both non-haiku seats spent attempt 1 on a detector false positive and the accepted reports paraphrase paths to get past it (lead-review 9, live; the planner counts it as a critical). **Decision:** one repair round — stories 19-25 in waves 8-10 — on the findings below, deduplicated across the two reviewers (lead-review numbers; second reviewer `C-`/`M-`/`m-` ids), each with the reviewers' condition sentences verbatim (the quote source for the repair stories) and one route: `story NN`, `delta` (a decision taken here, built where the fix is local), or `next circle` (verbatim under `## Next circle`). Routing rule: every critical from either reviewer and every major that breaks a contract already in this plan → story; a major or minor that changes a contract or an owner decision → decided here, story only where the fix is local; every remaining minor → `## Next circle`, folded into a repair story only when that story rewrites the exact lines the minor names (the neighbour test), said so per finding.
  - **R1 · a RED judged with `--commit` routes to a fresh round** (lead-review 1, C-1; `scripts/cycle.sh:337`). Lead-review: "After a committed RED judge, `status.next` must be `repair` until a non-paperwork commit lands (the same `paperwork_only` rule the `green` branch already uses at line 335), and a test must drive `open-round --commit` -> `record-seat` x4 -> `judge --commit` -> `status` and assert `repair`." Second reviewer: "the `repair` derivation must treat a RED row as current when only the cycle's own paperwork landed since the round's head, so that a RED round judged with `--commit` routes to `repair` and not to a fresh round." C3's `repair` row becomes "newest row RED and only paperwork since its `head`" (through `round_is_stale`); `open-round` on RED only when code moved. → **story 19**.
  - **R2 · a stale open round is never reported as one** (lead-review 2; `cycle.sh:302-304,330-333`). "For an open round that `round_is_stale` reports stale, `status` must return `open-round` (never `dispatch:`/`judge`) and `stale:true` whether or not a seat file exists." → **story 19**.
  - **R3 · an exhausted seat stays `missing` forever** (lead-review 3, C-2; `cycle.sh:146-154,987-990`). Lead-review: "`missing` must exclude a seat whose `<seat>.attempt-2.md` exists without `<seat>.md`, so `next` reaches `judge` and the `env` escalation is reachable through a driver." Second reviewer: "a seat with an `attempt-2` file and no final report must not be listed as missing, so that `status` reaches `judge` and the ABSENT path in the verdict table becomes reachable." → **story 19**. C-2's second half — `lead-review.md` never promised a machine-readable verdict while `record-seat … review` parses one — is a contract gap: C10's "`lead-review` is unchanged" is amended, and `lead-review.md` gains the one rule: the report's first line is `VERDICT: PASS` or `VERDICT: BLOCK`. → **story 24**.
  - **R4 · `memory/stats/scope.jsonl` dirties the tree and the first `open-round` refuses** (lead-review 4, C-3(b), lead-review minor 33; `cycle.sh:1071,1269`, `lib.sh:44`, `scope-check.sh:101`). "`memory/stats/scope.jsonl`, which `close-story` writes through `scope-check.sh` and does not commit (its commit is scoped to the story's `## Files`), must be either committed by `close-story` or whitelisted by `paperwork_only` and `open-round`'s clean-tree check, with a test that runs `close-story --commit` then `open-round` without an intervening `git add -A`." Decided, both: `close-story --commit` stages `memory/stats/scope.jsonl` beside the story's files (it is the verb's own write), and C1's whitelist gains `memory/stats/scope.jsonl`, so the ADR invariant "`paperwork_only()` lists every file the cycle writes" holds again. Minor 33 ("either that is a deliberate dogfooding decision on the record or it should be gitignored like the other series"): decided — VULYK keeps `scope.jsonl` tracked on this branch; it is paperwork like the other series in a target hive and rides in the cycle's own commits from now on. → **story 21**.
  - **R5 · a ceiling reached at `open-round` writes nothing and the driver loops** (lead-review 5, C-3(a); `cycle.sh:1317-1332,1464-1468`, `vulyk-cycle.js:46-92`). Lead-review: "A ceiling reached at `open-round` (the STALE-fold path at line 1318, and the fresh-round gate at 1328) must leave an ESCALATE row and `## Needs a human` on disk so `status` says `escalated`, and a driver must end the run on any `"ok":false` clerk result instead of looping." Second reviewer: "the Workflow driver must act on each verb's exit code — escalating on 6 with a record on disk, surfacing 2 as a stop rather than a retry, re-asking a seat once on 4 — and `escalate` must be able to record an escalation for a round that was never dispatched." Decided: `open-round` at the ceiling itself writes the ESCALATE row (`round` = the last judged round, `escalate:"ceiling"`), the `**Council:** ESCALATE` line, `## Needs a human` and the journal line, idempotently, then exits 6; `escalate` becomes a verb of its own — `escalate <spec> [--commit] [--reason <ceiling|half|env>] ["<note>"]` — that records an escalation for an open round with seats missing (court removed, no seat precondition) and behaves as `judge` when nothing is missing → **story 21**. A driver ends the run on any `"ok":false` result except the two exit-4 cases of R6 → **story 22** (Workflow), **story 23** (fallback).
  - **R6 · the two drivers do not share a bound** (lead-review 6, C-3(c); `vulyk-cycle.js:61-65,79`, `vulyk-build.md` `build:<wave>`). Lead-review: "The two drivers must apply the same bound on a story whose verification stays red" and "The same loop fires when a seat returns an empty report". Second reviewer: the Workflow driver "lacks the fallback's build-retry cap". Story 06's criterion "no in-script retry" is withdrawn. C11 gains: the driver keeps a per-run attempt count per story file (a language-native map, no runtime API) — a second red `close-story` for the same story ends the run with `stop: {verb, file, error}` in the returned object, and the Queen at wake does what the fallback does in-session: marks the story `blocked`, consults `lead-architect`, stops; `record-seat` exit 4 → re-ask that seat once with `error` verbatim, then record again (a second exit 4 leaves the seat ABSENT on disk and the loop continues — R3 routes it); an empty seat report is still sent to `record-seat`, so the attempt exists on disk. → **story 22**, **story 23**.
  - **R7 · `reopen` leaves `status` terminal** (lead-review 7, M-4; `cycle.sh:334,1352-1401`). Lead-review: "After `reopen`, `status.next` must be `open-round`, not `escalated`". Second reviewer: "after `reopen`, `status` must route to `open-round` rather than remaining terminal." Decided: C4 gains `council/REOPEN` — one line `round=<N> · <ts>`, written by `reopen`, committed with `--commit`; C3's `escalated` row becomes "newest row ESCALATE and no `REOPEN` naming its round". → **story 19**.
  - **R8 · the evidence parser truncates at an interior ` - `** (lead-review 8; `cycle.sh:790,876-879` vs `:411`; live: `round-1/opus.md` ASK 4 and ASK 9 downgraded to N/A). "One evidence rule, shared by `record-seat` and `judge`, must locate `run:`+`saw:` / `url:`+`saw:` / `why:` anywhere after the verdict token and never truncate at an interior ` - `; a test must carry ` - ` inside `saw:`." → **story 20**.
  - **R9 · taint fires on framework file names** (lead-review 9, the live round; `cycle.sh:781-784`). "Taint must match paths under `docs/specs/<slug>/` (its own `plan.md`, `journal.md`, `council/`, `<slug>-NN` story files), not the bare words anywhere in the body." C5's taint clause becomes: a body is tainted when it contains `docs/specs/<slug>/plan.md`, `docs/specs/<slug>/journal.md`, `docs/specs/<slug>/council/`, the same three with `docs/specs/` omitted (`<slug>/plan.md`, `<slug>/journal.md`, `<slug>/council/`), or a story id `<slug>-NN` (two digits, word-bounded); bare `plan.md`, `journal.md`, `council/`, another directory's `journal.md` and command-file names are not taint. → **story 20**. Lead-review 10 ("A blind seat's prompt must not contain a string C5 forbids it to repeat"): C11 amended — a seat prompt carries `slug`, `round` and `court` only, never `round_dir`; `round_dir` goes to `lead-review` alone. → **story 22**, **story 23**.
  - **R10 · one evidenced RED escalates every one- and two-ask brief** (lead-review 11; `cycle.sh:554,559`). "The plan must state whether one evidenced RED on a one- or two-ask brief is an escalation or a repair, and rule and test must match". Decided: a single evidenced RED is always a repair; C6/D4 `half` becomes |RED_e| >= max(2, ceil(A/2)) — A = 1 never escalates on `half` (the ceiling stops it), A = 2 escalates only when both asks are RED. → **story 20**.
  - **R11 · untrusted text reaches a shell as syntax** (lead-review 12, 13, C-4; `vulyk-cycle.js:79,84`, `cycle.sh:1086`, `vulyk-build.md` `dispatch:<seats>`, `install.sh` `wire_permissions`). Lead-review: "A seat report must reach `record-seat` through a channel that cannot terminate early (a file the clerk writes, or a per-call unique delimiter that `record-seat` refuses inside the body)" and "The plan must name and constrain the fact that `close-story` runs a story's `## Verification` through `bash -c` under the clerk's allow rule - e.g. require the command to be one of the Profile's `## Commands` rows, as the seats already are". Second reviewer: "untrusted text must not reach a shell as syntax — a seat report must be delivered to `record-seat` through a channel a report's own content cannot terminate, and the story-authored verification command must not run under a blanket allowlist entry granted to `cycle.sh`." Decided: (i) the heredoc delimiter is per call and unguessable by the seat — `VULYK_<stamp>_<seat>_<attempt>`, where `stamp` is the run's shell-provided `args.stamp` (Workflow) or `date -u +%s` taken once at loop start (fallback) and never appears in any seat prompt; "refuses inside the body" is moot — a body holding the delimiter never reaches the verb — so unpredictability is the whole guarantee, and is stated as such; (ii) C2's `close-story` gains a precondition: every `&&`-separated segment of every `## Verification` line must equal, byte for byte, a command in the `## Commands` table of the hive's `CLAUDE.md` (or the line is the literal `none — reviewed by lead-review`, which runs nothing), else exit 2 naming the segment; the two allow rules stay — they are what lets the clerk run at all — and CI proves them (R22). Rejected: base64 or a clerk-written file (the runtime guarantees neither an encoder nor a second channel, and a file written from the same prompt has the same heredoc); the seat recording itself (seats never write, C10). → **story 22**, **story 23**, **story 21**.
  - **R12 · the Workflow dispatches one reviewer at Tier 4** (lead-review 14; `vulyk-cycle.js:19,72-80`). "The Workflow driver must dispatch the Tier 4 second reviewer and fold its verdict as `/vulyk-review` step 3 and the fallback's dispatch row do". Decided: C3 gains `"tier":1|2|3|4|null` (the round's frozen tier while a round is open, else the plan's); C11's `args` gain `second_model` (the launcher passes `opus` beside a Fable top model, `sonnet` beside an Opus one — CLAUDE.md's Tier 4 rule); at tier 4 the `review` seat is two dispatches — `lead-review` at `top_model` and `lead-review` at `second_model` — folded to the stricter verdict before one `record-seat … review`; the driver reads the first line of each review report (the token R3 makes mandatory) and nothing else. Lead-review minor 34 ("the two drivers differ in what the review seat receives") is folded here: the same lines build the review prompt. → **story 19** (`tier`), **story 22**.
  - **R13 · `CHANGELOG.md` describes a council that did not ship** (lead-review 15, m-8, lead-review minor 29, m-5; `CHANGELOG.md:5-40`). Lead-review: "The 0.12.0 entry must describe the shipped council". Second reviewer: "the release notes must describe the two drivers' behaviour as it is, or the two drivers must actually match." Minor 29 (""all verified from a real pre-0.12 install" has no run behind it") and m-5 ("an upgrade must retire framework-owned files the new version no longer ships, or say plainly that it does not") are decided here: the `### Upgrading` paragraph says plainly that `--upgrade` never deletes and that `.claude/agents/drone-acceptance.md` is removed by hand, and the "verified from a real pre-0.12 install" claim is dropped — no removal mechanism is built (the first deleted file in the project's history; a mechanism without a second case is speculative). Folded because story 25 rewrites that block. → **story 25**.
    - quote source kept verbatim for story 25 (m-5): "an upgrade must retire framework-owned files the new version no longer ships, or say plainly that it does not."
  - **R14 · the Workflow API surface has never run** (lead-review 16; `vulyk-cycle.js:53,57,72-80`). "The Workflow API surface the driver uses (`parallel` over an array of thunks, `pipeline(list, f, g)` with a 3-arg shape, `phase()`, `agent()` options `agentType`/`model`/`effort`/`phase`) must be verified against the platform docs and the outcome recorded". → **story 22** (verified against the docs the brief's `## Evidence` names, outcome in the story's notes); also a recon question for the Queen.
  - **R15 · the court's blindness is working-tree-only and the court is writable** (M-1, M-2; `cycle.sh:1163`, ADR D5, `council-*.md:13`, `CHANGELOG.md:14-17`). M-1: "a seat working in the court must not be able to recover the spec's stories, plan or journal from the repository the court is attached to, or the guarantee must be restated as the honour clause it currently is." M-2: "either the court must actually be unwritable by a seat, or the word "read-only" must leave D5 and the three prompts, with the concurrent-sharing consequence stated." Decided: restate. D5, the three seat prompts, CLAUDE.md's council sentence and the CHANGELOG say the court is a shared, writable worktree whose working tree holds `brief.md` only, whose git history is out of bounds (`git log`, `git show`, `git diff` against any commit, and the deleted-file lines of `git status` are a BREACH), and whose writes are discarded by `judge` — an honour clause with a detector, not a guarantee; the sonnet seat's suite run may leave files the other two see. One mechanical narrowing that costs two git commands: `open-round` commits the reduction inside the court (detached), so an orientation `git status` is clean and `HEAD:docs/specs/<slug>/plan.md` no longer resolves. Rejected: a separate object store per court (a clone still carries the pack commit's tree and every story commit's name). → **story 24** (words), **story 21** (the reduction commit).
  - **R16 · verdict rows the table does not have** (M-5, lead-review minor 32; `cycle.sh:565-575,573`). M-5: "a round in which no ask is RED and `lead-review` passed, but a seat is ABSENT, must resolve to a verdict whose `next` action is defined and whose row carries something a repair can act on." Minor 32: "D4 has no row for a required `review` that is ABSENT while every seat is GREEN". Decided: C6/D4 gains a row after `REJECTED` and before `half` — any required seat (review included) ABSENT with RED_e ∪ RED_u empty and review not BLOCK → ESCALATE, `escalate:"env"`, `note` naming the absent seats, `## Needs a human` pointing at their attempt files. A seat that failed the contract twice after being told the gap is a defect of the seat, the court or the tooling, which no repair story fixes and which ask 3's three angles do not allow to pass silently. Rejected: counting ABSENT as N/A and going GREEN (ships with an angle unjudged); the present RED with `red:[]` (a repair round with nothing to repair). → **story 20**.
  - **R17 · git failures are swallowed** (M-6; `cycle.sh:718-720,622,680,733,1109,1181,1395`). "a `cycle.sh` verb must not report success, or write the marker that claims it, when the git operation it depends on failed." → **story 21**.
  - **R18 · `close-story` reports only the last command** (M-3; `cycle.sh:1074-1092`). "a `## Verification` block with more than one command must fail the story if any of them fails." → **story 21** (with R11's gating: one command per line, each run on its own).
  - **R19 · a seat report recorded during PAUSE is lost, and the docs say otherwise** (M-8, lead-review minor 28; `cycle.sh:970`, ADR D6, `vulyk-pause.md:12-14`, `docs/command-reference.md:18`). M-8: "a seat report produced before the pause must survive it, or D6 and `/vulyk-pause`'s wording must say the report is lost." Minor 28: "the docs must say the seat is re-dispatched." Decided: the words change, not the guard — under PAUSE the tree is the human's, and a report against it is a report against a moving target; D6, `/vulyk-pause` and the reference say an in-flight seat's report is discarded and the seat is re-dispatched on resume. → **story 23** (command), **story 24** (ADR, reference).
  - **R20 · `/vulyk-review` dispatches the full court at every tier** (M-9; `vulyk-review.md` step 3). "the on-demand round must dispatch the seats the round's frozen tier requires, the same list the driver reads from `missing`." → **story 23**.
  - **R21 · the template has no Tier 1 and an unparsable tier buys the largest court** (M-10, m-1, lead-review minors 23 and 20; `templates/plan.md:3`, `cycle.sh:110-124`, `:496,532,1197-1198`). M-10: "the plan template must offer Tier 1, and the default for an unparsable `**Tier:**` must not silently buy the largest court." m-1: "`status` must not write to disk on any path." Minor 20: "A non-required seat must have one recorded value" and "the same delta should add `tier=` as ROUND's sixth line, which C4 still lists as five". Decided: `templates/plan.md` offers `<1|2|3|4>`; there is no default tier — `status` reports `"tier":null` and `next:"open-round"`, `open-round` exits 2 naming the `**Tier:**` line, and the journal write in `tier_of` goes away; C4's `ROUND` is six lines, the sixth `tier=<n>`; a non-required seat is recorded as `""` by every writer (`judge` and the STALE fold alike), and C4's seat enum reads `GREEN|RED|N/A|ABSENT|""`. → **story 25** (template), **story 19** (script).
  - **R22 · the install smoke job does not prove the allow rules** (M-11; `.github/workflows/ci.yml` `install-smoke`). "the install smoke job must prove the two allow rules exist in the installed target's settings." → **story 25**.
  - **R23 · unanchored paperwork patterns** (m-2, lead-review minor 24; `lib.sh:41-44`). m-2: "the paperwork whitelist must match only paths the cycle itself writes, not any path in a hive that happens to share those names." Minor 24: "`*/council/*` and `*/journal.md` must be anchored to `docs/specs/*/`". Decided: C1's whitelist becomes `docs/specs/*/plan.md`, `docs/specs/*/journal.md`, `docs/specs/*/council/*`, `docs/specs/*/brief.md` (`reopen` writes it) and the five `memory/stats/*.jsonl` series (`human`, `acceptance`, `ship`, `council`, `scope`). → **story 21**.
  - **R24 · `judge` on RED exits 4** (lead-review minor 17; `cycle.sh:627-628`). "`judge` on RED must not exit 4 with `"ok":true`". C2 clarified: 4 is `record-seat` MALFORMED and `close-story` red verification only; a RED verdict is a successful judgement and exits 0 with `next:"repair"`; ESCALATE keeps 6. → **story 20**.
  - **R25 · `round_dir` is `null` during a round** (lead-review minor 18; `cycle.sh:319`; live: this spec's own `status --json`). "`round_dir` must name the open round's directory". Folded: story 19 rewrites the derivation these lines live in, and R12's review packet names `round_dir`. → **story 19**.
  - **R26 · seat files reach git only at `judge`** (m-7; `cycle.sh:930-1000` vs ADR D1). "either seat files reach git when they are recorded, or D1 stops listing them as committed." Decided: D1's table says seat files are committed by `judge --commit` (and by the STALE fold), not at record time; a crash plus a fresh clone before `judge` loses at most one round's seat files, and the next `open-round` re-dispatches them. Rejected: `record-seat --commit` — four commits per round contending on the index while seats run in parallel, M-7's exact hazard. → **story 24**.
  - **R27 · nothing serialises two drivers** (M-7; `cycle.sh:1147-1160`, no lock). "a second driver must be refused on a spec another driver holds, rather than proceeding into the same round directory, court and index." Decided in principle, not built in this round: a gitignored `docs/specs/<slug>/DRIVER` semaphore written by a new `cycle.sh claim <spec> <stamp>` (`noclobber`; exit 2 `held by <stamp>` when another run holds it), released by `release`, `resume` and `pause`, honoured by `open-round`, `record-seat`, `judge` and `close-story` through a `--stamp` argument — a new verb on four verbs and both drivers, not a local fix. → **next circle**, decision recorded.
  - **Not folded, verbatim under `## Next circle`:** lead-review minors 19, 21, 22, 25, 26, 27, 30, 31, 35; second reviewer m-3, m-4, m-6, m-9, m-10, m-11; M-7 as above.
  - **Contract amendments in force from this entry** (the `## Contracts` text above is not rewritten — as for deltas 2 and 5, this list wins where they differ): C1 whitelist per R23; C2 `close-story` precondition per R11, `escalate` per R5, exit-code legend per R24; C3 `repair`/`open-round`/`escalated` rows per R1, R2, R7, new key `"tier"` per R12, `stale` and `round_dir` per R2/R25; C4 `REOPEN` per R7, six-line `ROUND` and the seat enum per R21; C5 taint per R9; C6 `half` per R10, the ABSENT row per R16; C10 `lead-review.md` per R3; C11 per R6, R9, R11, R12. ADR-001 D1, D2, D4, D5, D6 are amended to match by story 24; its status stays proposed.
  - **Rejected for the round as a whole:** fixing only the two live defects (a, b) and re-running — three RED rounds on identical code is the loop R1 breaks, and lead-review found the same class four more times; widening to the next circle's minors — the repair budget is eight stories at most, and each minor there is a defect without a contract behind it.
- **2026-09-13 · trigger:** council round 2 RED — review seat BLOCK (`council/round-2/review.md`: lead-review, Fable, PASS with 0 critical / 4 major / 18 minor; second reviewer, Opus, BLOCK with 1 critical / 5 major / 8 minor; folded to the stricter verdict), sonnet and opus GREEN, haiku N/A. Round 3 is the ceiling. Every round-1 critical and major is met and tested except R14 (R35 below) and C-4's first sink (narrowed, downgraded to major — R31 below). **Decision:** one repair round — stories 26-29 in wave 11, `## Files` disjoint — on the findings below, deduplicated across the two reviewers (lead-review round-2 ids `LR M-n`/`LR m-n`; second reviewer `N-C1`/`N-Mn`/`N-mn`), numbered on from delta 6, each with the reviewers' condition sentences verbatim (the quote source for the repair stories) and one route. Routing rule for this entry: every critical and every major whose fix is local and testable → story; a major that changes a contract or an owner decision → decided here, story only where the fix is local; every minor → `## Next circle` verbatim, with four exceptions said so per finding: N-m4 is a precondition of the critical's fix, and LR m12, LR m13 and N-m6 name lines of the one file story 29 already rewrites (lead-review's own m10 records what it costs to hold a file and leave a known defect in it).
  - **R28 · the Tier 4 review fold records a verdict nobody wrote** (N-C1, critical; `vulyk-cycle.js:69-73`; N-m4, `cycle.sh:438-446`, folded). N-C1: "a `review` seat whose reviewer dispatches produced no report must not be recorded as a verdict at all — the round must reach `record-seat` with something that fails the report contract, or not reach it — and at Tier 4 a single surviving reviewer must be visible as such rather than folded with a blank." N-m4: "the verdict must be read from the line the contract names, so a report that does not open with it is MALFORMED." Decided: the driver folds only when both reviewer results open with `VERDICT: PASS|BLOCK`; otherwise the report it sends opens with `NO VERDICT: top=<first line|(no report)> · second=<…>` followed by both bodies verbatim, so `record-seat … review` rejects it (exit 4), the R6 re-ask runs once, and a second rejection leaves `review.attempt-2.md` — ABSENT — which R16 turns into `escalate:"env"` with `## Needs a human` pointing at the attempt files, where the surviving report is readable. A null report from any seat at any tier is sent as an empty body, never the string `null`. N-m4 is folded because the whole-report grep would score the fold's surviving body: C5 amended — `record-seat … review` reads line 1 only (`^VERDICT: (PASS|BLOCK)\b` or `^(PASS|BLOCK)\b`); any other first line is MALFORMED. Rejected: folding a blank to BLOCK (a verdict nobody wrote, fail-closed but still a lie in the row); skipping `record-seat` on a blank (no attempt file, no `env` path, the surviving report lost). → **story 26** (driver), **story 27** (line 1).
  - **R29 · an empty worker report is unbounded** (LR M4, N-M1; `vulyk-cycle.js:96-101`; `vulyk-build.md` `build:<wave>` row). Lead-review: "A worker that returns an empty report must count as a miss under the two-attempt bound". Second reviewer: "a wave story whose worker returns nothing must be bounded by the same two-attempt rule a red verification is, and end the run with the story named rather than re-dispatching forever." C11 amended: the per-story attempt map counts an empty or null worker report as a miss beside a red `close-story` (one map, two attempts in total); `close-story` is not run on an empty report; the second miss for one file ends the run with `stop:{verb:"build", file, error}`, and the wake-up step applies the blocked / `lead-architect` rule to any `stop` that carries `file`, whatever its `verb`. The fallback's `build:<wave>` row says the same. Not decided: validating `worker:` against the agent registry (N-M1's aside) — the bound closes the loop; a typo now costs two dispatches and a named stop. → **story 26**, **story 28**.
  - **R30 · a repair that lands nothing loops, and the repair prompt names only `red`** (LR M3; `vulyk-cycle.js:144-149`, `vulyk-build.md` `repair` row, `cycle.sh:357`). "A repair step that lands nothing must end the run, and the Workflow's repair prompt must name the `lead-review` BLOCK when `red` is empty". Decided, two amendments: C3 gains `"review"` — the newest row's `review` value verbatim (`PASS`, `BLOCK`, `ABSENT`, `""`), `null` when no row exists — so the driver names the review verdict without inferring it from `red`; C11 gains a repair bound — one `queen-planner` dispatch per round number per run; a `status` that still says `repair` for the same `round` after it (the planner cut no story, nothing landed) ends the run with `stop:{verb:"repair", round, error}`; the prompt names `red` and `review`, and when `red` is empty says the RED is the review seat's BLOCK (or an owner `REJECTED`), points at `<round_dir>/review.md`, and asks for one story per critical and per major finding whose fix is local — never "each addressing exactly one of those asks" over an empty list. The fallback's `repair` row carries the same bound and the same prompt. Rejected: inferring BLOCK from `red:[]` (C6 yields that for `REJECTED` too; the driver would be reading the verdict table); pointing the planner at `review.md` without the key (the driver would still not know why the round is RED). → **story 27** (key), **story 26**, **story 28**.
  - **R31 · the delimiter's entropy is the clock** (LR M1, N-M3 — C-4's first sink, downgraded to major; `vulyk-build.md` step 1, `vulyk-review.md` step 2, `vulyk-cycle.js:28,121-124`). Lead-review: "The `record-seat` heredoc delimiter must carry entropy a seat cannot bound from its own clock" and "an unset `args.stamp` also yields the constant `VULYK_undefined_<seat>_1`". Second reviewer: "the delimiter must come from a value a seat cannot bracket from its own clock — or the two command files and R11 must say what the construction actually buys ("a per-run value the seat is not told", not "cannot guess")." Decided, both halves: `stamp` is `od -An -tx1 -N8 /dev/urandom | tr -d ' \n'` (16 hex characters) taken once by the launcher — `/vulyk-build` step 1 for both drivers, `/vulyk-review` step 2 — and never `date`; the Workflow refuses to start when `args.stamp` is missing or shorter than 12 characters (`stop:{verb:"launch", error}` before the first clerk call); R11's "unpredictability is the whole guarantee, and is stated as such" and the command files' "a delimiter no report body can guess or contain" are withdrawn — both files say the report travels as free text inside the clerk's prompt and the delimiter is a per-run random value the seat is never told. Rejected: `$RANDOM` (a few digits, no floor on its length); the run id (not a value the launcher holds before the run). → **story 28**, **story 26**.
  - **R32 · `pipeline` swallows a `BadLine`** (N-M2; `vulyk-cycle.js:127-138`). "a non-JSON clerk line during a seat recording must end the run with the raw line, the same as every other clerk call, rather than being absorbed by the stage that made it." Decided: no `clerk()` call runs inside a `pipeline` stage — seats are dispatched under `parallel` and recorded one after another in plain loop code, where a `BadLine` propagates to the one catch that returns its line. → **story 26**.
  - **R33 · `emit` writes unescaped values into the last line** (N-M4; `cycle.sh:1376`). "every value `emit` places in the JSON must be escaped so the last line is parsable whatever the hive's commands contain." C2 restated: the last line is one JSON object under every input; a fixture `## Commands` cell with `"` and one with `\` prove it. → **story 27**.
  - **R34 · ADR D2's driver sketch shows the forbidden shape** (N-M5; `docs/adr/001-cycle-state-contract.md:168-186`). "the illustrative driver in D2 must not show a shape an in-force decision forbids, or it must be removed rather than left as the canonical sketch." Decided: removed, not rewritten — the canonical driver is `.claude/workflows/vulyk-cycle.js`; D2 keeps its prose, names the four `args` (`spec`, `top_model`, `second_model`, `stamp`), and `## Resume` names the same four. A sketch beside the file drifts on every repair; a pointer cannot. Folded, same file, said so: LR m12 — "`## Consequences` must not say "Blindness has a mechanism and a detector instead of an honour clause" beside a D5 that now says the opposite."; LR m13 — "D1 must list `council/REOPEN` (one writer, `reopen`, committed) and `tier=` as `ROUND`'s sixth line, and D2's `record-seat` precondition must read "not stale by `round_is_stale`" rather than "`head` in `ROUND` == HEAD", which story 17 made false."; N-m6 — "every file and field the cycle now writes must appear in D1's table with its writer." — `council/CEILING` (writer `reopen`, reader `open-round`) joins the two. → **story 29**.
  - **R35 · R14 is still unmet and story 22's note says otherwise** (LR M2, N-m8; story 22 `## Implementation notes`, `brief.md` `## Evidence` line 3). Lead-review: "the story must record a real fetch (URL, date, what changed) or say plainly the docs were not consulted and the shapes remain unverified". Second reviewer: "a verification note must point at a record that contains what it says was checked, or say the check did not happen." Decided: fetch, not disclaim — a worker with `WebFetch` reads `https://code.claude.com/docs/en/workflows` and records URL, date, what the page says for each of the six shapes the driver uses (`agent`, `parallel`, `pipeline`, `phase`, `log`, `meta`) and for the three runtime behaviours R28/R29/R32 rest on (`agent()` returning `null`, a throwing `parallel` thunk resolving to `null`, a throwing `pipeline` stage dropping its item), and what changed (expected: nothing in the call shapes — the second reviewer checked them against the same reference — and the three behaviours are what story 26 handles). Why fetch: a disclaimer would be honest but would carry a round-1 major into round 3 unmet, while the driver's fixes in this very round rest on behaviours that reference documents; the fetch costs one call. The disclaimer is the fallback the story states for an unreachable page. The record replaces the false bullet in story 22 and says what that bullet claimed. → **story 29**.
  - **Not folded, verbatim under `## Next circle`:** lead-review round-2 minors 1-11, 14-18; second reviewer N-m1, N-m2, N-m3, N-m5, N-m7.
  - **Contract amendments in force from this entry** (as for delta 6, the `## Contracts` text above is not rewritten): C2 last line escaped per R33; C3 key `"review"` per R30; C5 `review` verdict from line 1 per R28; C11 empty-report miss per R29, repair bound per R30, `args.stamp` guard and no `clerk()` inside `pipeline` per R31/R32; delta 6 R11's "unpredictability is the whole guarantee" clause withdrawn per R31. ADR-001 D1/D2 amended by story 29; status stays proposed.
  - **Rejected for the round as a whole:** fixing N-C1 alone and re-running (the same driver has three more unbounded or silent paths — R29, R30, R32 — each the class the second reviewer blocked on); pulling the eighteen-plus-eight minors in (round 3 is the ceiling; each minor is recorded, none is a block).

<!--
The four lines below are the cycle's confirmation artifacts (docs/cycle.md): one per stage
whose command refuses without the one before it. Each placeholder is replaced by the
command or script that owns the line; `scripts/ship-check.sh` reads all four.
-->
**Approved:** Andrei, 2026-09-12 — "Одобряю с предложенными правками" (coverage findings a-f resolved in ## Assumptions; ADR-001 accepted as the contract, status stays proposed until it ships)
**Briefed:** <written by scripts/cycle.sh briefed at grill close - stage 01+02 in autonomous mode; /vulyk-build and ship-check.sh accept this or Approved>
**Branch:** vulyk/autonomous-cycle
**Checked:** <written by scripts/human-check.sh after the owner has looked - stage 05. /vulyk-ship refuses without it.>
**Council:** <appended by scripts/cycle.sh judge, one line per round - stage 04+05 in autonomous mode; ship-check.sh reads the newest memory/stats/council.jsonl row>
**Council:** RED round 1, 2026-09-13, at f0d206b, pack f455a9c64779
**Council:** RED round 2, 2026-09-13, at 19667a3, pack ab62eb9de683
**Council:** GREEN round 3, 2026-09-13, at 7bc6f7b, pack c0a97d497e2a
**Shipped:** 0.12.0, 2026-09-13, at a7e8c5d - merged to main, publish pending
